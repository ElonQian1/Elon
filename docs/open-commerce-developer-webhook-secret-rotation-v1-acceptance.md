# 开放商业开发者 Webhook 签名密钥轮换 V1 验收

## 当前状态

`implementation_uncompiled`

本批次只形成代码、执行格式化及仓库源码和文档门禁，尚未编译、迁移或验证真实轮换流程。

## 已形成代码

- V147 持久化订阅级签名密钥版本，版本 1 保持既有密钥派生兼容。
- App 所有者可显式轮换，新密钥只返回一次。
- 轮换以旧版本为并发条件，原子停用订阅、重置端点验证并终止旧密钥下未完成投递。
- PC 门户展示密钥版本并提供轮换入口，提示保存新密钥后重新验证。

## 统一回归必须验证

- V146 到 V147 升级、重复迁移、版本 1 既有密钥逐字节兼容。
- 并发轮换只允许一个请求成功，版本连续且不会重复展示密钥。
- 旧密钥在轮换后无法通过 challenge 或正式事件验签，新密钥可以完成验证。
- 轮换与工作器领取、完成、失败、App 停用及订阅启停并发时的失败关闭行为。
- PC 类型、按钮状态、一次性密钥展示和小屏布局。

## 仍未完成

- 自动轮换策略、双密钥过渡窗口、外部密钥管理系统和跨运营方证书互认。
