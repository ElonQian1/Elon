# 开放商业开发者 Webhook 死信人工重试 V1 验收

## 当前状态

`implementation_uncompiled`

本批次只形成代码、执行格式化及仓库源码和文档门禁，尚未编译、迁移或向真实端点重试投递。

## 已形成代码

- V148 保存人工重试次数和最近人工重试时间。
- 只有已验证且启用订阅下的死信可以原地重新排队。
- 同一投递 ID 重置本轮自动尝试状态，不复制终态事件或业务动作。
- PC 门户展示人工重试轮次，并仅在死信状态提供显式重试按钮。

## 统一回归必须验证

- V147 到 V148 升级、重复迁移和既有投递默认值。
- 同一死信并发重试只成功一次，人工重试轮次准确递增。
- 已送达、等待中、自动重试中和投递中的记录全部拒绝人工重试。
- 订阅停用、验证失效、App 停用、密钥轮换与工作器领取之间的并发边界。
- 人工重试后最多执行新一轮八次自动尝试，成功与再次死信状态正确。
- PC 类型、按钮状态、错误提示和小屏布局。

## 仍未完成

- 批量死信处理、按时间窗回放、外部告警通知和自动修复策略。
