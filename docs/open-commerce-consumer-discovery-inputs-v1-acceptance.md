# 消费者发现请求输入边界 V1 验收

状态：`implementation_uncompiled`

## 已形成代码

- 服务端规范化搜索词、能力键、调用方 App ID 和返回数量。
- 空可选值不再作为无效精确条件进入目录查询。
- SQL LIKE 特殊字符按普通字符查询。
- 规范化后的返回数量进入结果截断和排序凭证输入指纹。

## 最终统一回归

- 空白、边界长度、超长和控制字符搜索词。
- 合法、大小写混合、空白和非法能力键/App ID。
- `limit` 为 0、1、50 和超大值时的返回及凭证一致性。
- `%`、`_`、反斜杠的文字匹配不扩大候选范围。
- HTTP、MCP 和 PC 调用的行为一致性。

本批按快速实现策略只完成格式化、静态差异和文件规模检查；未执行 Cargo 编译、数据库查询测试、接口测试或端到端验证。
