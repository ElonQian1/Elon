---
version_status: current
reviewed_at: 2026-08-03
---

# 一龙项目当前事实

本文件是 Codex、Copilot、Claude、Gemini 和平台内 AI 的短事实快照。开始实现或判断“项目现在有什么”时先读本文件；详细架构、接口和历史原因再按链接读取。讨论、草稿和 Git 历史不能覆盖这里及正式决策。

## 当前产品主链

- 已实现：用户通过 Android APK 或 PC 工作台描述需求，AI CLI/API 代理在真实 Git 工作区开发、验证、构建和发布应用。
- 已实现：项目、频道、多人/多 AI 协作、Windows 节点执行、上下文编译、项目文档治理与版本恢复等基础能力。
- 已形成第二阶段代码、未编译：Windows 节点会为普通 Codex 编码任务注入仅含 `project_context_plan` 的项目文档 `context` profile；它最多规划 8 份文档和 2400 token 后续阅读，并把自身结构化响应限制在约 800–2000 token。clean worktree 的同 revision/查询使用 5 分钟、64 项 LRU；`plan_receipt.plan_id` 允许重复调用只返回 `not_modified` 小回执。dirty worktree 禁用缓存。响应只保留少量路径、标题、图谱入口和实现引用，不返回正文；歧义、非当前、非权威与 dirty 信源只作显式元数据警告，不冒充语义冲突已比较。精确单文件任务由 MCP 指令要求跳过，完整治理工具仍只对显式文档任务开放；其他 CLI 普通任务暂不自动注入。本批次按快速实现策略未编译、未测试。
- 已形成代码、尚未完成平台运行验收：`yilong-ui-live` MCP 可在用户不打开微调画布时发现项目的 Web、PWA、Tauri 和 Android 目标，建立可由同一 canonical Git 项目的新 MCP/PC 会话恢复的后台 `designSessionId`；Web/PWA/Tauri 前端复用受控无头浏览器生成 PNG 与可查询的紧凑语义 UI 树，并只允许 click/waitFor/assertText 重放，Android 继续要求真实 Live Runtime。PC `/pc/ui-tuner` 已形成“左侧平台/会话/UI 树、中间证据与语义选区、右侧默认 Codex 对话”的同数据面客户端代码。Tauri 当前只覆盖前端 WebView，不能声明原生宿主已验证；通用多端可撤销草稿、源码绑定/写回回执和平台运行验收仍未完成。权威边界见 `docs/headless-ui-design-mcp.md`。
- 已实现：开放商业网络 V1 的商户节点、能力、授权、调用、计量和审计主干；HTTP 与 MCP 共用领域服务。能力输入和处理器输出已按有限 Schema 配置失败关闭，无效输入不创建调用，无效输出以零金额失败并释放 Grant 预算，历史成功结果重放前按当前输出契约复验。
- 已实现、生产配置依赖环境：受控 `merchant_runtime` 可把平台调用安全转交给商户自有 ERP 后端；首个 `cofficethinking` 参考节点已覆盖商品发现、服务端报价、用户确认后的幂等下单和订单查询。连接器 SDK 已落位通用 Node 运行时内核，统一签名、重放保护、Grant、订单确认、幂等生命周期、Manifest 和响应信封；商户仍需实现自己的业务处理器与生产持久化存储。运行时每次调用重新核对标准 443 HTTPS 白名单，并形成 DNS 私网拒绝、禁代理、禁重定向和单次公网地址固定代码，但该新增安全批次尚未编译或网络验证。真实公网启用仍需 HTTPS 域名、白名单和双方密钥，通用内核完整回归按当前策略后置。
- 已实现、范围受限：项目成员可按商户读取终态能力调用证据、结果摘要、可选标准业务回执和当前 ERP 实例关联；新商户运行响应中的非法标准回执失败关闭。项目编辑者可保存 `project_editor_asserted` 人工衔接回执；每个数据接入也可签发权限固定为 `business_handoff.write`、1–366 天限时的一次性机器 Token，服务端仅保存 SHA-256，轮换、撤销、到期和接入停用立即失败关闭，机器回执以 `adapter_token_authenticated` 固化凭据 ID 和版本。升级前已有凭据补 90 天期限。`applied` 必须绑定有效标准业务回执和外部目标记录摘要。系统从业务证据和最新回执实时派生待处理与需重试队列，成功或忽略自动移出，失败保留重试。机器鉴权不是外部平台签名或回读；队列不自动写入 ERP/CRM，不创建第二套订单账本，也不证明真实支付、履约或退款。
- 已实现：通用 ERP 蓝图 V1.3 可按修订号追加蓝图定义、发布不可变版本、新建独立商户项目或把用户有编辑权的现有项目纳入实例治理、按固定版本检索能力、聚合经授权的脱敏通用提案、原子创建提案或实例初始化 Matter，以及保存升级前后配置快照、商户执行证据和完整回滚；现有项目纳入不会复制、覆盖或清理原项目，初始化 Matter 会先要求盘点已有能力。工作台和 AI 可从 Matter、Assignment 与 Artifact 真源读取实时状态、失败任务和证据缺口。AI 不得自动接受提案、演进蓝图、启动或合并任务、发布公共内核或采用升级。权威入口为 `docs/erp/README.md`。
- 已实现：商户数据来源接入控制面、幂等同步回执及不含密钥和原始经营数据的 AI 开发上下文；具体大型平台生产适配器仍未完成。
- 已实现、范围受限：连接器 SDK、商户主动发布的跨项目脱敏目录、消费者发现与第三方应用沙盒；可以验证非付费发现、显式限时授权、App 身份绑定、停用与新凭据恢复、申请方撤回、测试 Token 调用、固定时间窗配额、单个 Grant 的总调用与总计量预算，以及商户手动封禁具体 App 并原子撤销其授权；PC 新授权默认 30 天，长期授权需显式选择，批准期限和预算对双方可见，到期后发现与调用失败关闭。消费者可对已发布商户建立最长 366 天、可随时撤销和安全续期的匿名关系凭证；关系凭证本身不保存偏好值。消费者还可保存类别、标签、城市和价格上限等低敏结构化偏好，显式用于本次发现，并对有效 `preference.remember` 关系选择字段生成匿名快照；商户只读取仍有效关系的披露，档案更新、关系续期或失效不会自动扩张访问。消费者也可发起原子撤销关系的关联数据删除请求，并把本人关系、私有续期链、请求回执、当前低敏偏好档案、历史披露快照和账户级终态调用凭证保存为幂等不可变 V3 数据包；服务端与 PC 同时复核总包及每条凭证的 SHA-256，历史 V1/V2 包保持逐字节摘要兼容。接收方可把外部 V1/V2/V3 包完成完整性验证后保存为当前用户和项目独享的隔离快照；消费者还能登记来源运营方 RSA 公钥，来源 SDK 在本地签名，接收方把验证通过的快照标记为导入时签名可信。用户登记公钥仍不是全网身份认证，所有导入仍不会恢复关系、生成 Grant、合并偏好、写 ERP、创建订单或结算。凭证可包含消费者已经获得的商户返回结果，但不含原始调用输入和内部标识，也不等于商户数据库完整订单、真实支付或履约证明；数据包仍不含联系方式或消费者账号。本人账户还可跨项目单独列出、读取和下载这些调用凭证，并固定说明当前未扣真实资金。删除档案只移除平台内档案和披露，不冒充商户外部删除证明；商户 `completed` 也仅是可审计声明。失败调用退回预算，孤儿调用失败关闭并释放预留；商户侧活动证据只提醒人工处置，不自动评分或封禁。它仍不是具备生产审核、真实扣款、完整订单迁移、全网可信来源互认、偏好跨运营方合并、外部通知、动态风控、敏感数据保险箱、外部删除适配器和全网滥用治理的公共网络。隔离导入基础批次曾完成编译检查；随后签名信任批次按快速策略未编译、未测试，统一回归仍待最终执行。
- 已实现、范围受限：PC 消费者沙盒可按商户发布的有限输入契约生成非技术调用表单，未声明默认值的可选字段默认省略，无法安全呈现的契约失败关闭。`action` 还必须经过服务端 5 分钟两阶段确认；确认绑定当前用户、App、商户、能力、Grant、幂等键和输入摘要，并与 Invocation 创建原子消费，同一幂等请求可重放但不能产生第二次动作。开发者测试 App 可按绑定游标轮询自己的终态调用摘要并读取单条结果，数据库在终态转换时原子追加一次序号；已验证部分仍是可恢复轮询，不是跨运营方事件总线。该确认不包含 WebAuthn 或跨设备可信展示，不替代商户运行时内部订单、支付、风控或履约确认。
- 已形成代码、未编译：开发者 App 可配置标准 443 端口的 HTTPS Webhook，并在创建时固定沙箱或生产环境以及成功、失败事件范围；既有订阅迁移为沙箱。新订阅先停用，接收端完成签名 challenge 精确回显后才从当前终态序号开始。终态事件按环境原子进入耐久队列，工作器使用短租约、指数退避、死信和自动停用；外呼拒绝私网或特殊 DNS 地址、禁用代理和重定向，并固定本次公网连接。订阅密钥按订阅和版本派生且只在创建或显式轮换时显示一次；单条死信可重试，历史通知最多幂等补发 100 条。生产订阅默认关闭，必须同时开启生产 Webhook 与生产凭据开关，并持续满足 App、资料、域名、准入及凭据条件；撤销、轮换、到期或资格失效会停用生产订阅并终止未完成投递。PC 可选择环境和事件、验证端点、轮换密钥、管理启停、查看投递、重试与补发，并按沙箱/生产查看只读订阅、积压、重试、待处理与已确认死信和生产阻断健康摘要；项目编辑者可用不可覆盖的操作人、时间和原因确认不再重试的单条死信，原失败记录继续保留，后续人工重试会重新打开处理链路。本批次尚未编译、迁移、DNS、TLS、并发、跨语言签名、网络或 UI 验证；它不是工商身份、跨运营方总线、支付通知或 ERP 写入证明。
- 已形成代码、未编译：开发者 App 可维护 HTTPS 主页、隐私政策、服务条款、支持邮箱和拟申请能力，以修订号保存、提交并由平台管理员批准或要求修改；编辑会清除旧结论并退回草稿，所有变化进入审计。提交前必须把一次性 challenge 发布到主页同源固定 `well-known` 地址并完成当前修订的域名控制证明；出站主机由运营方精确白名单控制，并在请求前拒绝私网或特殊 DNS 地址、固定本次公网连接。PC 已提供资料维护、域名验证、提交和仅系统管理员可见的待审工作台。资料或域名 `approved/verified` 不改变沙盒环境、不签发生产凭据，也不是组织身份、域名法律所有权、真实交易或外部平台授权证明；本批次尚未编译、迁移、DNS、TLS、网络、权限或 UI 验证。
- 已形成代码、未编译：资料审核和当前域名证明通过后，App 所有者或项目管理员可提交主体名称、注册地区、登记编号和明确声明，平台管理员可批准、退回或暂停独立准入记录并固定人工风险层级。资料变化会原子失效待审或已批准记录，App 停用会原子暂停记录，重新启用不会自动恢复。主体信息仍是申请人声明，未连接工商或第三方身份核验；`approved` 本身不签发生产凭据、不改变沙盒 App，也不开启真实资金或外部平台权限。本批次尚未编译、迁移、接口、权限或 UI 验证。
- 已形成代码、未编译、默认关闭：平台管理员可在当前资料、主页域名证明和准入记录均有效时，为开发者 App 显式签发范围不超过已审申请、受人工风险层级期限约束的 `oc_live_` 生产凭据。完整密钥只显示一次，数据库仅保存 SHA-256；再次签发会原子撤销旧凭据。生产调用、动作准备和动作确认复核能力范围；调用账本保存平台/沙箱/生产环境及非秘密凭据 ID，幂等重放、终态事件和 Webhook 按环境隔离，沙箱不得触达真实商户运行时。资料修改、域名重验、App 停用或准入暂停会原子撤销，并停用关联生产 Webhook。PC 可签发、查看元数据和紧急撤销，并通过只读生产就绪总览按固定顺序查看 App、资料、域名、准入、凭据和生产 Webhook 阻断，分别判断生产调用与通知是否就绪。`OPEN_COMMERCE_PRODUCTION_CREDENTIALS_ENABLED` 未显式开启时签发和生产鉴权失败关闭。它仍不是外部组织核验、真实平台授权、真实资金、链上资产或生产部署证明；本批次尚未编译、迁移、接口、权限、并发、网络或 UI 验证。
- 已形成代码、未编译：消费者可为隔离导入生成低敏偏好差异和关系重新授权候选，只能在明确确认及修订号未变化时采用偏好；采用保存前后快照，且仅在档案未发生后续修改时允许回滚。它不恢复关系、Grant、披露，不写入订单、ERP 或结算。该批次按快速开发策略尚未编译、构建或测试。
- 已形成代码、未编译：PC 与连接器 SDK 可用本地口令把普通或已签名消费者数据包包装为 PBKDF2-SHA256 + AES-256-GCM 离线归档，并在上传前于消费者设备解密。口令不进入服务端；当前没有找回或云托管，且该批次尚未进行构建、往返或跨端互操作验证。
- 已形成代码、未编译：消费者可把隔离包内来源关系人工映射到当前有效发布商户，PC 端可复用公开目录搜索并选择目标，再用本人独立开发者 App 发起一笔范围不超过旧关系的新授权申请。映射只记录 `consumer_confirmed`，目录搜索不构成身份认证，旧关系和旧 Grant 不复制，目标商户仍需重新决定。该批次尚未编译、构建或测试。
- 已形成代码、未编译：项目编辑者可在本地生成商户 RSA 密钥，下载私钥后发布经持有证明验证的公钥指纹；有效指纹随商户开放目录对外可见，可撤销且不可恢复。新导出使用 V4 并携带关系引用商户的有效指纹声明；仅当来源运营方签名已验证时，才可生成当前已发布商户的精确指纹匹配候选。映射仍需消费者确认和商户重新授权；指纹不是工商认证，也不会自动合并商户或关系。V1/V2/V3 仍保留导入兼容路径。该批次尚未编译、构建、SDK、摘要兼容或密码学往返测试。
- 已实现、只预演：项目 AI 资源控制面可盘点自己的 Codex、本人节点、已授权共享 Codex 和平台模型并保存策略；它不启动任务、不验证外部额度，也未接管真实调度。
- 已实现、默认关闭，离线适配器批次未编译：AI 任务与开放商业调用的链外影子经济层，可生成幂等用量凭证、验收后双分录，以及未提交网络的 Sui 链下投影包；追加式争议可阻断投影，已接受争议可创建独立纠正 Matter，人工验收后原子追加冲销与替换双分录而不改写原凭证。两条腿可共同保存为原子纠正包；系统还能从任意标准、冲销或替换凭证只读解析根、纠正步骤和当前有效凭证，异常循环或分叉失败关闭。标准包和纠正包可导出统一离线交接文件；项目还能签发限时、限网络和限包类型的离线预检机器凭据，外部工具只有在服务端只读重建交接包且摘要一致时才能追加 `passed/rejected` 报告。项目编辑者可将当前可导出的投影显式加入预检队列，机器按权限领取 60 至 900 秒短时租约，交接摘要漂移会阻断任务，报告与任务完成原子写入。连接器 SDK 和 CLI 已写入本地复核、显式报告及 `claim/renew/release/complete` 租约命令，适配器和租约凭据不接受命令行参数；这些命令尚未运行。报告和任务不改变投影状态，也不授权签名、广播、最终性或资金移动。当前没有 Sui 网络适配器，不移动真实资金。
- 已实现但边界有限：API Token 保管、远程节点、计算使用台账，以及默认关闭的节点模型算力共享策略。节点所有者可选择共享模型、最大并发和每日 Token 预算；指定与自动调度会在同一事务中检查今日实耗、活动租约预留和本次保守预留，所有者自用不受共享限制。可信终态先冻结实际 Token，再进入异步结算；提前断流、真正租约过期和服务重启会失败关闭并幂等释放预授权，最终状态不接受迟到覆盖。所有者控制面会从持久化记录派生近 24 小时失败、预留超出和过期租约健康快照。该快照不是 SLA 裁决或自动赔付。它目前仍不是包含异构任务、竞价、故障赔付、真实提现或链上结算的开放算力市场。

## 已接受的产品方向

- 先用 AI 应用开发、ERP、营销内容、小游戏和经营分析帮助商户获得直接价值，再逐步形成开放商业网络。
- 商户数据由商户控制；消费者 AI、商户 AI 和第三方应用通过授权接口连接，不把所有参与者锁进同一个 App。
- 开放商业当前权威入口是 `docs/open-commerce/README.md`；实现状态先看 `docs/open-commerce/capability-baseline.md`。
- 商户 ERP 采用“公共内核 + 行业插件 + 商户主题 + 私有扩展”的分层模型；`cofficethinking` 与最小零售参考实例已证明同内核可保持不同主题和插件，商户私有扩展不随公共版本升级被覆盖。

## 尚未成为当前事实

- 接入器任务领取、短时租约、1 小时硬期限内续租、一次性租约密钥、原子完成、主动释放、拒绝退避、第 6 次拒绝暂停、编辑者恢复、Connector SDK 客户端、通用 ERP/CRM 适配器工作器和项目侧租约监控代码已经落位，并已通过 Rust 编译、PC 生产构建及 SDK 模块语法检查。按当前“先连续实现、最终统一回归”的策略，完整 Rust、迁移、HTTP、PC、SDK 和文档门禁仍后置；统一验证与发布复核完成前不能宣称生产可用。既有凭据不会自动获得读取权，只有项目编辑者明确轮换并加入 `business_handoff.claim` 后才能领取单条衔接任务。
- 消费者 AI 的跨项目基础目录与授权沙盒已实现；节点模型推理的显式供给授权已实现。生产应用互认、公共网络治理、异构闲置算力公开市场、自动收费与跨主体结算仍在路线图或提案阶段。
- 通用 ERP V1.3 用初始化 Matter 和可机读物化合同衔接真实项目开发，并从既有任务与产物记录派生可恢复状态；它能登记现有平台项目，但不会自动导入本机目录、复制商户源码、启动 Assignment、执行数据库迁移或部署生产系统。`cofficethinking` 仍需先在一龙创建或绑定对应平台项目，再由有编辑权的用户显式纳入。Matter 仍需商户批准和启动，真实商户项目继续走现有项目开发和发布流程。
- Sui Move 合约、测试网/主网提交、NET、CREDIT、RevenuePosition、链上治理和收入权益均是草案；链外 Sui 信封不等于已上链、已发币或公司股权。
- 任何提案只有经过正式 ADR、实现引用和验收后，才能进入本文件的“已实现”部分。

## 明确禁用

- 不设置独立“预言家 AI / Demo Oracle / DemoPreview”前置角色。见 `docs/decisions/reject-demo-oracle-role.md`。
- 不恢复 AI-to-AI Skill 路线或 Skill 市场作为当前主架构。多 AI 协作继续使用项目频道、Matter/Assignment、运行路线和供应商工具协议。见 `docs/decisions/reject-ai-to-ai-skill-route.md`。
- Codex 桌面监督与 PC 自动续跑自 2026-07-26 起暂停；暂停解除前不得自动派发或恢复该流程。

## 读取顺序

1. 当前状态：本文件。
2. 项目定位：`AI_PROJECT.md`。
3. 系统分层：`AI_ARCHITECTURE.md`。
4. 实现入口：`AI_INDEX.md`。
5. 开放商业专题：`docs/open-commerce/README.md`。
6. 通用 ERP 蓝图：`docs/erp/README.md`。
