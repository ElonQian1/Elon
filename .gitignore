# Rust
/target/
server/target/
server/cargo-config.toml
server/Cargo.lock

# Android build outputs & local config
android/.gradle/
android/*/build/
android/app/build/
android/chat-voice-kit/build/
android/local.properties
android/*.apk
android/app/*.jks
android/app/*.keystore
# Signing keys anywhere in the repo root
/*.jks
/*.keystore

# Environment secrets
.env.local
.env.*.local
server/.env
!server/.env.example

# OS
.DS_Store
Thumbs.db

# VS Code workspace file
*.code-workspace

# Temporary AI deploy helpers
scripts/_wsl_deploy.sh
scripts/_deploy_temp*.sh
.runtime/
/.elon/ui-design/
.elon/ui-tuner/live/
.elon/ui-tuner/fit-runs/

# 本地运行目录（start-local.ps1 自动生成）
data-local/
workspaces-local/

# node-agent 启动器目录里的本地 exe（不纳入版本管理）
scripts/node-agent-launcher/*.exe
scripts/node-agent-launcher/elon-node-agent.exe.new
