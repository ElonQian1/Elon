# 消费者 AI 单能力授权申请 MCP V1 验收

状态：`implementation_uncompiled`

## 已形成代码

- 默认 MCP 身份、非本人 App、错误确认短语和仍能覆盖下一次调用的已有 Grant 失败关闭。
- 单能力范围进入既有授权申请服务和审计账本。
- 相同待审批范围及用途保持既有幂等语义。
- 执行计划把申请工具标为需要用户明确确认的下一步。
- 现有 Grant 全部预算不足或币种不匹配时允许重新申请，不改写旧 Grant。

## 最终统一回归

- 默认身份、自有 App、非本人 App、停用或被封 App。
- 公开、受限、未发布和不存在的能力。
- 首次申请、相同重放、用途变化、已有 Grant 和已有不同待审批请求。
- 多 Grant 中至少一张可用、全部次数耗尽、单位价格超过剩余金额、零价能力和预算币种不匹配。
- 新 Grant 批准前旧 Grant、历史申请和已用预算保持不变。
- 审计字段、商户收件箱、批准/拒绝以及期限和预算仍由商户决定。
- 工具调用前后不存在 Invocation、订单、计量或结算副作用。

本批按快速实现策略只完成 Rust 格式化、静态差异和文件规模检查；未执行 Cargo 编译、MCP 调用、数据库或端到端验证。
