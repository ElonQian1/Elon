# Writing And Retry Grouped Delivery

## Published Build

`writing-retry-grouped-release-20260914-135352-338` passed in 493.9 seconds:
Release Kotlin/Java, lintVital, assembleRelease, upload and remote byte/hash
verification. Source `bfd87daaecdeef9374cfaa041e5307e01e92ccd9`, adapter 391,
APK **1.1.1723 / 1723**, 40,469,711 bytes, SHA-256
`431b0600d1386eba89fd9ecbcf7de4b47ba2d08f66e6440f104a7ca7ea7dc7c8`.
The publisher installed it without clearing data on the whitelisted Xiaomi.
The later wireless connection was verified as the same handset. Its production
native chat reported authenticated, adapter 391, no draft, idle voice and no
active generation. No microphone, proxy or Cookie setting was changed.

This groups the already-tested temporary Writing Block save, PDF exporter and
runtime-parent retry correction. It does not promote unverified private retry
to the production default. The previous 51 Android tests and four native PDF
renderer cases are retained evidence, not repeated or renamed as UI acceptance.

## Earlier Retry State

Before installation, the old APK's background snapshot appeared unauthenticated.
Returning the existing MainActivity to the foreground restored authenticated /
ready; it was not evidence of expired login.

Read-only checks found the exact controlled regenerate fixture, one completed
assistant response with its marker, one user and no active generation. Three
page acknowledgements preserved document generation, route, turns and the exact
fixed unsent test draft. A fresh history GET returned the owned current branch
with complete pagination; the private ledger was idle, unarmed and not pending.
The runtime stop diagnostic had no cached generation/request owner.

Only `ELON_RETRY_UNSENT_DRAFT_V1` was then cleared, through the existing exact-
match cleanup. The earlier uncertain runtime write was **not replayed**. These
checks establish a stable present state, not a retrospective success receipt
for the old timed-out request. Do not relabel that request as successful.

## Production PDF Attempt

The existing semantic UI harness now accepts PDF in the native format picker,
reuses the controlled Unicode/table/code document fixture and bounds the saved
artifact. It checks source identity, file size, transfer SHA-256 and PDF magic.
For PDF it explicitly emits `parser_verified=false`: opening the container is
not text or renderer verification, and a source hash is not a PDF file hash.
The existing Word evidence harness passed 63 checks in
`writing-pdf-ui-harness-tests-20260914-135828-371` (10.2 seconds).

`writing-pdf-production-ui-1723-20260914-140446-925` was interrupted after
selecting the existing controlled Writing Block conversation, before native
editor admission. It failed `foreground_package_mismatch`; a subsequent OS
foreground read showed the phone's system in-call Activity. No PDF was produced,
no new message or cloud write was sent, and the awake lease was restored.
Original conversation restoration was **not confirmed**. Do not close a call or
force navigation to make this test pass. Reinspect the current foreground and
draft before resuming; do not assume the old fixture is still selected.

## Remaining Acceptance

- Native PDF copy export passed below. Opening an external phone PDF reader
  was not tested; exact Unicode extraction remains the documented limitation.
- Temporary/project/widget/Library Writing Block cloud save: controlled owner-
  specific device evidence; ordinary save 1692 is not proof for those variants.
- Independent regenerate failed admission below; an owned private stream and
  reconciliation still need evidence. Do not promote the runtime success.

Reuse completed local editing, undo/redo, Markdown/source export, ordinary
Writing Block save and Word 1718 acceptance. Shared-copy writes remain separate.

## Resumed Production PDF Acceptance

After the foreground returned to MainActivity, a read-only telephony check found
both call states idle. The resumed test first failed
`requested_export_format_missing` in
`writing-pdf-production-ui-resumed-1723-20260914-141103-691` (31.2 seconds).
The production format label is exactly `PDF`; the harness incorrectly expected
`(.pdf)`. This was a harness defect, not absence of the product capability.
Cleanup restored the editor source, original view and awake lease.

After matching the actual label, the same 1723 APK passed
`writing-pdf-production-ui-label-fixed-1723-20260914-141338-933` in 51 seconds:
native open/edit, undo/redo, PDF selection and export, saved-file hash, export
actions, share-picker open/cancel, reset/reopen and original view restoration.
No new message, cloud write or third-party share was made. The external Java/D8
runner compiled and executed. Its focused evidence regression passed 64 checks
in `writing-pdf-ui-label-regression-20260914-141423-454` (1.9 seconds).

The saved file is a 60,689-byte, one-page unencrypted PDF, SHA-256
`5cdbc752ba9cb8a05b97ef6d3d97459ee3acfc2145cee48a87a46f088b056bb6`.
Independent strict `pypdf` parsing found all ten synthetic paragraphs after NFKC
and whitespace normalization. Exact text and literal marker extraction did not
pass; normalization is not an exact-copy fix. Poppler rendered the actual file;
visual inspection confirmed Chinese, emoji, emphasis, numbering, a table and
indented code without clipping or overlap. This is PDF copy/export acceptance,
not an external phone-reader or exact text-round-trip claim.

The synthetic PDF/preview are retained outside worktrees under Git common
`ai-research-artifacts/writing-pdf-production-1723-20260914/`. PNG SHA-256:
`cfdcb17bce82e1382ed26642b2f0d4056cdd1b4fb772139dc1583411c9354382`.
Reuse this completed `android_chatgpt_writing_block_pdf_export_v1` copy scope;
do not repeat it absent a current regression.

## Corrected Retry Attempt

`fresh-retry-native-corrected-1723-20260914-141551-927` ran once on adapter 391
(53.9 seconds). It created a new isolated fixture and obtained a complete seed
reply, then pressed the production native retry button with an unsent test draft.
The retry produced `official_runtime_v1:regenerate_observed`, but the independent
HTTP assertion failed. Retained page command receipts, read without another
write, showed `phase=rejected`, `code=scope_unsupported`, `dispatched=false`,
`accepted=false`, `pending=false`, no stream events and no reconciliation.

The earlier parent correction is therefore not sufficient for this live owner.
Do not guess which remaining admission predicate failed or weaken the owner
checks. The test closed its trial, cleared only its exact synthetic draft and
restored the prior view; the subsequent state was idle with no draft. No retry
was replayed. Next work is a read-only diagnosis of the rejected owner/context,
not another blind send or promotion to default.
