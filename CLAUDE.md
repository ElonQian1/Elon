# Claude 项目入口

先读 `.github/copilot-instructions.md`、`AGENTS.md` 和 `AI_CURRENT.md`，再按任务路由读取专项文档。
共享规则是唯一真源，不在本文件复制任务生命周期或另设一套权限。

Win 无人值守更新、重启、打开功能和会话读取验收按共享规则中的
[执行入口](docs/win-conversation-unattended-acceptance.md)。已有授权时直接执行；实际工具限制如实报告。
