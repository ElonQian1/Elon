# APP 版本记忆

最后更新：2026-06-22

## 当前产品版本代号

一龙 APP 当前这个版本叫：**初始01版本**。

## 边界说明

- 这里记录的是给人和 AI 代理理解用的产品版本代号。
- 这不是 APK 发布脚本使用的 `versionCode` / `versionName`。
- 这不是后端 `server/Cargo.toml` 的 `package.version`。
- 发布版本号仍由服务器发布流程和脚本分配，禁止为了这个代号手动递增构建版本字段。
