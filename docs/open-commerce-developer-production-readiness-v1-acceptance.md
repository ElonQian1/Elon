# 开放商业开发者 App 生产就绪总览 V1 验收

## 当前状态

`implementation_uncompiled`

本批只形成代码、Rust 格式整理及仓库静态门禁，尚未编译、运行 API、查询真实数据库或验证 PC 页面。

## 已形成代码

- App 级生产就绪 API，复用项目编辑权限和 App 管理归属边界。
- 八个固定步骤、稳定阻断码、按依赖顺序返回的下一步，以及完整阻断列表。
- 生产调用和生产 Webhook 两个独立就绪结论，避免“有凭据”等同于“通知已配置”。
- 当前资料修订与域名、准入、凭据绑定检查，以及生产双开关和活动生产订阅检查。
- PC 独立总览组件，可选择 App、刷新状态并把内部阻断码显示为明确操作提示。
- 全部状态只读派生，不新增数据库迁移，不自动改变 App、审核、准入、凭据或订阅。

## 统一回归必须验证

- App 停用及重新启用、资料草稿/待审/退回/批准和资料修订变化。
- 域名待验证、失败、过期修订和当前修订验证成功。
- 准入缺失、待审、退回、批准、暂停和旧修订记录。
- 生产凭据开关关闭、无凭据、凭据到期/撤销/旧修订和当前有效凭据。
- 生产 Webhook 开关关闭、无订阅、待验证、停用和活动生产订阅。
- 多项同时阻断时的步骤顺序、`next_action_code`、调用就绪与通知就绪组合。
- 跨项目、非所有者、只读成员、项目管理员和不存在 App 的权限结果。
- PC 快速切换 App、过期响应、刷新、空列表、小屏布局和生产构建。

## 仍未完成

- Rust/TypeScript 编译、API、权限、状态竞争、性能和 UI 验证。
- 自动修复、外部告警、SLA、真实平台授权、真实资金、链上提交和生产部署证明。
