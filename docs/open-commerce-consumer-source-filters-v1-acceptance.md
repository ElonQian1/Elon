# 开放商业消费者来源厂商与数据域筛选 V1 验收

## 当前状态

`implementation_uncompiled`

## 已形成代码

- 两个可选请求字段及服务端规范化。
- 能力候选层的厂商和数据域精确匹配。
- 规范化条件响应回显与内部回执要求派生。
- 匹配原因、PC 输入和结果标志。
- 请求指纹与排序凭证中的来源过滤对象。

## 统一回归必须验证

- 空值、空白、大小写、合法边界和非法标识的规范化结果。
- 单厂商、单数据域、两者组合和无结果查询。
- 与内部回执要求、声明期要求、能力键、价格上限和五种排序器组合。
- 普通商户声明不会因名称相似被错误命中。
- 条件进入输入摘要和凭证负载，切换后摘要不同。
- PC 输入、响应标志、错误提示和窄屏布局。

## 未完成

- 本批所有编译、接口、兼容性、凭证、浏览器和 UI 验证。
- 多值、排除、模糊或厂商别名过滤。
- 外部平台身份、授权、回读和数据真实性验证。
