# 开放商业消费者多来源偏好合并 V1 验收

## 当前状态

`verified_local_http_pc_build`

## 已形成代码

- 选择 2 至 10 个本人隔离数据包并重新验证包完整性与归属。
- 逐字段展示当前值、全部来源候选值、来源信任状态和冲突数量。
- 用户逐字段明确选择来源，未选字段保持当前值，服务端不自动裁决。
- 通过当前修订号阻止预演后的并发覆盖。
- 在单一事务中保存偏好结果、采用前快照、全部参与包和逐字段摘要来源。
- 合并后无其他修改时可回滚；检测到后续修订时拒绝覆盖。
- PC 工作台提供多包选择、冲突预演、来源选择、采用历史和回滚入口。

## 已验证

- 服务层覆盖 2 至 10 个来源、重复或越权来源、已删除或无偏好来源、五个允许字段、冲突预演、逐字段来源选择、采用前后快照、摘要来源和审计脱敏。
- 未参与来源、重复字段、未知字段、未变化值、过期修订和合并后人工修改均失败关闭；无原档案时回滚会删除本次创建的档案，有原档案时恢复原值。
- 进程内 Axum 路由覆盖未登录、非项目成员、同项目其他消费者、跨项目、采用、隔离列表和回滚。
- 从真实 V141 磁盘数据库升级到当前版本后，既有偏好与单包采用记录保持可读，V161 表和索引只存在一份，迁移重复执行和数据库重新打开均成功。
- 既有单包选择性采用兼容回归继续通过。
- PC 静态契约确认 2 至 10 包限制、预演失效、逐字段来源、人工确认、修订绑定、回滚和关系/订单/ERP 隔离提示；完整开放商业 PC 契约与 TypeScript/Vite 生产构建通过。

## 验证证据

- Rust 定向服务、HTTP 与 SQLite 迁移回归：8 项通过、0 项失败，验证指纹为 `f0acee622caab752062337632807e93b6cf355d6defb9b6f71c836b92598ba79`。
- PC 多来源静态契约：`node scripts/test-open-commerce-portability-merge-pc.js`，并已纳入 `npm --prefix pc-frontend run test:open-commerce`，完整契约通过。
- PC 正式构建：`npm --prefix pc-frontend run build`，严格 TypeScript 检查与 Vite 生产构建通过。

## 未完成

- 三方历史合并、跨设备审批、关系恢复及订单和 ERP 数据迁移。
- 真实浏览器人工交互、窄屏视觉验收、绑定真实 TCP 端口、公网部署和生产数据库升级。
- 跨运营方公共传输、生产身份互认以及真实外部平台数据接入。
