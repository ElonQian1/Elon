# 消费者 AI 完整发现 MCP V1 验收

状态：`implementation_uncompiled`

## 已形成代码

- 工具清单公开完整消费者发现输入 Schema。
- 调用复用服务端消费者发现、输入规范化、排序、筛选和候选范围逻辑。
- 默认 MCP 身份降级为公开发现身份；显式 App 身份继续校验所有权。
- MCP 初始化说明明确发现不会自动调用或下单。

## 最终统一回归

- MCP `tools/list` 的 Schema、只读和开放世界标注。
- 默认身份发现公开及受限能力时的授权状态。
- 合法自有 App、非本人 App 和非法 App ID。
- MCP 与 HTTP/PC 在相同输入下的结果、候选范围和排序凭证一致性。
- 所有来源、价格、能力和硬约束组合的失败关闭行为。

本批按快速实现策略只完成 Rust 格式化、静态差异和文件规模检查；未执行 Cargo 编译、MCP 调用、数据库迁移、HTTP 或端到端验证。
