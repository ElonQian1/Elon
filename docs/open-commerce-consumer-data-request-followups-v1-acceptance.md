# 开放商业消费者删除请求催办与升级关注 V1 验收

## 当前状态

`implementation_uncompiled`

## 已形成代码

- V163 追加式催办与升级关注表、请求级查询索引和单次升级唯一约束。
- 24 小时首次等待、24 小时冷却、最多 3 次催办和 7 天内部运营目标。
- 请求与消费者项目、用户的所有权复核，终态请求阻断新操作。
- 相同幂等键安全重放；即使请求随后终结，原操作的重放仍可返回当前请求。
- 消费者 PC 催办与升级入口，以及商户 PC 的目标、次数、逾期和优先关注提示。
- 原删除请求数据模型只增加跳过零值序列化的运营字段，V5 原始导出查询不主动补充这些字段。

## 统一回归必须验证

- V163 全新数据库、V162 升级数据库、重复迁移和外键级联删除。
- 创建后不足 24 小时、刚好 24 小时、跨时区时间、错误时间和未来时间的收口。
- 三次催办、每次 24 小时冷却、第四次拒绝和并发双击只新增一次记录。
- 相同幂等键同参数重放、不同动作重放、不同说明重放，以及终态后的原操作重放。
- 不足 7 天、刚好 7 天、从未催办、已升级、终态请求和并发升级。
- 跨用户、跨消费者项目、跨商户项目和错误请求 ID 的隔离。
- 商户列表中未终结升级请求、逾期请求、普通请求和终态请求的稳定顺序。
- 审计不包含消费者账号、项目 ID、原始数据或外部凭据，并固定保留非法律、非仲裁标志。
- V5 导出摘要和历史包兼容，不因列表运营字段产生非预期字节变化。
- Rust、V163、HTTP、TypeScript、PC 操作、窄屏布局和错误提示。

## 未完成

- 本批所有编译、迁移、并发、时间、接口、行为和 UI 验证。
- 自动通知、平台工单、客服处置、争议仲裁、处罚赔付和法律期限策略。
- 跟进记录导出、跨运营方迁移、外部平台删除适配器和回执核验。
