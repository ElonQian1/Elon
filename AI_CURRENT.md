---
version_status: current
reviewed_at: 2026-08-05
---

# 一龙项目当前事实

本文件是 Codex、Copilot、Claude、Gemini 和平台内 AI 的短事实快照。开始实现或判断“项目现在有什么”时先读本文件；详细架构、接口和历史原因再按链接读取。讨论、草稿和 Git 历史不能覆盖这里及正式决策。

## 当前产品主链

- 已实现：用户通过 Android APK 或 PC 工作台描述需求，AI CLI/API 代理在真实 Git 工作区开发、验证、构建和发布应用。
- 已实现：项目、频道、多人/多 AI 协作、Windows 节点执行、上下文编译、项目文档治理与版本恢复等基础能力。
- 已形成第四阶段代码、未编译：Windows 节点为普通 Codex 编码任务按需注入仅含 `project_context_plan` 的项目文档 `context` profile；明确“只改一个源码文件/行号”且不涉及架构、跨模块或当前状态的任务在启动前跳过该 MCP。它最多规划 8 份文档和 2400 token 后续阅读，并把结构化响应限制在约 800–2000 token；相同请求使用 5 分钟、64 项 LRU，clean worktree 绑定 HEAD，dirty worktree 只在最多 256 个普通变更文件、合计 64 MiB 内完成安全流式 SHA-256 指纹后缓存。短期 MCP 会话目录新增不含正文/任务文本的 plan ID、路径和内容哈希账本：即使代理忘记传 `previous_plan_id`，同会话相同计划也自动返回 `not_modified`，revision 变化时省略未变信源并报告新增、移除和内容哈希漂移；语义正文仍明确未比较。`performance_receipt` 继续报告规划/缓存/估算节省，并增加最终结构化内容与 MCP tool result 的实际 UTF-8 字节回执。完整治理工具仍只用于显式文档任务，其他 CLI 普通任务暂不自动注入。本批次按快速实现策略未编译、未测试。
- 已实现并完成隔离候选节点首轮实测：`yilong-ui-live` MCP schema v1.11 可在用户不打开微调画布时发现 Web/PWA/Tauri/Android、恢复项目级 `designSessionId`，并把自然语言持久化为 revisioned `DesignIntentPlan`。Rust `--locked` check、目标契约测试、PC 构建/ESLint/画布测试和 bridge 测试通过；候选 Windows 节点实际回读 v1.11 与 24 个 capability ID。真实 Web fixture 在无可见浏览器下完成页面捕获、语义节点查询、点击/断言、回归基线、草稿预览/恢复和六步计划回执；Tauri 已完成 `TAURI_FRONTEND_WEBVIEW_ONLY` 前端捕获且没有冒充原生宿主。正式节点发布回读、Tauri 原生证据、PWA 独立目标、Android、比较器、模拟器、真机与人工视觉仍未验收；权威边界见 `docs/headless-ui-design-mcp.md`。
- 已形成第五阶段代码、未编译：完整治理 profile 新增原生工具理解候选的提交/列表工具；候选只在工作区外 SQLite 保存短摘要、topic、相对路径、定位符和当前 SHA-256，不复制源码正文、命令输出、聊天、prompt 或 Codex 私有 memories。审核项继续复用现有 suggestions/revision/authorization/apply/Git 事务进入 `.elon/document-sections.json.context_memories`，作为可跨 PC 的低权重导航记忆；轻量 `project_context_plan` 每次最多返回 3 条 query 相关且全部证据 hash 仍匹配的摘要，漂移即失效，当前源码、测试与约束性文档始终优先。本批次遵照快速实现要求只做格式化、静态门禁和差异审查，不执行 Cargo 编译、单元测试或真实 Codex/MCP 集成测试。
- 已形成第六阶段代码、未编译：PC 项目文档工作台新增本机原生理解候选收件箱，可按 pending/reviewed/rejected/applied 分页查看证据有效性并批量接受、拒绝或恢复。接受会再次核对 evidence SHA-256、catalog revision 和 suggestions revision，只把短结论合并到现有 `proposed_context_memories`；拒绝/恢复只改工作区外 SQLite，不写项目仓库。正在生成的建议不会被覆盖，并发状态变化失败关闭；前端同时保留 manifest/suggestions 的记忆字段，避免编辑其他文档设置时丢失跨 PC 项目记忆。共享仍只经原有 apply/Git manifest 生效，候选、聊天、prompt、命令输出和 Codex 私有 memories 都不是第二真源。本批次按快速实现要求不运行 Cargo/npm 编译、单元测试或真实 Codex/MCP/浏览器集成测试。
- 已形成第七阶段代码、未编译：完整 governance profile 新增显式任务后原生理解批量回执，一次原子保存 1–8 条短候选；代理可只提交工作区相对证据路径，本机服务端会检查路径越界、普通文件和 8 MiB 上限后绑定当前 SHA-256。PC 收件箱可在保留证据和 producer 的前提下修订 pending/rejected 候选的 summary/topics，并以 `updated_at_ms` CAS 防止旧页面覆盖新修订；漂移候选可被选中拒绝清理，但不能接受。普通 context profile 仍保持单一只读工具，回执不自动触发、不保存源码正文/任务文本/聊天/prompt/命令输出/Codex 私有 memories，也不绕过 suggestions/revision/authorization/apply/Git 真源链路。本批次遵照快速实现要求，只做代码、格式化、静态门禁和差异审查，不运行 Cargo/npm 编译、单元测试或真实 Codex/MCP/浏览器集成测试。
- 已形成第八阶段代码、未编译：普通 Codex 宽任务拆为单工具只读 `context` 与单工具只写候选 `receipt` profile，完整治理 schema 不进入普通任务；官方 `PostToolUse`/`Stop`/`SessionEnd` Hook 只在临时目录保存相对路径、读写类型和不可逆指纹，按新路径、读证据、最多六路径/360 字符及单会话三次做预算门控，不保存源码、响应、命令、prompt、聊天、transcript 或 Codex 私有 memories。非托管 Hook 首次/变化后仍须 `/hooks` 信任，节点不绕过 trust。证据升级为工作区 SHA-256 + 可用时 Git 对象双身份，漂移只给重定位候选；PC 增加共享记忆 health/Memory CI。回执增加本机会话 provenance、人工修订链、去重、replacement、潜在冲突和固定生命周期，相同已拒绝/审核/共享候选不被重开。效果仅为本地结构/字节估算，固定 `not_vendor_billing=true`、`not_total_task_tokens=true`。详细边界见 `docs/project-document-governance-mcp.md`；本批次未运行 Cargo/npm 编译、单元测试或真实 Hook/MCP/Codex/跨 PC/浏览器验证。
- 已形成第九阶段代码、未编译：Git 共享导航记忆新增兼容默认值的 owner、repository/path scope、reviewed_on/reviewer/复核周期和可选 expiry；旧 manifest 不迁移、不阻断，只在 health 标为治理缺口。governance MCP 和 loopback API 共用分页 Memory CI v2，支持 advisory、fail_on_drift、strict，覆盖证据漂移、重定位、到期、复核逾期、生命周期缺口和共享事实潜在冲突，并返回不自动执行的结构化 repair plan 与建议退出码。PC 显示这些计数/修复入口、Hook 信任状态、app-server 真实收益观测仍未接入，以及官方 Codex Memories 不读/不写/不复制的边界。Hook 为 SessionStart/PreCompact/PostCompact/SubagentStart/SubagentStop 只预留未启用的有界 no-op 缝；普通代理、直接 Codex、插件 bundle 的接入能力以静态 manifest 声明，不伪装已安装或已执行。真实 token/时间收益只定义官方 hook 与 thread/tokenUsage 事件的聚合合同，未观测匹配 baseline 前不宣称节省。本批次按快速实现要求仍不运行 Cargo/npm 编译、单元测试、浏览器或真实 Codex/MCP/Hook/跨 PC 集成。
- 已形成第十阶段代码、未编译：共享导航记忆上限增至 256，并新增 federation scope IDs、branch、release 与 clean/dirty worktree 限定；`project_context_plan` 接受 task paths、独立联邦 scope 和 release，服务端绑定当前 branch/worktree，范围进入缓存键与回执，先失败关闭排除不适用记忆，再验证最多 24 条并只注入 3 条。`scripts/project-memory-ci.ps1` 可实际采用 health 建议退出码；PC 对唯一 Git 对象重定位提供显式 pending 修复候选，拒绝原因和最近 200 条 producer 审核统计形成只读质量反馈。仓库已生成并静态校验 `plugins/yilong-project-memory`，以两个最小权限 MCP profile 和受 Hook trust 约束的路径账本接入直接 Codex，但尚未安装或启动。app-server 白名单事件接入器与工作区外聚合窗口代码已落位，只保存不可逆 session 指纹、计数和 token 累计值，不落原始事件；未形成匹配 baseline 前仍不得宣称真实 token/时间收益。本批按快速实现要求只做 rustfmt、JS/JSON/plugin 静态语法与差异门禁，不运行 Cargo/npm 编译、单元测试、节点、浏览器或真实 Codex/MCP/Hook/app-server/跨 PC 集成。
- 已完成项目记忆首轮隔离运行验收：`elon-pc-node` 真实构建、项目文档 Rust 回归和 context/receipt profile 固定隔离测试通过；唯一跨产品功能图/技术架构图的重复关系已删除，版本化检索 9/9 通过。独立 `127.0.0.1:7817` 节点无需打开页面即可 bootstrap 两个单工具 profile，stdio 代理 initialize/tools/list 分别约 171ms/166ms，越权切换返回 401；首次 context 计划约 3.6s/4652B，同会话重复调用约 1.2s/2839B，返回 `not_modified`、相同 plan ID 和零源码正文。Memory CI 空库返回 pass/0；Hook 执行程序的路径账本及 SessionEnd 清理已独立运行通过。app-server 观察器仅用合成白名单事件验证配对与不保存原始 payload，真实 Codex 插件安装、`/hooks` 信任、官方事件流任务 A/B、跨 PC 和浏览器仍未验收，不能据合成窗口宣称真实 token 或时间节省。详见 `docs/project-memory-agent-integration.md`。
- 已实现：开放商业网络 V1 的商户节点、能力、授权、调用、计量和审计主干；HTTP 与 MCP 共用领域服务。能力输入和处理器输出已按有限 Schema 配置失败关闭，无效输入不创建调用，无效输出以零金额失败并释放 Grant 预算，历史成功结果重放前按当前输出契约复验。
- 已实现、生产配置依赖环境：受控 `merchant_runtime` 可把平台调用安全转交给商户自有 ERP 后端；首个 `cofficethinking` 参考节点已覆盖商品发现、服务端报价、用户确认后的幂等下单和订单查询。连接器 SDK 已落位通用 Node 运行时内核，统一签名、重放保护、Grant、订单确认、幂等生命周期、Manifest 和响应信封；商户仍需实现自己的业务处理器与生产持久化存储。运行时每次调用重新核对标准 443 HTTPS 白名单，并形成 DNS 私网拒绝、禁代理、禁重定向和单次公网地址固定代码，但该新增安全批次尚未编译或网络验证。真实公网启用仍需 HTTPS 域名、白名单和双方密钥，通用内核完整回归按当前策略后置。
- 已实现、范围受限：项目成员可按商户读取终态能力调用证据、结果摘要、可选标准业务回执和当前 ERP 实例关联；新商户运行响应中的非法标准回执失败关闭。项目编辑者可保存 `project_editor_asserted` 人工衔接回执；每个数据接入也可签发权限固定为 `business_handoff.write`、1–366 天限时的一次性机器 Token，服务端仅保存 SHA-256，轮换、撤销、到期和接入停用立即失败关闭，机器回执以 `adapter_token_authenticated` 固化凭据 ID 和版本。升级前已有凭据补 90 天期限。`applied` 必须绑定有效标准业务回执和外部目标记录摘要。系统从业务证据和最新回执实时派生待处理与需重试队列，成功或忽略自动移出，失败保留重试。机器鉴权不是外部平台签名或回读；队列不自动写入 ERP/CRM，不创建第二套订单账本，也不证明真实支付、履约或退款。
- 已实现：通用 ERP 蓝图 V1.3 可按修订号追加蓝图定义、发布不可变版本、新建独立商户项目或把用户有编辑权的现有项目纳入实例治理、按固定版本检索能力、聚合经授权的脱敏通用提案、原子创建提案或实例初始化 Matter，以及保存升级前后配置快照、商户执行证据和完整回滚；现有项目纳入不会复制、覆盖或清理原项目，初始化 Matter 会先要求盘点已有能力。工作台和 AI 可从 Matter、Assignment 与 Artifact 真源读取实时状态、失败任务和证据缺口。AI 不得自动接受提案、演进蓝图、启动或合并任务、发布公共内核或采用升级。权威入口为 `docs/erp/README.md`。
- 已实现：商户数据来源接入控制面、幂等同步回执及不含密钥和原始经营数据的 AI 开发上下文；具体大型平台生产适配器仍未完成。
- 已实现基础闭环、后续批次未编译：开放商业已具备连接器 SDK、商户主动发布目录、消费者发现、限时授权、匿名关系、低敏偏好披露、客户端加密敏感数据保险箱、删除请求、商户未核验外部证明、动作确认、调用配额、终态凭证和手动封禁。消费者保险箱由客户端用 PBKDF2-SHA256 与 AES-256-GCM 加密，服务端只保存不透明密文、非敏感标签和最小审计元数据，不持有口令且不能解密；当前无密码找回、多设备密钥同步、商户或 AI 授权解密。消费者当前新导出为 V5，可携带本人关系、续期链、删除请求、未核验删除证明、低敏偏好、历史披露、账户级调用凭证和商户身份声明；V1-V4 继续作为兼容输入，导入默认只保存当前用户与项目独享的隔离快照。用户可显式采用单包低敏偏好，或从 2 至 10 个隔离包逐字段选择来源完成多来源合并；服务端保存摘要来源、修订和回滚快照，但不会自动裁决冲突、恢复关系、生成 Grant、合并证明、写 ERP、创建订单或结算。删除证明固定为商户提供且平台未核验，不等于美团、ERP、CRM 或会员系统真实完成删除。当前仍缺生产审核、真实扣款、完整订单迁移、全网身份互认、外部通知、真实删除适配器与回执核验和联邦治理；V4/V5、保险箱、多来源偏好合并与删除证明等新增批次尚未编译或测试，统一回归待最终执行。权威入口为 `docs/open-commerce/README.md`。
- 已形成代码、未编译：消费者删除请求增加固定 7 天内部运营目标、24 小时后首次催办、24 小时冷却、最多三次催办和超时后一次升级关注；操作使用追加式 V163 记录与幂等键，商户收件箱优先显示未终结的升级和逾期请求。该目标不是法定时限，升级不会启动平台仲裁、处罚、赔付、外部通知或真实删除。跟进记录当前不进入 V5 可携带包。权威边界见 `docs/decisions/open-commerce-consumer-data-request-followups-v1.md`。
- 已实现、范围受限：PC 消费者沙盒可按商户发布的有限输入契约生成非技术调用表单，未声明默认值的可选字段默认省略，无法安全呈现的契约失败关闭。`action` 还必须经过服务端 5 分钟两阶段确认；确认绑定当前用户、App、商户、能力、Grant、幂等键和输入摘要，并与 Invocation 创建原子消费，同一幂等请求可重放但不能产生第二次动作。开发者测试 App 可按绑定游标轮询自己的终态调用摘要并读取单条结果，数据库在终态转换时原子追加一次序号；已验证部分仍是可恢复轮询，不是跨运营方事件总线。该确认不包含 WebAuthn 或跨设备可信展示，不替代商户运行时内部订单、支付、风控或履约确认。
- 已形成代码、未编译：消费者发现可在偏好匹配、最低调用价、公开能力优先、最近更新和商户名称五种内置透明排序器之间显式切换；响应公开策略说明、用户选择状态和 `paid_placement=false`，未知策略失败关闭。排序只作用于现有公开目录最多 100 个商户候选，最多返回 50 条，不包含第三方排序器 SDK、实时距离、外部评价、真实库存证明、反作弊或联邦排序治理，也不代表全网客观最优。权威边界见 `docs/decisions/open-commerce-pluggable-ranking-v1.md`。
- 已形成代码、未编译：消费者可显式要求单次发现排序凭证；凭证只携带输入 SHA-256 指纹、候选范围、内置策略版本和有序公开结果，PC 以 Web Crypto 复核后下载。凭证不持久化、不含搜索词和偏好明文，固定 `signed_by_operator=false`，只证明所下载字节与摘要一致，不证明运营方身份、商户数据真实、目录完整或算法公平。权威边界见 `docs/decisions/open-commerce-consumer-ranking-receipts-v1.md`。
- 已形成代码、未编译：每项公开能力可显示商户项目声明的来源和 `current/stale/unknown` 新鲜度。项目编辑者还可把当前能力版本显式绑定到同项目、同商户、同接入的成功或部分成功业务同步回执及已登记数据域；目录公开厂商、接入方式、数据域、回执状态、完成时间和 SHA-256，并以回执完成时间计算声明期。能力更新或接入停用后旧绑定不再公开，无有效绑定时退回能力声明时间。所有来源固定 `externally_verified=false`，内部回执不证明美团等外部平台签发、数据真实、库存实时或订单完成。权威边界见 `docs/decisions/open-commerce-public-data-provenance-v1.md`、`docs/decisions/open-commerce-capability-source-link-v1.md`。
- 已形成代码、未编译：消费者可显式要求只返回仍处于商户声明有效期内的能力；默认关闭，不改变旧发现结果。开启后 `stale/unknown` 能力在候选阶段排除，响应、原因、输入指纹和排序凭证均记录该选择；筛选不构成外部平台核验，也不自动下架或降权。权威边界见 `docs/decisions/open-commerce-consumer-freshness-filter-v1.md`。
- 已形成代码、未编译：消费者可另行显式要求只返回当前仍有关联内部业务同步回执的能力；默认关闭，与声明期筛选可独立组合。响应、匹配原因、输入指纹和排序凭证记录该选择；失效绑定会自动回退普通商户声明并被筛除。该筛选仍固定未外部核验，不证明美团等平台授权、数据真实或交易完成。权威边界见 `docs/decisions/open-commerce-consumer-source-requirement-v1.md`。
- 已形成代码、未编译：消费者还可按单个来源厂商标识和数据域精确过滤能力；空白按未设置处理，非法标识失败关闭，规范化条件在响应、匹配原因、输入指纹和排序凭证中回显。填写任一条件会派生内部回执来源要求，但厂商和数据域仍是商户项目声明，不证明官方授权或真实回读。权威边界见 `docs/decisions/open-commerce-consumer-source-filters-v1.md`。
- 已形成代码、未编译：消费者可独立要求内部同步回执在 1 秒至 365 天的指定时间内完成；无效、缺失或未来完成时间失败关闭，阈值进入响应、匹配原因、输入指纹和排序凭证。该条件只约束商户项目内部回执年龄，不证明外部平台数据真实、完整或仍然实时。权威边界见 `docs/decisions/open-commerce-consumer-source-age-v1.md`。
- 已形成代码、未编译：消费者发现响应可从当前运营方最多 100 个目录候选中汇总内部回执厂商和数据域建议，PC 支持选择建议或继续手输。响应固定非穷尽，并按公开能力而非商户计数；建议不保证最终命中，也不构成外部厂商身份或授权证明。权威边界见 `docs/decisions/open-commerce-consumer-source-filter-options-v1.md`。
- 已形成代码、未编译：消费者发现支持三位字母价格币种，存在价格上限但省略币种时按既有界面语义默认 CNY；服务端先精确匹配币种，再比较微单位价格，禁止跨币种数值比较。条件进入响应、匹配原因、输入指纹和排序凭证，但不提供汇率换算或支付结算证明。权威边界见 `docs/decisions/open-commerce-consumer-price-currency-v1.md`。
- 已形成代码、未编译：消费者 AI 可精确要求信息查询或经营操作、公开调用或需授权调用；未知值和公开发现中的 `owner_only` 失败关闭，条件进入响应、匹配原因、输入指纹和排序凭证。筛选不会绕过授权审批、Grant、动作确认、配额或结算。权威边界见 `docs/decisions/open-commerce-consumer-capability-filters-v1.md`。
- 已形成代码、未编译：消费者可把城市、经营类别和全部标签从软偏好显式提升为硬约束；启用但缺少对应偏好值时失败关闭，商户资料缺失或不匹配时排除。约束进入响应、匹配原因、输入指纹和排序凭证，但只基于商户项目公开资料，不证明地址或标签真实。权威边界见 `docs/decisions/open-commerce-consumer-preference-constraints-v1.md`。
- 已形成代码、未编译：普通消费者发现响应直接公开候选上限、当前目录候选数、合格数、返回数和是否截断，PC 明示“当前运营方目录、非全网穷尽”。空结果不代表全网不存在其他商户；计数也不证明目录完整、数据真实或排序公平。权威边界见 `docs/decisions/open-commerce-consumer-candidate-scope-v1.md`。
- 已形成代码、未编译：消费者发现基础输入由服务端统一规范化；搜索词最多 200 个字符且拒绝控制字符，能力键和 App ID 复用现有标识符规则，返回数量先限制为 1 至 50，目录查询把 SQL LIKE 特殊字符按普通文字处理。该规则不依赖 PC 控件，不改变固定 100 个候选窗口、授权或排序语义。权威边界见 `docs/decisions/open-commerce-consumer-discovery-inputs-v1.md`。
- 已形成代码、未编译：消费者 AI 可通过只读 MCP 工具 `open_commerce_discover_for_consumer` 复用 PC 同源的完整发现服务，表达透明排序、来源、币种、能力和偏好硬约束并读取候选范围、授权状态和可选排序凭证。默认 MCP 身份只能获得 `pc-web` 公开发现语义，显式 App ID 仍校验当前用户所有权；工具不自动申请授权、调用能力、下单或结算。权威边界见 `docs/decisions/open-commerce-consumer-discovery-mcp-v1.md`。
- 已形成代码、未编译：消费者 AI 选择能力后可用只读 MCP 执行计划复核拟调用输入、App 身份、封禁、授权和下一次调用的 Grant 预算。服务端遍历该 App 对该能力的全部未撤销、未过期 Grant，优先选择可覆盖下一次调用次数和单位价格且币种一致的一张；只有全部不可用时才返回 `grant_refresh_required` 和具体预算原因，已有续额申请则返回等待审批。动作计划明确拆成准备、用户明确同意、确认和调用；规划固定无副作用，不占 Grant 次数或预算，不创建确认、调用、订单或结算。权威边界见 `docs/decisions/open-commerce-consumer-capability-execution-plan-v1.md`。
- 已形成代码、未编译：消费者 AI 在用户明确同意后，可通过 MCP 以本人已注册 App 身份向商户提交单个 `authorized` 能力的最小授权申请。固定确认短语、App 所有权、当前可覆盖下一次调用的 Grant 和待审批幂等均由服务端复核；如果现有 Grant 的次数、金额或币种不能覆盖下一次调用，允许提交新的期限或预算申请，但旧 Grant 不自动扩容、撤销或改写。成功只表示 `pending`，商户仍决定批准、期限、总次数和总预算，AI 不能自批或扩权。权威边界见 `docs/decisions/open-commerce-consumer-authorization-mcp-v1.md`。
- 已形成代码、未编译：消费者 AI 可只读列出当前项目内本人拥有的开发者 App，并查看当前 MCP 身份和沙箱可用状态，以选择 `x-elon-app-id`；响应不含测试 Token、Token 提示、生产凭据或其他用户标识。该工具不创建、轮换或启停 App。本批还把 MCP 初始化协议说明抽成独立模块，使主路由回到文件规模门禁以内。权威边界见 `docs/decisions/open-commerce-consumer-app-directory-mcp-v1.md`。
- 已形成代码、未编译：消费者 AI 可按 `pending/approved/rejected/canceled` 只读列出当前用户通过当前项目本人 App 发出的授权申请，并读取批准后的 Grant 条件；还可单独列出当前仍有效的本人 Grant，查看能力范围、用途、期限以及调用次数和计量预算的已用、剩余值。两类查询同时绑定用户与当前项目本人 App，响应省略商户项目、申请人、授权人和审核人内部 ID；预算已用尽的 Grant 仍可见但明确标记不可继续消费。项目编辑者经用户明确确认可撤回本人 pending 请求并形成双侧审计，非 pending 保持原状态且不会撤销已批准 Grant。历史 `approved` 或列表中的有效 Grant 均须由执行计划按拟调用输入再次复核，列表本身不占预算、不调用能力。权威边界见 `docs/decisions/open-commerce-consumer-authorization-status-mcp-v1.md`。
- 已形成代码、未编译：消费者 AI 可通过 `open_commerce_get_my_action_confirmation` 重新读取当前用户与当前 MCP App 持有的单份动作确认，核对商户、能力、授权、幂等键、输入字段形状、有效状态、失效时间和下一步。读取不返回原始输入值，不修改确认状态，其他用户或 App 统一不可见；过期状态在读取时即时派生。动作准备、确认和调用工具已从主 MCP 路由抽成独立模块，原有两阶段确认、一次消费和幂等重放语义不变。本新增批次未编译或测试，权威边界见 `docs/decisions/open-commerce-server-action-confirmation-v1.md`。
- 已形成代码、未编译：当前用户明确停止时，可通过 `open_commerce_cancel_my_action_confirmation` 幂等取消当前 MCP App 下尚未创建 Invocation 的 `pending` 或 `confirmed` 动作。服务端保存 `canceled_at`，兼容复用既有 `expired` 终止状态，并在安全投影中显示 `canceled`；其他用户/App、自然过期或已消费确认不能伪装为取消。取消不撤销已创建调用，不退款、不回滚商户订单或外部动作。本新增迁移、并发和 MCP 行为未运行验证，权威边界见 `docs/decisions/open-commerce-server-action-confirmation-v1.md`。
- 已形成代码、未编译：开发者 App 可配置标准 443 端口的 HTTPS Webhook，并在创建时固定沙箱或生产环境以及成功、失败事件范围；既有订阅迁移为沙箱。新订阅先停用，接收端完成签名 challenge 精确回显后才从当前终态序号开始。终态事件按环境原子进入耐久队列，工作器使用短租约、指数退避、死信和自动停用；外呼拒绝私网或特殊 DNS 地址、禁用代理和重定向，并固定本次公网连接。订阅密钥按订阅和版本派生且只在创建或显式轮换时显示一次；单条死信可重试，历史通知最多幂等补发 100 条。生产订阅默认关闭，必须同时开启生产 Webhook 与生产凭据开关，并持续满足 App、资料、域名、准入及凭据条件；撤销、轮换、到期或资格失效会停用生产订阅并终止未完成投递。PC 可选择环境和事件、验证端点、轮换密钥、管理启停、查看投递、重试与补发，并按沙箱/生产查看只读订阅、积压、重试、待处理与已确认死信和生产阻断健康摘要；项目编辑者可用不可覆盖的操作人、时间和原因确认不再重试的单条死信，原失败记录继续保留，后续人工重试会重新打开处理链路。本批次尚未编译、迁移、DNS、TLS、并发、跨语言签名、网络或 UI 验证；它不是工商身份、跨运营方总线、支付通知或 ERP 写入证明。
- 已形成代码、未编译：开发者 App 可维护 HTTPS 主页、隐私政策、服务条款、支持邮箱和拟申请能力，以修订号保存、提交并由平台管理员批准或要求修改；编辑会清除旧结论并退回草稿，所有变化进入审计。提交前必须把一次性 challenge 发布到主页同源固定 `well-known` 地址并完成当前修订的域名控制证明；出站主机由运营方精确白名单控制，并在请求前拒绝私网或特殊 DNS 地址、固定本次公网连接。PC 已提供资料维护、域名验证、提交和仅系统管理员可见的待审工作台。资料或域名 `approved/verified` 不改变沙盒环境、不签发生产凭据，也不是组织身份、域名法律所有权、真实交易或外部平台授权证明；本批次尚未编译、迁移、DNS、TLS、网络、权限或 UI 验证。
- 已形成代码、未编译：资料审核和当前域名证明通过后，App 所有者或项目管理员可提交主体名称、注册地区、登记编号和明确声明，平台管理员可批准、退回或暂停独立准入记录并固定人工风险层级。资料变化会原子失效待审或已批准记录，App 停用会原子暂停记录，重新启用不会自动恢复。主体信息仍是申请人声明，未连接工商或第三方身份核验；`approved` 本身不签发生产凭据、不改变沙盒 App，也不开启真实资金或外部平台权限。本批次尚未编译、迁移、接口、权限或 UI 验证。
- 已形成代码、未编译、默认关闭：平台管理员可在当前资料、主页域名证明和准入记录均有效时，为开发者 App 显式签发范围不超过已审申请、受人工风险层级期限约束的 `oc_live_` 生产凭据。完整密钥只显示一次，数据库仅保存 SHA-256；再次签发会原子撤销旧凭据。生产调用、动作准备和动作确认复核能力范围；调用账本保存平台/沙箱/生产环境及非秘密凭据 ID，幂等重放、终态事件和 Webhook 按环境隔离，沙箱不得触达真实商户运行时。资料修改、域名重验、App 停用或准入暂停会原子撤销，并停用关联生产 Webhook。PC 可签发、查看元数据和紧急撤销，并通过只读生产就绪总览按固定顺序查看 App、资料、域名、准入、凭据和生产 Webhook 阻断，分别判断生产调用与通知是否就绪。`OPEN_COMMERCE_PRODUCTION_CREDENTIALS_ENABLED` 未显式开启时签发和生产鉴权失败关闭。它仍不是外部组织核验、真实平台授权、真实资金、链上资产或生产部署证明；本批次尚未编译、迁移、接口、权限、并发、网络或 UI 验证。
- 已形成代码、未编译：消费者可为隔离导入生成低敏偏好差异和关系重新授权候选，只能在明确确认及修订号未变化时采用偏好；消费者可逐项选择类别、标签、城市、价格上限和公开商户偏好，服务端只合并真实变化的所选字段，未选字段保持当前值。采用保存完整前后快照和实际选择字段，且仅在档案未发生后续修改时允许回滚。它不恢复关系、Grant、披露，不写入订单、ERP 或结算。该批次按快速开发策略尚未编译、构建或测试。
- 已形成代码、未编译：PC 与连接器 SDK 可用本地口令把普通或已签名消费者数据包包装为 PBKDF2-SHA256 + AES-256-GCM 离线归档，并在上传前于消费者设备解密。口令不进入服务端；当前没有找回或云托管，且该批次尚未进行构建、往返或跨端互操作验证。
- 已形成代码、未编译：消费者可把隔离包内来源关系人工映射到当前有效发布商户，PC 端可复用公开目录搜索并选择目标，再用本人独立开发者 App 发起一笔范围不超过旧关系的新授权申请。映射只记录 `consumer_confirmed`，目录搜索不构成身份认证，旧关系和旧 Grant 不复制，目标商户仍需重新决定。该批次尚未编译、构建或测试。
- 已形成代码、未编译：项目编辑者可在本地生成商户 RSA 密钥并发布经持有证明验证的公钥指纹；V4 首次携带关系引用商户的有效指纹声明，V5 继续继承该字段并增加删除证明。仅当来源运营方签名已验证时才生成当前已发布商户的精确指纹匹配候选；映射仍需消费者确认和商户重新授权。V1-V4 保留导入兼容路径，但 V5 与历史摘要兼容、SDK 和密码学往返尚未验证。
- 已实现、只预演：项目 AI 资源控制面可盘点自己的 Codex、本人节点、已授权共享 Codex 和平台模型并保存策略；它不启动任务、不验证外部额度，也未接管真实调度。
- 已实现、默认关闭，离线适配器批次未编译：AI 任务与开放商业调用的链外影子经济层，可生成幂等用量凭证、验收后双分录，以及未提交网络的 Sui 链下投影包；追加式争议可阻断投影，已接受争议可创建独立纠正 Matter，人工验收后原子追加冲销与替换双分录而不改写原凭证。两条腿可共同保存为原子纠正包；系统还能从任意标准、冲销或替换凭证只读解析根、纠正步骤和当前有效凭证，异常循环或分叉失败关闭。标准包和纠正包可导出统一离线交接文件；项目还能签发限时、限网络和限包类型的离线预检机器凭据，外部工具只有在服务端只读重建交接包且摘要一致时才能追加 `passed/rejected` 报告。项目编辑者可将当前可导出的投影显式加入预检队列，机器按权限领取 60 至 900 秒短时租约，交接摘要漂移会阻断任务，报告与任务完成原子写入。连接器 SDK 和 CLI 已写入本地复核、显式报告及 `claim/renew/release/complete` 租约命令，另有可中止、自动续租、异常释放和幂等完成的单任务工作器；适配器和租约凭据不接受命令行参数，这些路径尚未运行。报告和任务不改变投影状态，也不授权签名、广播、最终性或资金移动。当前没有 Sui 网络适配器，不移动真实资金。
- 已实现但边界有限：API Token 保管、远程节点、计算使用台账，以及默认关闭的节点模型算力共享策略。节点所有者可选择共享模型、最大并发和每日 Token 预算；指定与自动调度会在同一事务中检查今日实耗、活动租约预留和本次保守预留，所有者自用不受共享限制。可信终态先冻结实际 Token，再进入异步结算；提前断流、真正租约过期和服务重启会失败关闭并幂等释放预授权，最终状态不接受迟到覆盖。所有者控制面会从持久化记录派生近 24 小时失败、预留超出和过期租约健康快照。该快照不是 SLA 裁决或自动赔付。它目前仍不是包含异构任务、竞价、故障赔付、真实提现或链上结算的开放算力市场。
- 已形成基础代码、未编译且未完整接线：任务级分布式算力联邦已定义 Provider、Workload、版本化 Offer、标准 Compute SKU、期货/远期 Price Snapshot、Job、Reservation、带 fencing generation 的 Attempt Lease、分层执行用量与结算回执，以及旧 LLM V1 的部分事实投影。共享 CapacityPool 已形成 v165-v168 SQLite schema 与本地 Store，并以追加式多 meter 账本和稳定 Claim 管理供给、持有、释放与到期；Reservation Claim 强制绑定 Offer、Job 和 Reservation，公开 standalone 方法拒绝绕开 Broker 单独改变该类 Claim。
- `implementation_uncompiled`：本人 HTTP/MCP 可管理自有 Provider、CapacityPool 和当前 Bucket，审计 Pool epoch 与脱敏账本，并原子增加 self-declared 供给或将 available 撤入 retired。服务端生成版本、时间和摘要，不暴露路由、凭据、原始档案、消费者或业务因果字段。审计健康与 available 均不等于硬件 verified 或可交易；这些入口不激活或发布 Offer。边界见 `docs/distributed-compute/{provider,capacity-pool,capacity-bucket,capacity-supply}-api.md`。
- `implementation_uncompiled`：v177-v181 已形成激活证据申请、本人 HTTP/MCP、管理员审核/过期批准废止、双方只读预检、不可变计划、原子应用和紧急隔离回执。应用在一个事务内登记 active Provider 下一版本、追加 Pool 激活事件、终结申请/计划并保存回执；隔离按精确应用摘要和原因，把当前 active Provider 新版本与 Pool epoch 原子转为 quarantined，保留原 activated/applied 历史。两者均不发送节点命令、不读取凭据正文、不直接改写 Offer、不开放预留、不派发任务或移动资金；恢复尚未实现，管理员写入口不向 MCP 开放。边界见 `docs/distributed-compute/activation-evidence-api.md`。
- `implementation_uncompiled`：本人 HTTP/MCP 可创建、精确修订和撤销规范化 draft Offer；管理员 HTTP 可原子发布、draining 退场，并在无活动预留时终结。v182-v184 保存回执与依赖索引，均不移动资金。详见 `docs/distributed-compute/offer-api.md`。
- `implementation_uncompiled`：本人 HTTP/MCP 可从 active Offer 发布规范化 fallback_curve Price Snapshot；项目级 HTTP/MCP 可创建 submitted Job、发现并绑定当前候选，并读取本人/项目 Job 与 Reservation。报价不移动资金；真实价格源和自动撮合尚未接线。详见 `docs/distributed-compute/price-snapshot-api.md`。
- `implementation_uncompiled`：v175/v176 已形成平台人民币余额 Broker Reserve 与未激活终态；v185-v194 已覆盖 Attempt 激活、Lease、证据、Verification、Execution Receipt、可信终态与容量收口。v195 原子结清消费者预授权并登记 Provider/平台 pending 收益；v196-v199 形成 72 小时挑战、决议、accepted 纠正与净额释放；v200 把 Provider 本人 available 原子转入 withdrawn 保留区；v201 为每份提款申请增加唯一的取消、拒绝或外部已付款声明终态。取消/拒绝全额返还内部余额；外部已付款只登记管理员声明和证据摘要，不执行或验证付款。上述控制面尚未执行编译、迁移、并发或接口运行验证，也未实现真实派发、可信事件自动接线、非金额补救、available 事后追索、自动释放、真实付款适配器、外部证据自动核验、外部矿池、多币种或 Sui 提交。权威入口为 `docs/distributed-compute/README.md`。
- 已形成节点插件合同、真实签名准入内核与内部兼容代码，未编译、未接线：NodeRuntime 持有本机 Compute Plugin Host，现有 `LlmStreamRequest` 仍经内建 legacy runner 调用原本地推理实现，旧 WebSocket 协议、能力上报和 Chunk/End/Error 保持不变。节点侧已有外置签名 Manifest、关闭共享时零下载的 InstallPlan、active/candidate 双槽生命周期、短时 `ReadyCapability`、`Start/RenewLease/Cancel` Attempt 命令，以及 Runner 本机事件与 Host 盖章事件合同；本批补入 RFC 8785 JCS、SHA-256、Ed25519 验签、Manifest 语义校验、库存 revision+digest 绑定、Publisher/Control 双 keyring revision+digest、候选清理动作和逐段下载实时再授权合同。本机耐久真源使用独立 SQLite，并固定 root-signed 双 keyring、库存 CAS、候选所有权、三段式下载认领和可信时间边界；固定路径、WAL/FULL/foreign-key authority schema v2、inventory payload v2、singleton 初始化、keyring seal、防回滚/防分叉、活动 binding/可信时间 CAS、旧 tip 归档重验和逐 key 对账已形成代码。原子 PlanApply 也已形成：完整 signed request/typed admission 幂等封存，六类 action 投影，本机随机 candidate owner、完整下载身份、inventory/receipt/event/seal、epoch 推进、历史回放交叉校验和 sealed execution capsule 均在单一 `BEGIN IMMEDIATE` 内完成。逐段取数现已覆盖 fresh read、validated begin/redirect CAS、commit/abort、跨 authority/process 代次撤销、稳定 outcome 读取和 prepared recovery-abort；claim identity 在 Store mutation 前生成，非授权 recovery key 同时绑定原 Store session，无论成功还是 Store 结果不确定都不会丢失恢复身份。初始 Store 实际回滚时，只有 authority、download 和冲突 claim 仍精确等于 mutation 前快照才返回 `NotCreated`；redirect 缺失或任何漂移继续失败关闭。commit/abort 必须先对账 session provenance，Store mutation 失败不返还旧 mutation capability，commit 还保留原打开文件句柄。v1 从未真实接线或建库，本批明确由 v2 对旧/未知库 fail-close，不提供预生产 v1 原地迁移。以上新增仍未编译、测试、执行 DDL 或打开真实数据库。仓库仍无生产 root pin；可信时间观察值生成/attestation、数据库外防整库回滚锚点、受管文件安全根、no-follow/no-reparse 钉住打开、文件游标对账与短文件损坏转换、全工件摘要、candidate promotion/GC、跨重启恢复枚举、Sidecar IPC、Host 状态机和通用云协议仍未实现；`DurablyWrittenComputePluginSegment` 也仍无生产构造器，`ReadyCapability` 不等于商业 Offer，不能称为按需插件运行系统已实现。

## 已接受的产品方向

- 先用 AI 应用开发、ERP、营销内容、小游戏和经营分析帮助商户获得直接价值，再逐步形成开放商业网络。
- 商户数据由商户控制；消费者 AI、商户 AI 和第三方应用通过授权接口连接，不把所有参与者锁进同一个 App。
- 开放商业当前权威入口是 `docs/open-commerce/README.md`；实现状态先看 `docs/open-commerce/capability-baseline.md`。
- 商户 ERP 采用“公共内核 + 行业插件 + 商户主题 + 私有扩展”的分层模型；`cofficethinking` 与最小零售参考实例已证明同内核可保持不同主题和插件，商户私有扩展不随公共版本升级被覆盖。
- 一龙采用任务级分布式算力联邦：用户节点、受管集群和外部矿池统一为 Provider；客户端只在用户开启共享后按需下载插件与模型；公网先聚合独立任务，期货交付窗口的不可变价格快照作为首期结算基准。完整决定见 `docs/decisions/distributed-compute-federation-v1.md`。

## 尚未成为当前事实

- 接入器任务领取、短时租约、1 小时硬期限内续租、一次性租约密钥、原子完成、主动释放、拒绝退避、第 6 次拒绝暂停、编辑者恢复、Connector SDK 客户端、通用 ERP/CRM 适配器工作器和项目侧租约监控代码已经落位，并已通过 Rust 编译、PC 生产构建及 SDK 模块语法检查。按当前“先连续实现、最终统一回归”的策略，完整 Rust、迁移、HTTP、PC、SDK 和文档门禁仍后置；统一验证与发布复核完成前不能宣称生产可用。既有凭据不会自动获得读取权，只有项目编辑者明确轮换并加入 `business_handoff.claim` 后才能领取单条衔接任务。
- 消费者 AI 的跨项目基础目录与授权沙盒已实现；节点模型推理的显式供给授权已实现。异构算力联邦、通用节点插件合同、共享容量账本、本地原子 Reserve、未执行任务 Release/Expire 与 staging 无用量 Attempt 中止已写入，但插件下载与隔离执行、异构任务真实派发、已开始执行的 Attempt 终态和运行中 Broker 结算、外部矿池、验证计量、期货曲线/订单簿、自动收费与跨主体结算仍未实现。
- 通用 ERP V1.3 用初始化 Matter 和可机读物化合同衔接真实项目开发，并从既有任务与产物记录派生可恢复状态；它能登记现有平台项目，但不会自动导入本机目录、复制商户源码、启动 Assignment、执行数据库迁移或部署生产系统。`cofficethinking` 仍需先在一龙创建或绑定对应平台项目，再由有编辑权的用户显式纳入。Matter 仍需商户批准和启动，真实商户项目继续走现有项目开发和发布流程。
- Sui Move 合约、测试网/主网提交、NET、CREDIT、RevenuePosition、链上治理和收入权益均是草案；链外 Sui 信封不等于已上链、已发币或公司股权。
- 任何提案只有经过正式 ADR、实现引用和验收后，才能进入本文件的“已实现”部分。

## 明确禁用

- 不设置独立“预言家 AI / Demo Oracle / DemoPreview”前置角色。见 `docs/decisions/reject-demo-oracle-role.md`。
- 不恢复 AI-to-AI Skill 路线或 Skill 市场作为当前主架构。多 AI 协作继续使用项目频道、Matter/Assignment、运行路线和供应商工具协议。见 `docs/decisions/reject-ai-to-ai-skill-route.md`。
- Codex 桌面监督与 PC 自动续跑自 2026-07-26 起暂停；暂停解除前不得自动派发或恢复该流程。

## 读取顺序

1. 当前状态：本文件。
2. 项目定位：`AI_PROJECT.md`。
3. 系统分层：`AI_ARCHITECTURE.md`。
4. 实现入口：`AI_INDEX.md`。
5. 开放商业专题：`docs/open-commerce/README.md`。
6. 通用 ERP 蓝图：`docs/erp/README.md`。
