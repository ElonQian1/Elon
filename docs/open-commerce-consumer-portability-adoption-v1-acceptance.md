# 开放商业消费者可携带数据迁移预演与偏好采用 V1 验收

## 当前实现

- 导入包可生成当前偏好与来源偏好的逐字段差异。
- 来源关系只列为重新授权候选，不自动恢复关系、Grant 或披露。
- 用户明确确认后可采用低敏偏好，并通过当前修订号防止覆盖并发修改。
- 采用前后状态和修订保存在同一事务的采用记录中。
- 回滚要求当前修订仍等于采用结果修订，检测到后续修改即失败关闭。
- PC 工作台提供预演、来源信任提示、偏好采用、历史记录和显式回滚入口。
- 字段级选择由 `docs/open-commerce-consumer-portability-selective-adoption-v1-acceptance.md` 承接，仍待统一验证。

## 验证状态

根据当前快速开发策略，本批次未运行 Rust 编译、PC 构建、类型检查、迁移检查、行为测试或完整回归。当前状态为 `implementation_uncompiled`，不能声称可运行或通过生产验收。

## 后续统一验证

- 全新数据库和既有 V139/V140 数据库升级到 V141。
- 无当前档案、已有档案、修订冲突、重复采用和成功回滚。
- 采用后人工修改档案时拒绝回滚。
- 多用户、多项目、导入包归属和审计隔离；关系人工映射与重新授权由独立 V1 承接。
- PC 类型、API 路径和交互构建。
