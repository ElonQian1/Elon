# 币安网格持仓报告 v2 交付

量化拥有列表、持仓与参数编辑 UI；主 APK 只补托管报告。固定 positions 路由按已核实策略 shadow UID 与合约筛选，透传来源存在的杠杆、名义价值、浮盈亏、保证金、标记价和强平价。可选 funds 查询仍绑定同一 shadow UID，仅补 USDT 账户余额和维持保证金；缺失或失败保留有效持仓，不回退到父账户。

`report_read_v2` 返回 `yilong.binance_report.v2`；v1删除新增可选字段，保留旧严格客户端的精确集合。调用仍复用已有签名、授权、请求身份及观察前后账号核验，不新增金融写操作、主产品表单或定时查询。

23项报告适配器回归通过，覆盖精确小数、策略账户隔离、父账户拒绝、可选资金失败不清空持仓。原生网格34套197项回归全部通过；首次编译因240秒静默阈值中断，按大型编译调整有界超时后成功。正式发布见下方。真实接口可能未返回部分字段，这些字段保持未知。视觉和真实数据由用户在量化 APK 验收，本轮不操作手机页面。

量化端参考：`docs/requirements/grid-rich-management-v1.md` 与 `docs/delivery/grid-rich-management-20260916.md`（独立量化仓库）。

## 发布和安装

主APK1.1.1774/1774，源码 `e427016681391599e1d615f0b254700dedfb849c`，SHA256 `7c7725af96ce1ad12d58dca2e7e52ecd5132b967322776cba6e79443ab9d93a8`。官方发布成功，服务器来源/哈希、原签名核对通过。无线 `adb install -r` 成功，包元数据确认为1774；保留数据，未启动或检查页面。

量化0.7.77/94已同步发布、无线安装，源码 `14bbeaa0e3fb03fb8e61ea4329ffc99136f44116`，SHA256 `7cbb1e68514dd5af9357db418be9cf9ec8642dcb9435cbf93e62924be82da6d8`。其Debug656项（10跳过）及Release回归通过，最后9项定向测试通过；完整Rust验证因既有磁盘门禁未运行，GitHub验证因账号付款/消费额度未启动。真实数据及UI由用户提供截图验收。

发布脚本末尾旧worktree自动清理出现Branch属性告警，APK发布、哈希及安装均成功；本任务保留工作树，由统一收尾分别记录本机状态，不修改无关清理脚本。
