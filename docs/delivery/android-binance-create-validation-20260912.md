---
version_status: current
reviewed_at: 2026-09-12
---

# 创建准备动态规则检查交付

需求：[动态准备检查](../requirements/android-binance-create-validation-v1.md)。原创建只检查合约基础过滤器、身份和windowCount，未把动态格数/最低保证金用于准备；新`BinanceCreateRuleCheck`复用上一批参考服务，在原准备前完成精确校验。主服务不持有新的产品UI。

检查绑定账号、请求、合约、当前文档和观察时间；数量超界、区间过窄、保证金不足或动态结果缺失时不给准备资格。30秒有界轮询不重启读取，取消和替换请求使迟到回执失效。原会话在结果返回时再核对账号/文档，准备还不能超过参考有效期。原用户确认、持久化幂等及未知结果查询保持；没有修改交易适配器请求体。

主Debug编译及77项创建/宿主测试通过，其中10项新增覆盖精确最低额相等/不足、动态数量边界、异常结果、过期、取消、替换、超时及准备参考有效期。计算公式/JS适配器未修改，复用上批336个官网源码向量、52项JS回归证据；不能称本批已在本人账号运行。

量化0.7.28(45)配套修复idle阶段隐藏服务错误信息的问题，330项Debug/Release、官方Android合同和完整仓库验证通过；其发布回执`rel_d9a41b4891754e5abd8be12b88940aa5`已验证。双方保持V2/V1消息格式，需要一起更新以获得可见的失败提示。

主APK1.1.1676(1676)已发布，源码`a8af89b032aa2789be32d30e191ecccc532e48e5`，APK SHA256为`be806e3881bfb6443d75e1f0f51e400b8729a8fcd1014ea421f7e7b5c7381313`，签名证书`f79567cf8a7e610e218aa4b7a1292be93a9623d9bc06a9bafbf47b030f99010c`不变。正式发布脚本通过，远端APK哈希/长度与version.json核验一致；版本文件已还原。实际NFF后仅rebase一次，上游为无Binance重叠的Canvas功能，复用定向回归并以合并源码构建Release。发布后Branch字段的自动清理告警由统一收尾处理，不影响发布身份。

当前ADB清单为空，设备、原账号动态数据和官网同参数结果仍未验收。下一步原签名覆盖安装双包，验证数量越界、最低额不足/相等、检查中编辑、过期恢复与确认摘要；这些准备检查不需要提交交易。真实创建仍按用户H02最终确认后回读，不由AI代点。

当前Win研究MCP仍返回不含hosts的旧合同，hosts查询invalid_action；一次sessions动作未取得回执并最终expired。保存结构化证据后停止此路径，没有把旧研究资料或另一账号当作手机当前数据，未重启任何登录窗口。量化合并源码GitHub检查因付款/消费限额未启动，未冒充云端CI通过。

本批不改变Win、Chrome、网页登录资料、ADB服务或共享后端。整体商业Goal仍在进行；Feature Registry保持implemented，不标记为已验收。
