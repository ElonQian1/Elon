copilot 超出没有请求以后，是不是按照api key一样的付费
codex呢？
和自己使用openai /克劳德，同样的模型的api key相比
哪种更便宜


截至 **2026-06-01**，结论大概是：

**Copilot 超出额度后，已经越来越接近“API key 按 token 付费”的模式，但不是直接用你的 OpenAI/Claude API key 计费。** GitHub 现在用 **GitHub AI Credits** 计量：模型输入 token、缓存 token、输出 token 都会按模型价格折算成 credits，**1 AI Credit = $0.01**；额度用完后，要设置额外预算才会继续扣费，不是无条件自动乱扣。Copilot Pro / Pro+ / Max 每月分别包含 1,500 / 7,000 / 20,000 AI Credits；代码补全和 Next Edit suggestions 对付费用户仍不扣 AI Credits。([GitHub Docs][1])

旧的“premium requests”规则只主要影响还在旧年度套餐上的用户：例如 legacy Copilot Pro 是 300 requests/月，Pro+ 是 1500 requests/月，额外 premium request 曾是 **$0.04/request**；用完后可用 included models 或设置预算买更多。([GitHub Docs][2]) 但从 GitHub 官方公告看，**2026-06-01 起主线已经切到 usage-based billing**，也就是按 token 消耗算 credits。([The GitHub Blog][3])

**Codex 也类似，但分两种。**
用 ChatGPT 里的 Codex：Free / Go / Plus / Pro / Business 等套餐里包含一定 Codex 用量，Plus 是 $20/月，Pro 从 $100/月起；达到限制后，Plus / Pro 用户可以购买额外 credits 继续用。用 OpenAI API key 跑 Codex CLI / SDK / IDE extension，则是直接按 OpenAI API token 价格收费，而且没有一些云端功能，比如 GitHub code review、Slack 等。([OpenAI开发者][4])

### 和自己直接用 OpenAI / Claude API key 比，谁便宜？

| 场景                          | 通常更便宜                                                            |
| --------------------------- | ---------------------------------------------------------------- |
| 只是偶尔问代码、用量很少                | **直接 API key**，因为没有月费，只按 token 花钱                                |
| 每天在 IDE 里写代码，需要补全、聊天、解释代码   | **Copilot Pro / Pro+** 往往更划算，因为月费里包含 credits，而且代码补全不扣 AI Credits |
| 重度 agent 编程，频繁让 AI 改仓库、跑长任务 | **Codex Pro / Claude Max / Copilot Max** 这类订阅可能更划算，但要看是否经常撞限     |
| CI、批处理、自动化脚本、自己精细控制上下文      | **API key** 更可控，尤其可用 Batch/Flex 之类更低价模式                          |
| 超出套餐额度后的继续使用                | **基本接近 API 价格**，不是订阅内“无限便宜”了                                     |

看具体模型价格，Copilot 里的很多模型价格和 API 价格非常接近，甚至就是按对应模型 token rate 来折算。比如 GitHub Copilot 里 GPT-5.3-Codex 是 **$1.75 / 1M input tokens、$0.175 / 1M cached input、$14 / 1M output**；OpenAI API 的 gpt-5.3-codex 也是这个价格。([GitHub Docs][5]) Claude 也是类似：Copilot 表里 Claude Sonnet 4.6 是 **$3 input、$0.30 cached input、$3.75 cache write、$15 output / 1M tokens**；Anthropic 官方 Claude API 价格也列出 Sonnet 4.6 为 $3 input、$15 output，并有对应缓存价格。([GitHub Docs][5])

所以最关键的一句话是：

**在套餐额度内，Copilot / Codex / Claude Code 订阅通常比自己 API key 便宜；一旦超出额度并开始买额外 credits，大多数情况下就接近标准 API 价格了。**

有两个例外值得注意。第一，Copilot 付费用户使用 auto model selection 时，GitHub 说模型成本有 **10% 折扣**，所以某些交互可能比直接 API 略便宜。([GitHub Docs][1]) 第二，直接 API 如果你能用非实时的 Batch/Flex/缓存，就可能反过来比 Copilot/Codex 便宜；例如 Anthropic 标明 Batch 可省 50%，OpenAI API 也有不同服务层价格。([Claude][6])

### 我的建议

你如果主要写 Rust、在 VS Code / JetBrains 里边写边问，**先用 Copilot Pro 或 Pro+ 更省心**；它的补全价值很高，而且不用你自己管理上下文、token、IDE 插件和 billing。

你如果经常让 AI 自动改整个仓库、跑长任务、生成 PR，**Codex / Claude Code 的高阶订阅更像“买打包算力”**，在额度内可能比 API 划算很多；但撞限之后追加 credits 就要当成 API 价格来看。

你如果只是偶尔用，或者你愿意自己做工具链、严格控制上下文长度，**OpenAI / Anthropic API key 最便宜、最透明**。特别是长仓库任务，不要把整个 repo 都塞进去；直接 API 可以自己控制只传相关文件，这一点很省钱。

[1]: https://docs.github.com/en/copilot/concepts/billing/billing-for-individuals "Usage-based billing for individuals - GitHub Docs"
[2]: https://docs.github.com/en/copilot/concepts/billing/copilot-requests "Requests in GitHub Copilot (legacy) - GitHub Docs"
[3]: https://github.blog/news-insights/company-news/github-copilot-is-moving-to-usage-based-billing/ "GitHub Copilot is moving to usage-based billing - The GitHub Blog"
[4]: https://developers.openai.com/codex/pricing "Pricing – Codex | OpenAI Developers"
[5]: https://docs.github.com/copilot/reference/copilot-billing/models-and-pricing "Models and pricing for GitHub Copilot - GitHub Docs"
[6]: https://claude.com/pricing "Plans & Pricing | Claude by Anthropic"


我想共享出我的codex cli、copilot cli给我宿舍的舍友用，它每次都跑大量的上下文仓库，
超出额度，还要用我的api key的消耗，我应该怎么对他收费，他没有信用卡因此无法开通自己的，老是蹭我的，我要怎么收费比较好？


我建议你**不要卖“无限共享”**，也不要继续让他用你的主账号。最合理的是：

> **只允许他用独立项目 / 独立 key / 独立 GitHub 账号 seat；先充值，再按真实账单扣；实际成本 × 1.15～1.25；余额不足就停。**

原因很简单：他每次跑大量仓库上下文，成本不是“问几句话”的水平，而是可能一次几毛到几美元，agent 多轮跑起来一个月上百美元很正常。OpenAI 官方 Codex 文档也说，Codex 平均成本大约是 **每位开发者每月 100～200 美元**，但会因模型、实例数量、自动化和 fast mode 等差异很大。([OpenAI Help Center][1])

## 先把底线说清楚：不要共享账号密码

OpenAI 明确说账号是给创建者本人用的，别人需要使用应该注册自己的账号；共享登录凭证会带来安全和误用风险。([OpenAI Help Center][2]) OpenAI 的使用条款也写明不能共享账号凭证或把账号给别人用，你要对账号下发生的活动负责。([OpenAI][3]) GitHub 也很直接：一个 login 只能一个人用，不能多人共享。([GitHub Docs][4])

所以我的建议是：

**Codex / OpenAI API 可以给他“独立项目 + 独立 key”，但不要给你的主 key。Copilot CLI 不要共享你的 GitHub 登录；要么他用自己的 GitHub 账号，要么你用组织 seat 正规分配给他。**

OpenAI API 平台支持项目成员生成只限于某个项目和资源范围的个人 API key，这很适合给他单独算账。([OpenAI Help Center][5]) GitHub Copilot 在组织/企业方案里，Copilot seat 是分配给唯一 GitHub 用户账号的 license，组织 owner 可以给具体用户授权，授权后开始计费。([GitHub Docs][6])

## 我会这样收费

### 最推荐：预付制

让他先给你 **$100 预存款**。然后每个月按下面公式扣：

```text
他应付金额 = 他的实际账单成本 × 1.20
```

也就是你收 **20% 风险/管理费**。这 20% 不是赚大钱，而是覆盖这些风险：

他误跑整仓库、预算限制延迟生效、工具调用、容器费用、你要查账、你承担信用卡和账号风险。

OpenAI 官方说 API 可以设置月度预算，但限制执行可能有延迟，超出的部分仍由你负责。([OpenAI][7]) OpenAI 项目预算也更像软性支出门槛和提醒，超过后请求仍可能继续处理，不是绝对硬停。([OpenAI Help Center][5]) 所以**不要后付**，一定要预付。

### 我的建议价格表

| 类型                |                          你可以怎么收 |
| ----------------- | ------------------------------: |
| 偶尔用，轻量聊天/改小文件     |       **$30/月预存**，按实际成本 × 1.2 扣 |
| 经常用 Codex CLI 改项目 |      **$100/月预存**，按实际成本 × 1.2 扣 |
| 经常整仓库 agent、多轮跑任务 | **$150～$200/月预存**，按实际成本 × 1.2 扣 |
| 不接受预存、只想月底给       |                         **不要借** |

如果他真的“每次都跑大量上下文仓库”，我会直接要求：

```text
每月先付 $150。
月底给他看账单。
实际消耗 × 1.2 后从余额扣。
余额低于 $20 就暂停。
超过 $150 需要再次充值。
```

不要给他“$20/月随便用”这种套餐。那是在拿你的信用卡给他开无限额度。

## Codex / OpenAI API 怎么算账

如果他用的是 OpenAI API key 跑 Codex CLI，就按项目 usage 算最清楚。OpenAI API 的 gpt-5.3-codex 标准价是 **$1.75 / 1M input tokens、$0.175 / 1M cached input tokens、$14 / 1M output tokens**；Priority 模式是 **$3.50 / $0.35 / $28**。([OpenAI开发者][8])

你可以这样解释给他：

```text
一次仓库任务费用 =
输入 token 百万数 × 输入单价
+ 缓存输入 token 百万数 × 缓存单价
+ 输出 token 百万数 × 输出单价
+ 工具/容器费用
```

举例，假设他一次任务扫了 **500k input tokens**，输出 **50k tokens**，用 gpt-5.3-codex 标准模式：

```text
0.5 × $1.75 + 0.05 × $14
= $0.875 + $0.70
= $1.575

你收他：
$1.575 × 1.2 = $1.89
```

如果一次任务是 **2M input tokens + 200k output tokens**：

```text
2 × $1.75 + 0.2 × $14
= $3.50 + $2.80
= $6.30

你收他：
$6.30 × 1.2 = $7.56
```

这还没算工具调用、容器、web search 等附加费用；OpenAI 文档里工具和容器也是单独计费的，例如 web search、containers、file search 等都有自己的计费项。([OpenAI开发者][8])

所以他如果一天跑 10 次大任务，一个月真的可能是 **$100～$300+**，不是夸张。

## Copilot CLI 怎么收费

Copilot 现在是按 **GitHub AI Credits** 算，**1 AI Credit = $0.01**；Copilot Pro / Pro+ / Max 每月分别包含 **1,500 / 7,000 / 20,000 AI Credits**。([GitHub Docs][9]) Copilot CLI、Copilot Chat、cloud agent、Spaces、Spark、第三方 coding agents 都会消耗 AI Credits；但付费计划里的代码补全和 Next Edit suggestions 不扣 AI Credits。([GitHub Docs][9])

如果你能合法地给他一个独立 Copilot seat，那么收费公式可以是：

```text
Copilot 费用 =
seat 月费
+ 他额外消耗的 AI Credits × $0.01
+ 15%～20% 风险/管理费
```

如果你只是把自己的 GitHub Copilot 登录给他用，那我建议**不要这么做**。因为 GitHub 明确不允许多人共享同一个 login。([GitHub Docs][4])

如果你想比较正规，可以考虑 GitHub organization / Copilot Business seat。GitHub 公告说 Copilot Business 是 **$19/user/month**，并包含对应的月度 AI Credits；Copilot 的用量会按 token，包括输入、输出、缓存 token，按模型价格折算。([The GitHub Blog][10]) 但这条路要看你当前账号是否能开通/购买，因为 GitHub 文档也显示 2026 年 4 月起部分新自助注册临时暂停。([GitHub Docs][11])

## 具体操作建议

你现在应该做这几件事：

1. **立刻停用他正在用的主 API key**，重新生成你的个人 key。OpenAI 服务协议明确限制共享登录凭证，且禁止买卖、转让 API keys。([OpenAI][12])

2. 在 OpenAI dashboard 建一个项目，名字比如：

```text
roommate-codex
```

3. 只给这个项目开他需要的模型，比如先只开：

```text
gpt-5.3-codex
gpt-5.4-mini
```

不要一上来给 GPT-5.5、Priority、图像、web search、containers 全部开放。OpenAI 项目可以管理 Model Usage 和 rate limits。([OpenAI Help Center][5])

4. 让他先预存，比如：

```text
首次充值：$100
余额低于：$20 停用
单日建议上限：$10～$20
每月硬性人情上限：$150，超过必须先补钱
```

5. 每月底把 OpenAI 项目 usage 截图发他，按：

```text
实际成本 × 1.2
```

扣余额。

6. Copilot CLI 不要用你的个人 GitHub 登录给他跑。要么他自己搞 GitHub Student / Free / 自己的 Copilot，要么你用组织 seat 给他独立授权。GitHub 学生计划是给 verified students 免费使用 Copilot 功能的，但当前文档显示新注册曾临时暂停，所以他能不能马上开通要以 GitHub 页面为准。([GitHub Docs][13])

## 你可以直接这样跟他说

> 以后不能再用我的主账号和主 API key。AI coding agent 不是普通聊天，它会扫仓库、跑多轮上下文，账单可能很高。你要用可以，我给你单独建一个项目/key，先预存 $100，每月按实际账单 ×1.2 扣，余额低于 $20 就暂停。Copilot 登录我不能共享，因为 GitHub 和 OpenAI 都不允许多人共用账号。你没有信用卡没关系，可以转账/现金给我预存，但不能让我先垫无限额度。

我的底线建议是：

**Codex/API：可以代付，但必须独立项目 + 预存 + 实际成本 × 1.2。**
**Copilot CLI：不要共享个人账号；能分配独立 seat 才给用。**
**他不愿预付，就不要借。**

[1]: https://help.openai.com/zh-hant/articles/20001106-codex-rate-card "Codex 費率表 | OpenAI Help Center"
[2]: https://help.openai.com/zh-hant/articles/10471989-openai-account-sharing-policy "OpenAI 帳戶共用政策 | OpenAI Help Center"
[3]: https://openai.com/policies/row-terms-of-use/ "Terms of Use | OpenAI"
[4]: https://docs.github.com/site-policy/github-terms/github-terms-of-service "GitHub Terms of Service - GitHub Docs"
[5]: https://help.openai.com/zh-hant/articles/9186755-managing-projects-in-the-api-platform "在 API 平台中管理專案 | OpenAI Help Center"
[6]: https://docs.github.com/en/copilot/reference/copilot-billing/seat-assignment "GitHub Copilot seat assignment - GitHub Docs"
[7]: https://openai.com/api/pricing/ "OpenAI API Pricing | OpenAI"
[8]: https://developers.openai.com/api/docs/pricing "Pricing | OpenAI API"
[9]: https://docs.github.com/copilot/concepts/billing/usage-based-billing-for-individuals "Usage-based billing for individuals - GitHub Docs"
[10]: https://github.blog/news-insights/company-news/github-copilot-is-moving-to-usage-based-billing/ "GitHub Copilot is moving to usage-based billing - The GitHub Blog"
[11]: https://docs.github.com/en/copilot/get-started/plans "Plans for GitHub Copilot - GitHub Docs"
[12]: https://openai.com/policies/services-agreement/ "Services agreement | OpenAI"
[13]: https://docs.github.com/copilot/how-tos/manage-your-account/free-access-with-copilot-student "Access GitHub Copilot for free as a student - GitHub Docs"


他总是用我codex的包月额度，这些包月额度我应该怎么样折算给它？


我建议你把 **Codex 包月额度分成两种价**：

1. **不影响你使用时：按订阅成本分摊。**
2. **影响你、把你用到限额、逼你买 credits / 用 API key 时：按 credits 的替代成本收费。**

他现在这种“总是跑大量上下文仓库”的情况，应该按第 2 种算，不要按普通合租分摊算。

另外提醒一句：OpenAI 的 Pro 说明里明确把“共享账号凭证、把账号给别人用、转售访问”列为不允许的行为，所以更安全的做法是：让他用自己的 ChatGPT 账号，你替他付款或让他现金/转账给你，而不是继续登录你的账号。([OpenAI Help Center][1])

## 最公平的折算法：按 credits 记账

OpenAI 现在的 Codex 已经明确按 token / credits 计量，Codex rate card 里列出不同模型每 100 万输入、缓存输入、输出 token 消耗多少 credits；OpenAI 也说 Codex 额度会因代码库大小、任务复杂度、本地/云端执行而变化，大代码库和长会话会显著消耗更多额度。([OpenAI Help Center][2])

我会这样定价：

```text
他应付金额 = 他本月消耗的 Codex credits × $0.04 × 1.2
```

这里的 **$0.04 / credit** 是一个合理折算基准：OpenAI 学生 Codex credits 页面写着 **2,500 credits 等价于 $100**，换算就是 **$100 ÷ 2500 = $0.04/credit**。([OpenAI Help Center][3])
后面的 **1.2** 是你的风险费 / 管理费 / 信用卡垫付费。你也可以收 1.15，但我建议收 **1.2**。

所以：

```text
1 credit ≈ $0.04
你向他收：1 credit ≈ $0.048
```

如果他一个月用了 1,000 credits：

```text
1000 × 0.04 × 1.2 = $48
```

如果他一个月用了 2,500 credits：

```text
2500 × 0.04 × 1.2 = $120
```

如果他一个月用了 5,000 credits：

```text
5000 × 0.04 × 1.2 = $240
```

对你这种情况，我会直接要求他：

```text
每月先预存 $100。
余额低于 $20 停止使用。
月底按 credits × $0.048 扣。
不预存就不给用。
```

## 如果看不到详细 credits，就按任务类型估算

OpenAI 的 Codex rate card 给了 Plus / Pro 下的平均消耗：本地 Local Task 每条消息大约 GPT-5.5 是 14 credits、GPT-5.4 是 7 credits、GPT-5.3-Codex 是 5 credits、GPT-5.4-mini 是 2 credits；Cloud Task 和 Code Review 大约都是 25 credits。([OpenAI开发者][4])

你可以给他一张简单价格表：

| 他用的类型              | 官方平均 credits |   成本价 | 你收他，含 20% |
| ------------------ | -----------: | ----: | --------: |
| GPT-5.4-mini 本地一次  |  约 2 credits | $0.08 | **$0.10** |
| GPT-5.3-Codex 本地一次 |  约 5 credits | $0.20 | **$0.24** |
| GPT-5.4 本地一次       |  约 7 credits | $0.28 | **$0.34** |
| GPT-5.5 本地一次       | 约 14 credits | $0.56 | **$0.67** |
| Cloud Task 一次      | 约 25 credits | $1.00 | **$1.20** |
| Code Review 一个 PR  | 约 25 credits | $1.00 | **$1.20** |

但是这张表只能做“保底估算”。如果他每次都扫整个仓库、塞很多上下文、开 MCP、长时间 agent 循环，实际消耗会比普通任务高。OpenAI 也明确建议控制 prompt 大小、减少 AGENTS.md 注入、限制 MCP server，因为这些都会增加上下文和额度消耗。([OpenAI开发者][4])

所以我建议你对他说：

> 普通本地消息按表算；整仓库、大任务、云端任务、自动修 bug、多轮 agent，一律按 Usage Dashboard 的实际 credits 算。

OpenAI 文档说你可以在 Codex usage dashboard 看当前限制，CLI 会话里也可以用 `/status` 看剩余额度。([OpenAI开发者][4])
实际操作就是让他每次使用前后截图：

```text
使用前：/status 截图
使用后：/status 截图
差额 credits × $0.048 = 本次费用
```

## 如果他不愿意每次截图，就卖“固定额度包”，不要卖无限用

你可以这样给他三个档：

| 套餐  |               他先付 |            包含额度 | 适合情况                       |
| --- | ----------------: | --------------: | -------------------------- |
| 轻量包 |         **$30/月** |   约 625 credits | 小文件、小 bug、少量本地任务           |
| 中量包 |         **$60/月** | 约 1,250 credits | 每周几次 Codex 改代码             |
| 重度包 |        **$120/月** | 约 2,500 credits | 经常跑仓库、cloud task、PR review |
| 超出后 | **$0.048/credit** |          用多少扣多少 | 不允许欠账                      |

这个表里面：

```text
额度 = 他付的钱 ÷ 0.048
```

比如 $120：

```text
120 ÷ 0.048 = 2500 credits
```

对你室友这种“老是跑大量上下文仓库”的人，我不会给 $30 包。我会从 **$100 或 $120 预存** 起步。

## 如果你想按你的订阅费折算

OpenAI 当前 Codex 个人计划大致是：Plus $20/月，Pro 从 $100/月起；Pro 有 5x 或 20x 比 Plus 更高的额度，$100 Pro 标准是 5x Plus，$200 Pro 是 20x Plus。OpenAI 文档还写过 $100 Pro 到 2026-05-31 有 2x Codex promo，也就是临时 10x；但今天已经是 2026-06-01，所以除非你的 dashboard 仍显示 promo，否则不要再按 promo 价算。([OpenAI开发者][4])

按“Plus 等价额度”折算：

| 你的方案        | Codex 等价额度 | 每 1x Plus 额度成本 |
| ----------- | ---------: | -------------: |
| Plus $20    |         1x |   **$20 / 1x** |
| Pro $100 标准 |         5x |   **$20 / 1x** |
| Pro $200 标准 |        20x |   **$10 / 1x** |

如果他用了你一半的 Pro $100 额度：

```text
$100 × 50% × 1.2 = $60
```

如果他用了你 70% 的 Pro $100 额度：

```text
$100 × 70% × 1.2 = $84
```

如果因为他，你必须从 Plus $20 升到 Pro $100，那最合理的是：

```text
他至少付升级差价：$100 - $20 = $80
再加超额 credits 或管理费
```

如果因为他，你必须从 Pro $100 升到 Pro $200，那他至少付：

```text
$200 - $100 = $100
```

这个规则很重要：
**谁导致你升级，谁付升级差价。**

## 我给你的最终收费方案

我建议你直接执行这个：

```text
1. 不再让他无限用你的 Codex。
2. 他每月先预存 $120。
3. 每次使用前后用 /status 或 Usage Dashboard 截图。
4. 按实际 credits × $0.048 扣。
5. 如果看不到 credits：
   - GPT-5.3-Codex 本地一次 $0.25
   - GPT-5.4 本地一次 $0.35
   - GPT-5.5 本地一次 $0.70
   - Cloud Task / Code Review 一次 $1.20
6. 余额低于 $20 停止。
7. 如果他导致你升级套餐，他付升级差价。
```

你可以直接跟他说：

> Codex 包月额度不是免费的。你跑大仓库会消耗我的限额，还可能把我顶到要买 credits 或用 API key。以后按 credits 算，1 credit 收 $0.048。你先预存 $120，余额低于 $20 就暂停。要是不想按次截图，那就按重度包 $120/月，超出继续按 credits 扣。不给预存就不能用。

我的真实建议：
**不要跟他算友情价。**
他没有信用卡不是你的问题；你可以帮他代付，但不能让他拿你的账号和额度当无限资源。

[1]: https://help.openai.com/en/articles/9793128-about-chatgpt-pro-tiers "About ChatGPT Pro tiers | OpenAI Help Center"
[2]: https://help.openai.com/en/articles/20001106-codex-rate-card "Codex rate card | OpenAI Help Center"
[3]: https://help.openai.com/en/articles/20001147-codex-credits-for-students-terms-of-service "Codex credits for Students - Terms of Service | OpenAI Help Center"
[4]: https://developers.openai.com/codex/pricing "Pricing – Codex | OpenAI Developers"


他前面几个月都有用我的codex和copilot包月，都用50%的包月额度就被我叫停了，我想让他给回我钱，我想知道50%大概是多少token,怎么折算成token让他给钱


最现实的结论是：

**不要直接说“他用了 50% = 固定多少 token”。这不准确。**
你应该说：

> 你用了我每月 50% 的 Codex / Copilot 额度。因为官方都是按 input token、cached input token、output token 分别计价，所以 50% 不能唯一换成一个 token 数。我按官方 credits / 月费折算成美元，再给你 token 等价范围。

这样他比较难赖账。

## 先给你一个可以直接要钱的算法

你可以按这个公式算：

```text
他每个月应还 =
Codex 包月价值 × 50%
+ Copilot 包月额度价值 × 50%
+ 他造成的 API key / credits 超额实际账单
```

如果你想加一点管理费，可以乘以 1.1 或 1.2：

```text
友情版：实际应还 × 1.0
正常版：实际应还 × 1.1
严格版：实际应还 × 1.2
```

我建议你追过去几个月的钱时，**不要一上来乘 1.2**，先按 1.0 算。他要是继续用，之后再按预付制 + 1.2。

---

# 1. Codex 的 50% 怎么算

Codex 的包月额度没有一个固定公开的“每月多少 token”数字，因为 Codex 的消耗取决于模型、input、cached input、output、仓库大小、上下文长度、任务复杂度。OpenAI 官方也说 Codex 现在按 token-based rate card 计算 credits，而且实际 credit 用量取决于 input / cached input / output 的组合。([OpenAI Help Center][1])

所以 Codex 最公平的追账方式是：

```text
Codex 50% = 你的 ChatGPT / Codex 订阅月费 × 50%
```

如果你是这些套餐：

| 你的 Codex / ChatGPT 套餐 | 他用了 50% |   你可以让他每月还 |
| --------------------- | ------: | ---------: |
| Plus $20/月            |     50% |  **$10/月** |
| Pro $100/月            |     50% |  **$50/月** |
| Pro $200/月            |     50% | **$100/月** |

OpenAI 官方说明里把 Plus、Pro $100、Pro $200 分成不同用量层级，Pro $100 是 Plus 的 5x 限额，Pro $200 是 Plus 的 20x 限额；之前到 2026-05-31 还有 Codex 临时加量活动。([OpenAI Help Center][2])

## Codex 50% 换成 token 大概是多少？

OpenAI 的 Codex credits 可以用一个公开换算：**2,500 credits = $100**，所以：

```text
1 credit = $0.04
$1 = 25 credits
```

这个来源是 OpenAI 学生 Codex credits 页面，里面写明 2,500 credits 等价于 $100。([OpenAI Help Center][3])

用这个换算，Codex 50% 的 token 等价大概是：

### 如果你是 Plus $20/月，他用了 50%

也就是你向他收 **$10**，等价于 **250 Codex credits**。

| 模型            | 约等于 input tokens | 约等于 cached input tokens | 约等于 output tokens |
| ------------- | ---------------: | ----------------------: | ----------------: |
| GPT-5.5       |            200 万 |                 2,000 万 |              33 万 |
| GPT-5.4       |            400 万 |                 4,000 万 |              67 万 |
| GPT-5.3-Codex |            571 万 |                 5,714 万 |              71 万 |
| GPT-5.4-mini  |          1,333 万 |                  1.33 亿 |             221 万 |

### 如果你是 Pro $100/月，他用了 50%

也就是你向他收 **$50**，等价于 **1,250 Codex credits**。

| 模型            | 约等于 input tokens | 约等于 cached input tokens | 约等于 output tokens |
| ------------- | ---------------: | ----------------------: | ----------------: |
| GPT-5.5       |          1,000 万 |                     1 亿 |             167 万 |
| GPT-5.4       |          2,000 万 |                     2 亿 |             333 万 |
| GPT-5.3-Codex |          2,857 万 |                  2.86 亿 |             357 万 |
| GPT-5.4-mini  |          6,667 万 |                  6.67 亿 |           1,106 万 |

### 如果你是 Pro $200/月，他用了 50%

也就是你向他收 **$100**，等价于 **2,500 Codex credits**。

| 模型            | 约等于 input tokens | 约等于 cached input tokens | 约等于 output tokens |
| ------------- | ---------------: | ----------------------: | ----------------: |
| GPT-5.5       |          2,000 万 |                     2 亿 |             333 万 |
| GPT-5.4       |          4,000 万 |                     4 亿 |             667 万 |
| GPT-5.3-Codex |          5,714 万 |                  5.71 亿 |             714 万 |
| GPT-5.4-mini  |           1.33 亿 |                 13.33 亿 |           2,212 万 |

这里的计算依据是 OpenAI Codex rate card：例如 GPT-5.3-Codex 每 100 万 input token 是 43.75 credits，cached input 是 4.375 credits，output 是 350 credits；GPT-5.5 则是 125 / 12.5 / 750 credits。([OpenAI Help Center][1])

注意：**这些不是说他真的用了这么多 token，而是说“你收他这些钱，按官方价格大约能买到多少 token”。** 因为真实任务里 input、cached input、output 是混合的，不能只看一个数字。

---

# 2. Copilot 的 50% 怎么算

Copilot 要分两种情况：

## 情况 A：如果是 2026-06-01 之后的新 AI Credits 计费

GitHub Copilot 现在按 GitHub AI Credits 计量，**1 AI Credit = $0.01**；Copilot Pro / Pro+ / Max 每月分别包含 **1,500 / 7,000 / 20,000 AI Credits**。([GitHub Docs][4])

所以他用了 50%，你可以按这个收：

| Copilot 套餐         |  每月 AI Credits |        他用了 50% |        额度价值 |
| ------------------ | -------------: | -------------: | ----------: |
| Copilot Pro $10/月  |  1,500 credits |    750 credits | **$7.50/月** |
| Copilot Pro+ $39/月 |  7,000 credits |  3,500 credits |   **$35/月** |
| Copilot Max $100/月 | 20,000 credits | 10,000 credits |  **$100/月** |

这个算法比“月费 × 50%”更合理，因为他用掉的是你的 AI Credits 额度；GitHub 也写明额外预算按 **1 AI Credit = $0.01** 消耗，比如 $10 budget 对应 1,000 credits。([GitHub Docs][4])

## Copilot 50% 换成 token 大概是多少？

按 GitHub Copilot 官方模型价格，GPT-5.3-Codex 在 Copilot 里是：

```text
input: $1.75 / 100万 tokens
cached input: $0.175 / 100万 tokens
output: $14 / 100万 tokens
```

GPT-5.5 是：

```text
input: $5 / 100万 tokens
cached input: $0.5 / 100万 tokens
output: $30 / 100万 tokens
```

这些价格来自 GitHub Copilot 的模型价格表。([GitHub Docs][5])

如果按 **GPT-5.3-Codex** 来折算：

| Copilot 套餐   | 50% 额度价值 | 约等于 input tokens | 约等于 cached input tokens | 约等于 output tokens |
| ------------ | -------: | ---------------: | ----------------------: | ----------------: |
| Copilot Pro  |    $7.50 |            429 万 |                 4,286 万 |              54 万 |
| Copilot Pro+ |      $35 |          2,000 万 |                     2 亿 |             250 万 |
| Copilot Max  |     $100 |          5,714 万 |                  5.71 亿 |             714 万 |

如果按更贵的 **GPT-5.5** 来折算：

| Copilot 套餐   | 50% 额度价值 | 约等于 input tokens | 约等于 cached input tokens | 约等于 output tokens |
| ------------ | -------: | ---------------: | ----------------------: | ----------------: |
| Copilot Pro  |    $7.50 |            150 万 |                 1,500 万 |              25 万 |
| Copilot Pro+ |      $35 |            700 万 |                 7,000 万 |             117 万 |
| Copilot Max  |     $100 |          2,000 万 |                     2 亿 |             333 万 |

所以你跟他说时可以这样讲：

> 你用掉的不是一个固定 token 数，而是大概相当于几百万到几千万 input tokens，或者几十万到几百万 output tokens。output token 很贵，所以不能只按总 token 数算。

---

## 情况 B：如果是 2026-06-01 之前的 Copilot legacy premium requests

如果你说的是前几个月，很可能那时候 Copilot 还在用 legacy premium requests。GitHub 文档写过：Copilot Pro 每月 300 premium requests，Copilot Pro+ 每月 1,500 premium requests，额外 premium request 是 **$0.04/request**。([GitHub Docs][6])

那你可以这样收：

| Copilot 套餐   | 每月 premium requests | 他用了 50% | 按 $0.04/request 折算 |
| ------------ | ------------------: | ------: | -----------------: |
| Copilot Pro  |                 300 |     150 |           **$6/月** |
| Copilot Pro+ |               1,500 |     750 |          **$30/月** |

这个比用 token 算更稳，因为旧 Copilot 的账单单位本来就不是 token，而是 premium request。GitHub legacy 文档也说 premium request 可以用于高级 chat、premium model、large context、cloud agent、Spark 等功能。([GitHub Docs][7])

---

# 3. 你可以直接给他开的账单模板

假设你是：

```text
ChatGPT Pro $100/月
Copilot Pro+ $39/月
他每月都用了 50%
用了 3 个月
```

那你可以这样算：

```text
Codex:
$100 × 50% = $50/月

Copilot Pro+:
如果按现在 AI Credits:
7000 credits × 50% × $0.01 = $35/月

每月合计:
$50 + $35 = $85/月

3 个月:
$85 × 3 = $255
```

如果那几个月还是 Copilot legacy premium requests：

```text
Codex:
$100 × 50% = $50/月

Copilot Pro+ legacy:
1500 requests × 50% × $0.04 = $30/月

每月合计:
$50 + $30 = $80/月

3 个月:
$80 × 3 = $240
```

如果你想收友情价，可以收整数：

```text
3 个月你给我 $240 就行。
之后要继续用，必须先预存，不再后付。
```

---

# 4. 我建议你实际怎么跟他说

你可以直接复制这段：

> 前几个月你每个月大概用了我 Codex 和 Copilot 50% 的包月额度。我按官方价格折算，不按乱喊价。Codex 按我的 ChatGPT/Codex 月费 50% 算；Copilot 按官方 AI Credits 或旧 premium requests 算。
>
> 这个不能精确说成一个固定 token 数，因为 input、cached input、output 的价格不同，而且大仓库 agent 会消耗很多上下文。按官方价格换算，你每个月大概用了价值 $XX 的额度。过去 X 个月合计 $XX。
>
> 这次我不加管理费，按成本价收。以后如果还要用，必须先预存，不能再先用我的额度再说。

---

## 你最该采用的追账口径

我建议你这样定：

```text
Codex：按订阅月费 × 50%
Copilot：
  - 2026-06-01 后：按 AI Credits × 50% × $0.01
  - 2026-06-01 前：按 premium requests × 50% × $0.04
API key 超额：按实际账单 100% 补
```

最重要的是：
**过去的账，按成本价收回来；以后的账，必须预付。**

[1]: https://help.openai.com/en/articles/20001106-codex-rate-card "Codex rate card | OpenAI Help Center"
[2]: https://help.openai.com/en/articles/9793128-about-chatgpt-pro-tiers?utm_source=chatgpt.com "About ChatGPT Pro tiers"
[3]: https://help.openai.com/en/articles/20001147-codex-credits-for-students-terms-of-service "Codex credits for Students - Terms of Service | OpenAI Help Center"
[4]: https://docs.github.com/copilot/concepts/billing/usage-based-billing-for-individuals "Usage-based billing for individuals - GitHub Docs"
[5]: https://docs.github.com/copilot/reference/copilot-billing/models-and-pricing "Models and pricing for GitHub Copilot - GitHub Docs"
[6]: https://docs.github.com/en/copilot/reference/copilot-billing/request-based-billing-legacy/copilot-requests?utm_source=chatgpt.com "Requests in GitHub Copilot (legacy)"
[7]: https://docs.github.com/en/copilot/reference/copilot-billing/request-based-billing-legacy/github-copilot-premium-requests?utm_source=chatgpt.com "Overview of request-based billing (legacy)"


包月50%换算成token是多少钱？比直接按包月39美元一个月的一半贵多少？


