# 消费者 AI 本人 App 目录 MCP V1 验收

状态：`implementation_uncompiled`

## 已形成代码

- 只列出当前项目中当前用户拥有的 App。
- 返回当前 MCP 身份、App 状态和沙箱可用标记。
- 不返回测试 Token、Token 提示、生产凭据或其他用户标识。
- MCP 协议初始化说明已从主路由抽到独立模块，源文件规模门禁通过。

## 最终统一回归

- 空目录、单个 App、多人项目和跨项目隔离。
- 活跃、停用、沙箱和生产 App 状态。
- 默认 MCP 身份和显式 App 身份的当前项标记。
- 响应不存在 Token、提示、哈希、凭据和所有者 ID。
- 工具清单、MCP 初始化及主路由行为保持一致。

本批按快速实现策略只完成 Rust 格式化、静态差异和文件规模检查；未执行 Cargo 编译、MCP 调用、权限或端到端验证。
