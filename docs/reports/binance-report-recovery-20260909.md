---
version_status: current
reviewed_at: 2026-09-09
---

# 币安记录兼容与诊断

量化 0.6.8 真机读取到 1 条网格及非空成交页（本页20条、查询共60条），当前委托和策略持仓返回失败。不能把后两项称为可用。

本批主应用新增固定记录诊断：查询种类、请求/解析/身份复核阶段、成功/失败状态、HTTP状态和六位业务代码。既有 `binance_host_status` 返回这些元数据，原始记录、账户、凭据及错误消息不进入诊断。旧无记录诊断载荷继续兼容。

可选数值或展示字段格式变化时投影为未知；必填账户、策略、合约、方向及分页边界继续严格验证。无策略标识的数据不会被当作已验证空列表。不改变交易请求、授权或网站登录资料。

1.1.1607已正式发布并覆盖安装；APK摘要与服务器一致。当前委托恢复62条（买入47、卖出15），首条价格和数量有显示；策略持仓恢复1条，但数量和均价因上游JSON数字形式尚未正确展示。

追加修复在JSON解析前保留数字原文，并以字符串展开科学计数小数，不通过浮点数转金额；先验证原JSON语法，拒绝数字键等非法输入。可选字段继续严格控制范围，不支持的精度仍显示未知。23项Node套件通过（另含既有读取适配器19个断言），覆盖高精度数字、科学计数、字符串转义和非法JSON；Android记录/诊断8项定向测试仍适用。

最终1.1.1609(1609)由源码 `aab9208e44cc28e37d4b35f69f15492d55a888ff` 正式发布，SHA-256 `5c15f4e6372881b2b603066386174e221ab26e1804e94a12fd2ed78991a810d8`，与服务器一致；已用原签名 `adb install -r` 覆盖安装。量化0.6.9回读1条网格、62条委托、20/60条成交、1条持仓。持仓页数量与开仓均价均显示，5个文字字段无未知项；固定诊断为身份复核完成、HTTP200、业务000000。[有界验收证据](evidence/binance-report-recovery-20260909.json)。未执行交易。

更新后首次后台恢复未启动主应用登录上下文；正常打开主应用后，量化恢复已有同意及币安登录，未再次登录或授权。该冷启动恢复条件保留为后续体验缺口。发布成功，但自动工作树清理报缺少Branch属性；最终仓库收尾另行报告。
