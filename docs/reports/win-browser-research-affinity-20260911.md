---
version_status: current
reviewed_at: 2026-09-11
implementation_status: implemented
---

# Win 研究请求实例归属验证

## 结果与边界

研究桥接原来按全局队列竞争，任意 Win 实例可领取不属于它的会话请求。
本批将动作绑定原生进程与当前用户派生的实例 ID，并在节点单锁内核验租约和归属。
前端拒绝不匹配动作，领取后不重发原生操作；账号变化丢弃旧用户结果。

原生身份只公布当前用户实际持有 WebView 宿主的项目及会话 ID，最多 8 项。
新开会话的成功回执立即绑定，填补下一次心跳之前的间隔。节点重启可从运行宿主恢复；
原生进程重启或换用户得到新身份，不能接管旧会话。多宿主时 MCP 明确选择，Win 页面自选当前实例。
新节点拒绝旧桥接无身份领取；须配套更新节点、桌面壳和 PC 前端。

| 层次 | 本批证据 | 状态 |
|---|---|---|
| 根因复现 | 基线真实前端执行器，所属/非所属实例先领取两种顺序 | 已复现分配缺陷；未逐请求关联线上 PID |
| 前端 | 研究模块 23 项测试、TypeScript 检查、Vite 正式构建 | 通过 |
| 节点合同 | 导入真实 hub/API/MCP 的 harness 20 项 | 通过 |
| 原生 Win | 实际 desktop crate 研究模块 43 项测试 | 通过；其余 158 项未在此过滤器执行 |
| 完整节点 | 实际 elon-pc-node 完整构建，关闭 sccache | 通过 |
| Win 发布 | 正式安装包及远端版本均绑定 `98e3024b7966800cd72e8bf19d1cb82236a05efd` | 发布通过；本机未加载 |
| 云端、网页 | 服务 `0.3.1741` 同源；三个关键前端文件与构建哈希一致 | 发布与只读校验通过 |
| 当前币安只读链路 | 对应发布版本的列表、源码和请求读取 | 待验证；没有提交交易 |

最初节点回归为 18 通过、1 失败：已取消动作的旧测试仍用空领取请求，触发新版 400，
未到预期的取消冲突 409。修正测试的实例身份后继续验证取消语义，并补充真正旧客户端拒绝测试，
最终 20 项全部通过。失败记录保留，不改写为首次全通过。

完整节点第一次 check 在 `thiserror` 依赖的构建程序发生 Windows 访问冲突，未到本项目编译。
改用正式验证入口的完整 build、关闭 sccache 后成功，耗时约 142 秒；没有修改依赖版本或删除缓存。
前端最终 build 和 23 项回归在页面首次加载的身份解析修正后重新通过。

## 验证覆盖

- 所属/非所属实例任意先后轮询、错误显式实例、无实例旧客户端、重复领取。
- 项目隔离、宿主发现不泄漏其他项目会话；账号变化期间心跳与领取不执行旧命令。
- 宿主租约过期、新进程/新用户不接管、原进程心跳恢复、节点重启重新登记。
- 登记及绑定容量、整体心跳校验失败不部分写入；回执一致重投和冲突拒绝。
- 既有站点、范围、大小、凭据边界与结果校验；路由字段在进入原生业务命令前移除。
- 原生实例在运行时克隆/前端重载间一致，两个运行时或用户之间不同。

独立工件组 `win-browser-research-affinity-v1` 保存工作流、运行日志和后续安装证据。
构建缓存 doctor 报可用空间 7.72%，dry-run 没有可清理目标；未删除任何缓存，
正式验证入口仍允许并完成上述验证。没有重启或关闭现有登录窗口。

## 下一验收

正式 Win 版本为 `0.3.69+98e3024b7966800cd72e8bf19d1cb82236a05efd`，客户端 ZIP 的
SHA-256 为 `6163dfbcb0ce54c39b98196d2633d2cf19e316586fa15d7a4005fd8af5b08906`。
远端 outbox 首次同步成功；Win 发布约 346 秒，配套服务/前端发布约 746 秒，正式脚本均成功结束。
线上 BrowserResearchPage、browserResearchHost 及 App 三份脚本与发布构建完全一致。
此前安装的 Win 仍运行 `0.3.69+c87f2914577c05011d5b3fda1f110b09a6908f5b`；
本机正式客户端与另一调试客户端保持原进程，没有通过关闭窗口强行切换版本。

更新草稿仅指定上述精确版本，未发送。已请求本次正式 Win 重启授权，保持待用户答复；
此前只允许过单次重启，不能复用为永久窗口关闭授权。Chrome、另一调试客户端与登录 Profile 保留。
取得实际安装版本后，先从无交易样例验证宿主列表与定向，再在既有授权账户做只读采集。
只有取得新版现场证据后，才把对应安装和币安验收项改为通过。
继续量化高级止盈止损等研究；本批不替代量化商业 Goal 的剩余功能及必要人工交易验收。
