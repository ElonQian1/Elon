# New Personal Text Default, September 14

Evidence for the accepted personal scope of
[`android_chatgpt_fresh_new_conversation_text_dispatch_v1`](../chatgpt-fresh-new-text-dispatch.md).
It does not complete project, temporary, tool, attachment or initial
composer-free first-send acceptance.

## Complete Native Trial On 1720

The prior controlled turn was first resolved without sending:
`fresh-new-1720-resume-readback-20260914-103258-121` passed in 17.6 seconds,
inspected one exact candidate, matched its user identity and performed zero sends.

`fresh-new-1720-full-acceptance-20260914-103420-120` then passed in 57.7 seconds
on the installed normal APK 1.1.1720, adapter 388. Its source was
`72efc568e2b83d87802d43adc4817e9f0162c7a9`; APK SHA-256 was
`eda8284dda6e72edbd2549f5439d987939a8b001031254595b9e57f820dd63ec`.

- Actual production native composer and Send button; no official Submit callback.
- Two native clicks: new personal first turn, then one same-conversation follow-up.
- No seed send, duplicate user message, duplicate native answer or uncertain write.
- Each case had one matching native answer, a private HTTP receipt, exact
  native/provider conversation agreement and terminal history reconciliation.
- The follow-up used the prior assistant parent; the final owner was
  `owned/ready`, phase `completed`, `pending=false`, with 21 stream events.
- Original conversation and awake setting restored; zero diagnostic timeouts.
- Cookie, login, proxy and media state unchanged. No microphone operation.

The observed first/follow-up replies were at 14,188/12,932 ms and cases completed
at 21,375/18,024 ms. These include semantic UI and MCP polling; they are not
network first-token timings or proof of lower temperature.

## Promotion Change

Transaction version 23 and adapter 389 enable only the accepted native first-send
path with a bound official composer. They reuse the existing preparation,
security-aware same-origin HTTP, owned stream decoder, history reconciliation,
native receipt and identity-based placeholder merger. No second sender is added.

`__elonChatGptFreshTextNewConversationsEnabled = false` disables this default.
Explicit `true` and the existing one-command trial retain their research scope.
The main private-text switch still applies. Independent project, tool, attachment
and temporary flags remain opt-in; the initial composer-free path is not promoted.
This remains page-local private HTTP with the WebView identity/runtime layer,
not an independent Android HTTP client or elimination of that layer.

`fresh-new-default-tests-fixed-20260914-104704-858` passed all 269 focused Node
tests, with no failures, cancellations or skips. Real-context personal first-send
and follow-up fixtures now omit the experimental flag. Scope tests cover narrow
default admission, explicit opt-out, composer absence and retained trial behavior.
The initial red test reproduced the disabled default; a test-only asynchronous
cleanup assertion was corrected before the final green run.

Release and an unarmed production-default check are pending at this source commit.
The prior accepted trial is not mislabeled as an unarmed default check.
