# 一龙 PC 节点配置（可选 —— 普通用户完全不需要这个文件）
#
# 现在的标准用法：直接双击「一龙PC节点.exe」→ 浏览器打开管理页 → 用账号密码登录一次即可。
# 节点会自动注册并把凭证记住，以后启动什么都不用配。
#
# 本文件只给「高级 / 服务器 / 免网页登录」场景使用：
#   把本文件复制为同目录的  node-agent.env  再按需填写。

# 免网页登录：填了 token，首次启动就自动注册（等价于网页登录一次）
# NODE_USER_TOKEN=

# 手动指定节点凭证（搭配使用可跳过自动注册；一般不用）
# NODE_AGENT_ID=
# NODE_AGENT_SECRET=

# 云端地址（默认即下方值，一般不用改）
# NODE_CLOUD_URL=ws://43.139.149.158:8080/agent/ws
# 本地 Ollama 地址
# NODE_OLLAMA_URL=http://localhost:11434
# 管理页端口（默认 7799）
# NODE_ADMIN_PORT=7799
# 直接双击内部 elon-node-agent.exe 时默认自动打开管理页；通过「一龙PC节点.exe」启动会自动关闭重复开页。
# NODE_AUTO_OPEN_ADMIN=true
# elon-node-agent.exe 的完整路径（默认同目录）
# NODE_AGENT_EXE=D:\elon\elon-node-agent.exe
# 客户端启动时自动检查并更新内部节点程序（默认开启）
# NODE_AGENT_AUTO_UPDATE=true
# 自动更新和下载使用的服务器 HTTP 地址（默认 http://43.139.149.158:8080）
# NODE_AGENT_UPDATE_BASE_URL=http://43.139.149.158:8080

# ── 本地 TTS Worker（可选，提供情绪女声合成）────────────────────────────────
# 改为 true 后，托盘启动时自动伴生拉起 TTS Worker；退出时一并停止。
# TTS_WORKER_ENABLED=false
#
# 引擎选择：index_tts2（情绪最强）或 cosyvoice3（低延迟）
# TTS_PROVIDER=index_tts2
#
# TTS Worker 端口（默认 5011）
# TTS_WORKER_PORT=5011
#
# Python 可执行（默认读仓库内 .runtime\tts-worker-model\venv\Scripts\python.exe）
# TTS_PYTHON_EXE=D:\tts-venv\Scripts\python.exe
#
# 声音素材目录（含 voices/*.wav 和 emotions/*.wav，默认 server/assets/tts）
# TTS_ASSET_ROOT=D:\tts-assets
#
# IndexTTS2 模型（Provider=index_tts2 时需要）
# TTS_INDEXTTS2_MODEL_DIR=D:\models\index-tts2
# TTS_INDEXTTS2_CFG_PATH=D:\models\index-tts2\config.yaml
#
# CosyVoice3（Provider=cosyvoice3 时需要）
# TTS_COSYVOICE_REPO_DIR=D:\repos\CosyVoice
# TTS_COSYVOICE_MODEL_DIR=D:\models\CosyVoice3-300M-SFT
