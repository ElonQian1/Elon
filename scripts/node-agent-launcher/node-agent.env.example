# 一龙 PC 节点配置（可选 —— 普通用户完全不需要这个文件）
#
# 现在的标准用法：直接双击「启动一龙节点.cmd」→ 浏览器打开管理页 → 用账号密码登录一次即可。
# 节点会自动注册并把凭证记住，以后启动什么都不用配。
#
# 本文件只给「高级 / 服务器 / 免网页登录」场景使用：
#   把本文件复制为同目录的  node-agent.env  再按需填写。

# 免网页登录：填了 token，首次启动就自动注册（等价于网页登录一次）
# NODE_USER_TOKEN=

# 手动指定节点凭证（搭配使用可跳过自动注册；一般不用）
# NODE_AGENT_ID=
# NODE_AGENT_SECRET=

# 云端地址（默认即下方值，一般不用改）
# NODE_CLOUD_URL=ws://43.139.149.158:8080/agent/ws
# 本地 Ollama 地址
# NODE_OLLAMA_URL=http://localhost:11434
# 管理页端口（默认 7799）
# NODE_ADMIN_PORT=7799
# elon-node-agent.exe 的完整路径（默认同目录）
# NODE_AGENT_EXE=D:\elon\elon-node-agent.exe
