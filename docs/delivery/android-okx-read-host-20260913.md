---
version_status: current
reviewed_at: 2026-09-13
implementation_status: in_progress
owner: quant-grid
---

# 欧易手机只读授权服务交付

对应[正式需求](../requirements/android-okx-read-host-v1.md)。本批已实现主 APK 凭据输入与持续读取同意、AndroidKeyStore AES-GCM 本机保管、固定 Provider、实际账号核验以及合约网格列表/详情读取。量化界面、创建和管理不迁入主 APK。

## 当前证据

- 新增 18 项离线回归通过；连同既有币安托管和正式量化调用方边界，74 项测试通过。覆盖权限仅 Read、实际主/子账户、换号和迟到结果、撤销、短页继续、重复页/后续失败/超限不返回部分成功、共同规范化样本、HMAC 签名及密文篡改/错误密钥/不同 owner 或环境拒绝。
- 量化侧严格解析同一 `grid-center.v1` 测试样本，默认币安入口继续拒绝欧易源。共享样本还与原 Rust 欧易转换结果相等。
- 全球两个官方公开 time 地址可返回成功；这只证明公开地址连通，不证明实际 API 账号可用。服务固定使用全球正式域名，不重定向，不写交易。
- UI 工作台已登记授权表单；两个模拟器资源均被占用，没有创建新的渲染任务。真实视觉、AndroidKeyStore 在用户设备上的业务保存/恢复，以及真实 API 账户仍待验收。
- 主 APK **1.1.1710 / 1710** 经官方发布入口完成服务器版本、摘要与大小核验；源码 `72308a5f0f43ba2e7ae590fbe76442ab9c2c673d`，APK SHA-256 `899dc9a1ad84b27db8ecd79cbbb2bc28172a39a4e442b3a4d53fb951d7bd74a2`。
- 配套量化 **0.7.52 / 69**，源码 `c83d92af9d0767900697bc06f5ecbaf878a6377b`，APK SHA-256 `51049e4b2f7860bf9923d13f895a24935eed327852d8406035889b1db2d52cc8`；子项目发布回执 `rel_c0e22bfaa7ac4710aea79fa9f8c181eb` 身份和服务器返回核验通过。
- 2026-09-13 21:39（北京时间）两包均通过无线 `adb install -r` 原签名安装并核对设备版本。量化 MCP 实测交易所切换、欧易未授权状态以及返回币安恢复原账户 1 个新鲜网格；通过正常入口打开 `OkxConnectActivity`，有界检查三项输入标签、验证及取消按钮，取消后回到量化。未填写真实凭据或批准授权，不能据此宣称已验证欧易私人接口或设备上的密文保存。
- 工件组 `grid-okx-mobile-read-v1` 保存双包、发布/安装回执与页面结构证明。没有实际视觉帧；UI completion 因 `DEBUG_RUNTIME_NOT_CONNECTED` 未通过，资源占用期间未另建渲染任务，视觉记 `VERIFICATION_DEFERRED`。云端 CI 因 GitHub 账单/额度未启动，本地验证通过。主发布后的自动 worktree 回收报告 `Branch` 属性缺失，业务发布已成功，该本机清理异常另报。

## 仍需完成

双包已安装。用户从量化“合约网格 → 顶部交易所 → 欧易”进入，在主 APK 填写仅 Read 的 API Key、Secret、Passphrase，核验后同意返回。AI 随后读取列表、详情、账户类型与恢复结果；测试空账号时应显示已连接但无网格。撤销应删除本机授权，旧代次不得读取。资料不在聊天或证据文件中传递。

当前不含欧易创建、交易修改、结束、历史/成交/持仓和私有推送。主服务不会把未核验收益映射为 USDT；超出请求/响应边界时明确失败。网格工具整体 Goal 保持进行中。
