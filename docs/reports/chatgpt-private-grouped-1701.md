# ChatGPT 私有能力集中验收包 1701

2026-09-13。集中包已发布、无损安装；新增范围的生产 UI 验收尚未进行。

## 发布证据

- APK `1.1.1701` / code `1701`，源码 `f2f508a0cdc5d5ba9b2bcc02a270f1824584dce3`。
- SHA-256：`36b34b6d6164ebcb1d441ddedea2cce2e0ead319e2b6ae139e6ffe1070a9a6bc`。
- 标准发布日志 `webchat-private-grouped-release-20260913-150958-219`：
  `passed / exit 0`，536.5 秒；Release 构建成功，远端 APK 大小和摘要核对通过。
- 白名单小米自动无损安装成功，随后独立读取已安装包确认 `1.1.1701`。
  未清 Cookie、应用数据或登录状态，未修改独立代理。
- 首次命令包装因把 PowerShell 的 `& ./script` 交给 cmd 而立即退出，未启动构建；
  改用显式隐藏 `pwsh -File` 后只执行上述一次实际构建。不能把首次失败算作通过。
- 后续线上版本已推进到另一任务的 `1.1.1702`，源码 `45d3826b0`；已验证是本包源码后代。
  本轮不追逐重打包、不覆盖更新发布；手机回读仍为 `1701`。

## 能力边界

| 能力 | 当前状态 |
|---|---|
| 代码块本机编辑/导出 | 已在 1688 真机验收，复用，不重复测试 |
| 普通 Writing Block 本机编辑/导出及官网保存 | 已在 1692 真机验收，复用 |
| 项目自有会话 Writing Block 保存 | 已实现、离线验证、本包已安装；生产 UI 待验 |
| typed writing widget 读取与保存 | 已实现、离线验证、本包已安装；生产 UI 待验 |
| 独立文字请求扩展及独立重新生成 | 候选代码已打入本包；不继承旧链路验收结论 |

项目与 typed widget 的具体协议、测试和未开放范围分别见
[项目写回](../chatgpt-writing-blocks-project-save.md)、
[typed widget](../chatgpt-writing-blocks-widget-save.md)。
普通写作块完成证据见[原生写作块](../chatgpt-writing-blocks-native.md)。

## 验收准备

手机已在线，但本轮两次有限状态读取均为锁屏，未打开用户会话或发起云写入。
已请求解锁；不绕过锁屏。不能把安装成功称为用户可用验证。

重新生成脚本新增显式 `-FreshHttp -NativeRetry`，沿用真实原生按钮，联合核对
恰好一次独立请求、已归属流事件、原用户分支、正文回显、未发送草稿和历史对账；
不以旧 runtime 的成功回执冒充独立 HTTP。测试中断时只清自己的固定测试草稿，
pending、未知结果、用户编辑或切换页面均阻止清稿和导航。定向脚本检查已通过：
16 个拒绝证据、3 个原生身份、2 个草稿、9 个恢复用例；旧脚本契约、18 个文档连续性
和 13 个原生准入用例亦通过。这些是离线检查，不是真机重试成功。

后续解锁后复用本包与受控样本，优先验收项目/typed 写作块编辑、导出、单次保存和
重开一致，再验独立重试候选。不重复 1688/1692 已通过范围，不重放未确认写入。
本轮未测语音、Google 或发热；完整 Goal 仍有效。
