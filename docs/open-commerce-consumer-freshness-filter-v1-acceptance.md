# 开放商业消费者声明期筛选 V1 验收

## 当前状态

`verified_rust_sqlite`

## 已验证

- 默认关闭的请求字段及稳定响应枚举。
- 能力候选层的 `current` 精确筛选。
- 匹配原因和结果筛选标志。
- 请求指纹与排序凭证中的筛选条件。
- 字段省略和默认关闭的兼容性，以及显式开启后的 `current/stale/unknown` 精确选择。
- 同商户多能力、指定能力键、五种排序器和来源筛选的组合行为。
- 开关进入输入摘要和凭证固定枚举，切换后指纹变化。
- 发现读取不产生业务写入。

## 验证证据

- 候选范围、声明期、排序与凭证 SQLite 矩阵 5 项：`d352d3b0f8bc0d99b1740590a2ed0106f8cc47daf4aed5dd41fb9a2d6b8af445`。
- 可信来源与声明期组合回归：`ee12abfcf0a8df813884fc7983dc52b835062827ff55dcc922d73f67430eb920`。
- 消费者发现 MCP 回归：`bc677b1406510b3c3bd1942a3d00119c9ac59eb636dfed34ec5cc2bf39a3ead5`。

## 未完成

- HTTP 实例、PC 勾选、结果标志、空结果和窄屏布局验证。
- 基于真实外部平台回读的来源真实性验证。
- 商户过期提醒、自动暂停、消费者自定义时间阈值和联邦策略。
