---
title: 分布式算力当前实现状态
status: current
reviewed_at: 2026-08-31
owners: backend, pc, node-agent, ai-economy
implementation_status: mixed
---

# 分布式算力当前实现状态

本文承接 `AI_CURRENT.md` 的分布式算力细节，只记录已实现边界、验证强度和仍缺生产闭环。领域合同与验收细节以各链接专题文档为准。

## 供给与市场控制面

- API Token 保管、远程节点、计算使用台账，以及默认关闭的节点模型算力共享策略已实现但边界有限。节点所有者可选择共享模型、最大并发和每日 Token 预算；指定与自动调度会在同一事务中检查今日实耗、活动租约预留和本次保守预留，所有者自用不受共享限制。可信终态先冻结实际 Token，再进入异步结算；提前断流、真正租约过期和服务重启会失败关闭并幂等释放预授权，最终状态不接受迟到覆盖。所有者控制面会从持久化记录派生近 24 小时失败、预留超出和过期租约健康快照。该快照不是 SLA 裁决或自动赔付，当前仍不是包含异构任务、竞价、故障赔付、真实提现或链上结算的开放算力市场。
- 联邦基础合同已定义 Provider、Workload、Offer、SKU、Price Snapshot、Job、Reservation、Attempt Lease、用量与结算回执，但尚未完整接线。旧 LLM `NodeComputeRun` 已接入认证 `/api/me/node-usage` 的附加只读兼容投影，只认 `server_node_llm/node_llm`，固定 `partial/provider_reported_unverified`。PC 账本的兼容观察增量未编译、未运行或做浏览器验证，不生成 Job、Lease、Receipt 或可信计量。共享 CapacityPool 已形成 v165-v168 Store 与追加式多 meter Claim/账本；Reservation Claim 强制绑定 Offer、Job 和 Reservation，公开 standalone 入口拒绝绕开 Broker。
- F0 核心历史因果引用 Carrier ABI 已冻结 `execution_source_v1` 与 `settlement_source_v1` 两种基础 profile，并以 endpoint-only additive `settlement_release_source_v1` 闭合 v198 available释放，以 `execution_verification_source_v1` 闭合 v188最终声明→v189候选→v190消费者审核→v191平台观测→v192 accepted Verification→v193 Execution Receipt及 rebuilt execution digest。四类 read都只按历史 Lease root解析，以 private scope证明 consumer/provider-owner权限；成功响应固定 exact 5 string keys。现有六GET/六MCP扩为八GET/八MCP，普通MCP继续project isolation；PC必须先验证verification→execution receipt/digest等式，再验证execution→settlement→release，pending记录不把release absent冒充integrity。四profile的Domain、Store、Service、HTTP/MCP、PC及source-contract/golden/negative源码均已落盘，严格`design_frozen/source_written/source_review_only/implementation_uncompiled/implementation_unrun`、`passed=0/failed=0`；migration/table=`none`，writer、native digest rewrite与state/money effect均为`0`，compiled/run=`0/0`，F0 gate=`not_met`。新验证链只证明retained source integrity，不把Provider声明或平台观测升级为可信计量。见[core authority](federation-historical-causal-reference-abi-authority.md)、[core acceptance](federation-historical-causal-reference-abi-acceptance.md)、[release authority](federation-settlement-release-causal-reference-abi-authority.md)、[release acceptance](federation-settlement-release-causal-reference-abi-acceptance.md)、[execution verification authority](federation-execution-verification-causal-reference-abi-authority.md)与[对应 acceptance](federation-execution-verification-causal-reference-abi-acceptance.md)。
- v192 native retained read 已冻结并形成 Store/private scope、participant/admin Service、原两条 GET、两条 lease-only MCP 与 PC exact-52/条件 14 等式源码。它只从 historical Lease root 重审 v188-v192，不依赖 v193，accepted/rejected/disputed 均可读；普通 MCP 保持 project isolation，participant 对 missing/integrity/nonparticipant 统一脱敏 404。该增量严格 `design_frozen/source_written/source_review_only/implementation_uncompiled/implementation_unrun`、`passed=0 failed=0`、compiled/run=`0/0`、F0=`not_met`，migration/table/writer/state/money effect=`0`；旧 v192 或 PC 证据不得外推，人工 conservative inputs 仍不可信。见 [authority](attempt-verification-retained-read-authority.md) 与 [acceptance](attempt-verification-retained-read-acceptance.md)。
- 本人 Provider、Pool、Bucket 和 Supply 控制面已通过 Rust/SQLite、进程内 HTTP/MCP 和 PC 静态构建专项，可完成双 meter 供给追加、撤回、幂等重放、显式确认、鉴权、本人隔离、失败原子性、账本审计及数据库重开。真实 TCP、PC 浏览器交互和发布仍未验证；self-declared/available 不等于硬件 verified、市场报价、可预留容量或收入。见 `supply-control-plane-acceptance.md` 与 `pc-compute-build-acceptance.md`。
- V279 UserNode Provider Binding Root 已冻结并写入私有 Domain/canonical/validator、37列表 migration、Store/API、activation gate 与 authority/acceptance 源码。固定 schema/JCS/SHA-256 和 ID/request/material/receipt 四域；binding ID 只由 Provider/node/genesis/install/endpoint installation identity 派生，receipt 记录创建时 endpoint credential 与显式 sharing consent/authorization source，全部 downstream effect=`none`。Durable source 是一张 immutable 37列表、0 view/0 revocation/0 mutable head；Store source 在每次消费时重证 current consent/credential，receipt 本身不是 authority。当前严格 `design_frozen/source_written/source_review_only/implementation_uncompiled/implementation_unrun`、`passed=0/failed=0`，未编译、测试、执行 migration/SQLite/runtime/network；不证明 `node_binding_ref` 已在生产写入口收紧，也不生成 Ready、route、Offer、Attempt、Lease 或经济效果。见 [`V279 authority`](user-node-provider-binding-authority.md) 与 [`acceptance`](user-node-provider-binding-acceptance.md)。
- UserNode Ready source-lineage 与 local-currentness 未登记草案均为 `source_written/source_review_only/implementation_uncompiled/implementation_unrun`、`passed=0/failed=0`。前者只从线性 work-admission、Ready-health 与明确 `Untrusted` 的 Host observation 投影 CPU-only 六键谱系；后者只在未来 handle-bound/query-only Deferred 快照内重审 exact head/chain、install/promotion、授权、process、inventory/policy、exact Ready record 与 TTL，并以 HRTB/private-field seal 禁止权威外逃。原 envelope 的 local currentness、runtime transition、Host runtime、v15 session 四 gap 仍为 `missing`；无 handle-bound producer、schema/writer 或 Ready/Provider/route/Offer/Attempt/Lease/经济效果。见 [lineage authority](user-node-ready-source-lineage-authority.md)、[local-currentness authority](user-node-ready-local-currentness-authority.md) 与 [acceptance](user-node-ready-local-currentness-acceptance.md)。
- 节点插件 A2 的历史 Windows families 为 `8/8 + 8/8 + 16/16 + 49/49 + 36/36`，A2b2=`117/117`、同提交宽回归=`266/266`；Map/Lock `StaticContract=43476/43476 + 8668/8668`。本批 source-only baseline `82d52bad8fd6e34c3fc7e82c5346430dcb26e9cf` 新增 Map exact 6-member lifecycle，未运行 inventory 预期为 Map `12 present + 43,464 missing`、Lock `114 present + 8,554 missing`，`passed=0 failed=0 actual=not_run`；reviewed digest、manifest、`Qmap/Qlock`、`WindowsDynamic` 与生产效果仍关闭。见 [dynamic quotient](node-plugin-vfs-map-lock-dynamic-quotient-authority.md)、[open-attempt authority](node-plugin-handle-bound-open-attempt-authority.md) 与 [A2 acceptance](node-plugin-vfs-fault-acceptance.md)。
- UserNode Windows Runner loader/process 是未登记、不可达的 source drafts：typed owner chain 已从 retained discovery 铺到 authenticated exact-context intent、package-only pre-lease PE、preliminary requests、私有 GrantReady wave-zero prefix、signed recursive-policy envelope/verification evidence、A0 PolicyCurrent、authenticated recursive policy-bound base acquisition、Ak canonical request/resolution plan V1、exact-vector currentness-pending→DispatchReady、producer-wave custody V3 receipts、empty frontier/final aggregate、post-lease recursive final projection、QueryVerified lineage与 process pre-create retained projection；authenticated policy binding为V2，receipt-set/chain保持V1、parse/closure保持V2、profile保持V3。API-set仍只允许一步 terminal，nested API-set DAG与 Shadow positive path fail-closed，projection不是 live process machine/WOW64 receipt。真实 selector/policy signature verifier/currentness backend、prelease/recursive parser、GrantReady或逐波 resolver、name/system grant/candidate/lease backend与 positive advancer、post-lease sealer/query、live-OS/launch-security/create/resume/recovery producers仍缺；Windows dynamic=`0`。严格 `source_written/source_review_only/implementation_uncompiled/implementation_unrun`、`passed=0/failed=0`、`migration/table/writer=none/none/none`、四 gap=`missing`、loader exact 18 effects=`none`；未编译、测试或运行 Win32。见 [launch-context authority](user-node-windows-runner-launch-context-selection-authority.md)、[recursive policy currentness authority](user-node-windows-runner-recursive-policy-currentness-authority.md)、[acceptance](user-node-windows-runner-recursive-policy-currentness-acceptance.md)、[recursive plan authority](user-node-windows-runner-recursive-wave-resolution-plan-authority.md)、[recursive acquisition authority](user-node-windows-runner-recursive-system-image-acquisition-custody-authority.md)、[recursive closure authority](user-node-windows-runner-recursive-system-image-closure-authority.md)、[launch discovery](user-node-windows-runner-launch-path-discovery-authority.md)、[loader](user-node-windows-runner-loader-load-set-authority.md) 与 [process](user-node-windows-runner-process-custody-authority.md) 专题。
- v177-v181、v203-v205 激活、隔离和恢复已通过 Store/Service、本人/管理员 HTTP/MCP、角色隔离、幂等及文件重开专项；PC `/compute-supply` 与 `/compute-activation` 已静态生产构建。并发压力、真实 TCP、浏览器和生产库副本仍未验证；流程不连接节点、不派发、不发布 Offer 或移动资金。见 `activation-control-plane-acceptance.md`。
- v170、v182-v184 Offer 已通过 Store/Service、本人和管理员 HTTP/MCP、角色隔离、幂等重放及文件重开专项；PC `/compute-supply` 与 `/compute-offers` 已静态生产构建。并发压力、真实 TCP、浏览器、生产库副本和发布仍未验证；流程不生成 Snapshot、不取消已有预留、不派发或移动资金。见 `offer-control-plane-acceptance.md`。
- v171 Snapshot 与 v172-v176 Broker 已通过 HTTP/MCP、并发和文件重开专项；v223/v224 reference fallback 又通过四眼 HTTP/MCP、原子 Snapshot、拒绝零副作用、旧 TTL 触发器升级和重开专项，固定 `fallback_curve/sample_count=0`。PC `/compute-reference-curves` 已通过跨层契约、严格类型、lint、生产构建和 bundle budget；真实 TCP、浏览器、生产库副本、真实价格、撮合、派发和结算仍缺。见 `compute-management-mcp-acceptance.md`、`platform-reference-price-curve-api-acceptance.md` 与 `broker-control-plane-acceptance.md`。
- v225/v228 维持既有结论。v234 server-owned worker 已通过完整服务端测试目标编译、fresh/repeat migration 及 7 项管理员/Store/HTTP、worker、公平扫描本地专项，可按持久 keyset checkpoint 恢复已行权但到期的 Reservation，每 tick 最多 100 项；真实并发 CAS、进程崩溃、历史库升级、真实 TCP 与生产周期仍未验证。它不是 admin actor，不新增经济权威，成功仍只退款、归还 child Claim 并结束 Job/Reservation。v238 CapacityInstrument 已通过完整服务端编译和全新文件数据库迁移冒烟，但专属 lifecycle/adoption、下游门卫和管理员 HTTP 行为仍以其专题验收为准。Retirement 不锁死历史退款或容量归还；这些能力都不代表真实市场、价格、计量、执行或结算。见 `delivery-allocation-acceptance.md` 与 `capacity-instrument-acceptance.md`。
- `capacity_future` pricing-mode 交付结算谱系 bridge 已从 caller-supplied projection 铺到未登记 Lease-rooted Store retained resolver source：单一 Deferred snapshot 自行重建 Instrument/activation/adoption/publication、Commitment、exercised Allocation、Price Snapshot、F0 execution/verification/settlement/release，并只产出 crate-visible、private-field、non-Clone/non-Serde seal。v192 verification-role 与 v195 settlement-role digest 分字段保留、分别复用原 owner audit 从同一 v193 readings 重算且禁止直接判相等；v225 common multiplier、meter 顺序/granularity 与 v228 whole transfer 也在历史 owner 边界重审。状态严格为 `unregistered/draft_frozen/store_resolver_source_written/source_review_only/implementation_uncompiled/implementation_unrun`、`passed=0/failed=0`；Service/API、migration/table/writer/state/capacity/money effect 接线均为 0，pending 分支也不能用 release absent 推导当前状态。见 [`authority`](capacity-future-settlement-lineage-authority.md) 与 [`acceptance`](capacity-future-settlement-lineage-acceptance.md)。

## Attempt 与外部 Adapter

- v200/v201 Provider 提款申请与唯一终态已通过同一组 Store/Service 本地专项，合计 `3 passed / 0 failed`，验证指纹为 `588ba2509b7d80bfc9a72d1f68af4e1aff5d78c164bbeb0ea238f1dba0ccf6d8`。专项覆盖确认门卫、available 到 withdrawn 的原子冻结、精确幂等重放、所有者隔离、冲突幂等键与余额不足时零部分写入、取消只返还一次、文件数据库重开，以及管理员 `external_paid_attested` 只保存外部证据且不移动余额。PC 静态生产构建已通过；真实 HTTP/TCP、浏览器、生产数据库、并发故障注入、外部支付和 Sui 结算仍未验证或未实现。合同边界见 [`settlement-withdrawal-request-api.md`](settlement-withdrawal-request-api.md) 与 [`settlement-withdrawal-terminal-api.md`](settlement-withdrawal-terminal-api.md)。
- Attempt v175-v215、v221/v222 管理面及 v227-v253 Adapter 前置门已形成分层权威，生产成功链仍未运行。V239 形成限时动态沙箱签名收据，V241/V242 固定凭据验证器实现与公钥，V243 形成短时凭据验证回执，V244 汇合为可撤销采用授权；V244 migration 定向测试及 2 项真实 HTTP 链路测试通过。V245 legacy 投影加固 3 项迁移测试通过；V246 从 V227 CAS 同一复验句柄按 V232 manifest 安全解包、原子发布内容寻址目录并登记 `installed_inert`，V247 追加安装撤销终态与 sealed current-authority seam，二者文件系统/迁移 10 项及安装/撤销 HTTP 3 项通过，并修复 Windows 深层命名空间的原子发布长路径失败。主线 V248 属于 ERP；V249 用同一 V247 sealed 文件树登记 Provider-neutral release 与每个 Provider 独立的 installed-inert companion，V251 原位修复凭据验证器 JSON 合法键序被误判为冲突的问题；registry/companion migration 6 项与 HTTP 4 项通过。独立的 V250 为 exact V249 release 增加可续签漏洞情报 re-attestation，已通过 migration 合同 4 项与进程内 Axum HTTP 5 项。V252 以新 ABI 建立 release 级 durable challenge、RSA verifier 签名、sequence/predecessor、currentness 与撤销的六能力沙箱 re-attestation，已通过 migration 合同 7 项与进程内 Axum HTTP 2 项，指纹为 `3dc01f491da5a4be4f111ff5c2de29f556710eaed60fc61093da10e968492fa6`。V253 曾为 exact V249 Provider binding 建立可续签、可撤销的 credential authority，并验证 `registering -> 紧邻 active` 窄桥和 active 续签；历史批次通过 migration/源码合同 5 项与进程内 Axum HTTP 3 项，指纹为 `8c9c1fe9f6c0df7c5e56cc280c171ff06ea3dfd2498b27e4a1ba6a791f71c4c5`。这 `8 passed` 现仅为 historical/superseded evidence；V274 后 pre-V277 V253 currentness严格为 `registering-only`，本批 Rust/DDL/HTTP 收窄为 `source_review_only/implementation_uncompiled/implementation_unrun`、`passed=0/failed=0`。V250/V252/V253 只验证签名声明，不运行扫描、真实沙箱或 secret 解析；V253 不接收 Prepared、不重验 live-FS，settlement account 仅为历史观测而非 stable subject。生产库原位升级/重开/并发/崩溃验收、同事务 Provider activation/service actor/v213 compatibility/route、生产 Adapter、Runner、派发、计量结算、生产 TCP/部署/MCP/PC 仍缺。见 `external-pool-adapter-credential-reattestation-authority.md` 及 V249/V250/V252 专题文档。
- 算力 Attempt PC 的 12 个角色路由已通过跨层合同、严格类型、lint、生产构建和 bundle budget；Gateway 未接线，人工 Start、Renew、no-start Abort 仍关闭。v226 final-usage fence 已通过 5 项 Rust/SQLite 动态测试，覆盖新库迁移、精确重放、候选后封口、双连接竞争、legacy 漂移拒绝和下游审核失败关闭；它不新增账本或经济效果。真实 Gateway/Adapter、HTTP/TCP/浏览器、生产库原位升级和发布仍未验收。见 `pc-compute-attempt-workbenches-acceptance.md` 与 `attempt-final-usage-fence-authority.md`。
- Attempt v175-v215、v221/v222 管理面及 v227-v254 Adapter 前置门已形成分层权威，生产成功链仍未运行。V249-V253 的 registry、live-FS seam、漏洞/沙箱/credential currentness 保持原验证强度；V254 只新增 owner delegation 与 inert static compatibility candidate，dynamic preflight 在同一 `checked_at` 消费 V249 Prepared + current V250/V252/V253。V254 不激活 Provider、不创建 v213 authority/route、Pool、Offer、Job、Attempt、Start、usage 或 settlement；结果固定 closure 未实现、`activation_ready=false`。V254 已通过 fresh/repeat migration/源码合同 8 项与进程内 Axum HTTP 2 项，验证 migration 幂等、persistence ABI、投影、根/lineage/fence/precheck 合同，以及 owner/admin 鉴权、创建/重放、currentness、dynamic preflight、脱敏、零经济副作用和撤销失败关闭；证据指纹为 `806e156cb21d15700a304cf6d0843a793ef4a1c97c623af85dca985d0f4c000b`。它还为 `external_pool` CapacityPool active 与 Offer draft/active 安装覆盖 insert/update/version/direct SQL 的 temporary absolute deny。V253→V254 文件升级、文件重开、并发/崩溃、生产 secret resolver、Sidecar/transport、Runner/ACK、atomic activation、用完整 readiness/currentness admission gate 替换 absolute deny、真实 TCP/生产数据库/部署/MCP/PC 仍缺。见 `external-pool-provider-activation-candidate-authority.md` 与对应 acceptance。
- V255 在 V254 之上追加 Provider-specific、server-fixed inert runtime launch profile authority：fresh create/currentness 重审 V249 sealed installation tree 并消费 current V249/V254 与本机 45 字段 policy root；exact replay 只重审历史 V254、历史安装根和 Prepared 文件，撤销及撤销重放不依赖后来 upstream/policy/FS currentness。profile 固定 `launch_profile_current_inert`、`runtime_launch_ready=false`，不启动 entrypoint/Sidecar，不读取或解析 secret，不调用 resolver，不激活 Provider，不创建 v213 authority/route、Pool、Offer、Job、Attempt、Start、usage 或 settlement，也不消费 V250/V252/V253 短 TTL evidence。V254 的 18 个 temporary absolute deny 保持原样。V255 已通过 fresh/repeat migration、Store/source contract 与 owner/admin 进程内 Axum 定向测试，合计 `12 passed / 0 failed`，验证指纹为 `e6919db4d7535bae1e8fc4017e1c7e829a3ad0ce23407e3c29c636a5557c0575`；生产 resolver、Sidecar/transport、probe/Runner/ACK、runtime readiness、atomic activation、完整 market admission gate、生产 TCP/数据库/部署/MCP/PC 仍缺。见 `external-pool-adapter-runtime-launch-profile-authority.md` 与对应 acceptance。

- V256 不再追加 durable receipt，而是实现 server-only ephemeral operator-mounted bundle resolver。固定 path 只由 exact profile digest 派生；strict manifest 同一 `checked_at` 绑定 current V255 与 Store 内部选择的 current V253 receipt/material/expiry，config/credential 只在 retained handles 和 locked、zeroize-on-drop memory 中短时存在。V256 已通过完整 `elon-server` 目标编译和 14 项定向测试，覆盖 8 项 manifest/摘要/locked-memory 行为及 6 项 source contract；错误的 Windows 常量导入和 V257 聚合辅助函数借用生命周期已修复，指纹为 `fd22ac143bc522669a3b224a4375fdc40e748d26d69bbb3330d1b408f3860618`。Windows protected-DACL 仍固定 `UnsafeCustody`，Linux syscall、生产 mount/secret 和 Drop 后 zeroization 取证未验收。数据库 schema 保持 V255，无 migration/receipt/API/process/network/probe/activation/market/settlement；Provider 继续 `registering`、`runtime_launch_ready=false`，V254 18 deny 原样保留。见 V256 authority 与 acceptance。

- V257 source capsule 仍只从 fresh V249 Prepared retained entrypoint 逐字 materialize 到 exact `0500`、四项 seals 的匿名 memfd；其旧 WSL2 `11 passed / 0 failed` 与指纹 `2ac6c80f0d9c83f193090535c454851f3055fd835e3217dd3ca43bda79a35ebd` 保留为历史证据。V267 保持该 policy V1/root 不变，另派生 sealed launch image，并让 Store session root 使用 launch SHA-256、profile/installation 继续使用 source SHA-256。当前 V262/V263/V265 fixture 已改用 production source→derived-launch materializer；重放后 WSL2 `v267_` 为 `12 passed / 0 failed`，Yama 2 内核矩阵为 `12 passed / 0 failed`。这只证明受控 fixture，不代表真实第三方 ELF 或生产就绪。Provider 保持 `registering`，V254 18 deny 原样保留。见 V257 与 V267 authority/acceptance。

- V258 在 exact V255 profile 下追加 Provider-specific durable upstream transport target authority，补齐此前 onboarding/bundle/capsule 缺失的 canonical DNS hostname、TCP port、expected TLS leaf SPKI pin 与 server-fixed broker transport policy root。owner/admin policy/create/currentness/revoke 同形；Store 单事务重验 V249/V254/V255、Provider、Prepared、policy、linear predecessor 与 actor-bound idempotency，公开 summary 递归隐藏 hostname/port/SNI/SPKI/actor/idempotency/confirmation/receipt JSON。完整 `elon-server` 测试目标已编译，7 项 migration/Store/ABI 合同、2 项源码边界合同和 2 项 owner/admin 进程内 Axum HTTP 全部通过，合计 `11 passed / 0 failed`，指纹为 `bb3faae9295d682d573a5bc5a1d608be711a18c021be273120bf5181b1312aac`。V264 已消费该 target 建立 server-owned authenticated TLS channel，但 V258 durable authority 自身仍不做网络或 probe，不生成 route/service actor 或 activation；四项 readiness 固定 false，Provider 保持 `registering`，V254 18 deny 逐字保留。V257→V258 文件升级、重复 migration、重开、并发/崩溃、真实生产 TCP 和生产数据库仍未验收。见 `external-pool-adapter-upstream-transport-target-authority.md` 与对应 acceptance。
- V259 durable supervisor/session companion 的历史 V1 migration/Store/HTTP `13 passed / 0 failed` 与指纹 `af27aff6c90e44409ee3da8d7fbc5a32dd2910766a962c2cbc1cc08ab5eda17f` 保留；九项 effect 仍为 none、七项 readiness 仍 false。V267 把 fresh current catalog 升为 `external_pool_adapter_supervisor_session_policy_v2` revision 2，historical V1 exact validator 不变，migration 只重装 current policy projection。当前 WSL2 `v267_` 的 12 项中已动态覆盖 fresh/repeat migration、历史 V1 回读和 current V2；V266 Profile V1 继续使用冻结 V1 catalog。Provider 保持 `registering`，V254 18 deny 逐字保留。
- V260 的旧 `5 passed / 0 failed` 仍为历史证据。V267 在固定 receive allocation 上新增 `MSG_TRUNC|MSG_CTRUNC` 与 nonzero ancillary fail-close；当前 session-core 已动态验证 plain seqpacket、oversize、`SCM_RIGHTS` 和 kernel credentials，`4 passed / 0 failed`。显式 `MSG_CTRUNC`、未知 ancillary 与完整 terminal 矩阵仍缺。既有 HKDF/HMAC、sequence 与 terminal 合同不授予 process/network/activation authority。
- V261 的旧 `3 passed / 0 failed` 仍为历史构建。当前 V267 已编译，并在 Yama 2 的 WSL2 内核矩阵中验证 post-exec SET/GET stub、受限 `prctl`、execveat/seccomp negative shape、clone3/cgroup/namespaces、pidfd reap 与 cleanup；12 项全部通过。四项双资源 cleanup fault case 也通过。Yama 3、全部 host-gate 负值、长期/并发 child 与生产监控仍未验证。
- V262 exec 后 authenticated runtime 的旧指纹只保留 provenance。当前 production materializer 已接入 fixture，并在 V267 内核矩阵中通过 mutual bootstrap、root drift、seccomp negative、terminal cleanup 和 kernel isolation。结果只适用于本机受控 fixture/Yama 2，不等于第三方 Adapter 或生产 kernel 已验收。
- V263 Secret delivery 的旧指纹仍不证明生产 Secret 安全。当前 V267 Store session root 绑定 launch SHA-256，私有 binding 保留 source/launch digest 与 launch size；测试 Secret delivery、root drift、shutdown zeroization 已在当前内核矩阵通过。生产 Secret resolver、真实凭据和取证仍未验证。
- V264 新增 server-owned broker TLS transport core：从 current V258 authority 冻结 exact target 和完整 installation binding，在事务外执行 fresh DNS、公网单播全答案门卫、选定地址直连 TCP、TLS 1.3-only、WebPKI hostname/time/chain 和 exact leaf SPKI pin，再用第二份 Prepared handle 在 `Immediate` 事务中复验 target 与完整 binding。生产 Rust check 通过，本地 TLS 1.3/1.2 及源码合同 `11 passed / 0 failed`，最终指纹为 `acc1163dd5eb4a994eda3c77f975872890aa715b53841e25d921868fab0a24cd`。它没有应用层读写方法，只连接 loopback fixture；未组合 V263 Secret、未访问生产 upstream、未生成 probe/route/activation/usage/settlement，不开放 HTTP/MCP/PC/APK，也未部署。Provider 保持 `registering`，V254 18 deny 不变。见 `external-pool-adapter-broker-tls-transport-authority.md` 与对应 acceptance。
- V265 ELNW/loopback TLS 的旧指纹 `1e6d35c1d91d7dc6cdbf1e53c31486ab78001ebb768466f8d90356d575116984` 保留为历史 provenance。当前 V267 内核矩阵已通过 authenticated no-work fixture、shutdown/reap 与 cleanup，但没有重跑生产形态的完整 TLS/upstream/Secret 组合，也没有 durable observation、route、activation、usage 或 settlement。
- V266 Linux x86_64 machine Profile 的摘要 `a63d30b6f2f75c78c156ddb9ea609312f8b9b6726f403fedb960ed8a754fa047` 与 `6 passed / 0 failed` 继续绑定冻结 supervisor/session V1 catalog。V267 不改写它；V268 是唯一的 Profile V2 authority。V268 已完成 24/24 本地源码合同和迁移验收，但 Profile 正向 runner/signature 仍未运行；V269 已完成配置/请求与源码合同 23/23，真实 startup、HTTP、SQLite、Linux child 与 signer 仍为 `passed=0/failed=0/unrun`。
- V267 post-exec supervisor hardening 已从源码阶段推进为 `implementation_compiled / local_windows_and_wsl2_kernel_verified`：rebase 后 Windows 受管 check 指纹 `31f20bef1febf422e8fe4abc159c06b3a9985f77e563486b9b841cde2d0ca7ab`，受管 `v267_` 指纹 `6578adda332eb0404e25b8839a1ea4519e251506d0158a525f5a836f3859f62f`；当前 WSL2 `v267_` 12/12、Yama 2 内核矩阵 12/12、transport 4/4，四项 lifecycle 目标 case 通过。内核脚本恢复 `Yama=1`、根 subtree 为空且无遗留测试 cgroup。仍未验证 Yama 3/完整负值矩阵、第三方 ELF、生产 Secret/upstream、长时/并发/OOM/重启/监控及经济链。Provider 保持 `registering`，V254 18 deny 原样保留。见 V267 authority 与 acceptance。
- V268 追加 Provider-neutral signed runtime compatibility：独立 Profile V2 绑定 current V267 launch、V259 V2、V257 source policy、server-fixed runner 与四个 release-declared public fixture；append-only challenge、Store-private observation、exact V237 signed receipt、revocation/currentness 形成 release lineage。统一定向验收 24/24 覆盖 22 项源码合同和 2 项 fresh/repeat/reopen 动态迁移，验证精确四表一 view、零 durable row、UDF 畸形输入失败关闭、V273 恢复零 eligible row 与 18 deny 不变，指纹为 `9924f3425a1b5e0449fb291634a38fd0d0d334fd0f9fcde9ccb0467e0c0a8430`；状态为 `implementation_partially_verified/targeted_local_source_contracts_and_migration_verified`。HTTP、正向 RSA signature、SQLite 正向 lineage/concurrency 和 Linux runner 未运行。它不授予 production readiness，Provider 仍 `registering`，V254 18 deny 不变。
- V269 只接一条默认关闭的 platform-admin signing-handoff POST，以启动托管 delegated cgroup FD 同步调用 V268 private runner，并返回最小 signer payload；没有 server 私钥、自动 signer transport、worker/outbox/job/lease/retry、新表或 migration。当前为 `implementation_partially_verified / targeted_local_configuration_and_source_contracts_verified`：配置/请求与源码合同 `23 passed / 0 failed`，指纹 `f245567d18e5ef2c6f521d3f13a486a6b255aebac9c1039794d3294386b82675`；真实 startup、HTTP、SQLite、Linux child 与 signer 仍为 `passed=0/failed=0/unrun`。它不是 Provider-specific readiness、atomic activation 或 market admission。
- V270 Provider-specific post-cleanup readiness 使用独立 startup gate、process HMAC/custody epoch、六份 late-bound Prepared 和 cleanup 后 final `BEGIN IMMEDIATE`，最长 15 秒且不放大 V250/V252/V253/V268 authority。Windows 受管 `provider_runtime_readiness` 17/17 与 custody 3/3 已覆盖静态合同、fresh/repeat/reopen/integrity、process epoch/seal/TTL 子集，状态为 `implementation_partially_verified/targeted_local_verified`。HTTP/Linux production fixture、真实 Secret/upstream、并发和故障矩阵仍未运行；Provider=`registering`、`activation_ready=false`、九项 effect=`none`、V254 18 deny 不变。见 V270 authority 与 acceptance。
- V271 只替换 v213 source trigger 的 external-pool branch，把 V221 logical Adapter 经 exact V249/V254 lineage 映射到唯一 projection ID，不新增 table/view/index/UDF/API/runtime。Windows 受管 `v271_` 6/6 已覆盖 fresh/repeat/reopen、缺 fence/历史 route 失败关闭、唯一 projection 正向和 8 类跨根漂移，状态为 `implementation_partially_verified/local_migration_and_source_bridge_verified`。完整 current fence 写入、legacy branch 动态、并发、崩溃和旧库升级仍未运行；打开 fence=`0`。见 V271 authority 与 acceptance。
- V272 冻结 Provider-neutral ELTP v1 controlled conformance：独立默认关闭 startup gate、14-root/8-exchange、2 张 append-only table、1 个诊断 view、同进程 HMAC 与 3 条 admin route。统一 `task_protocol_conformance` 21/21 包含 18 项源码合同、2 项 Windows SQLite 和 1 项真实 Axum 门卫；动态覆盖 fresh/repeat/reopen、exact 2 表 1 view、18 个 V254 fence 不变、完整性 UDF，以及 401/403/422 先于 runtime 503，指纹为 `d08956fe46177ab11ea9038ce3306eff9e30e7fb09d5c07673e4dd12412ad45b`。WSL2 GNU server target direct stateful-oracle 3/3 覆盖八步状态、乱序/重复拒绝及 receipt 后置状态门，指纹为 `9f00ae268b6a4f52511884ec8943c1ffaab4630d808981e7b8a89020dba73019`；authenticated in-process session/wire 1/1 进一步覆盖真实 seqpacket 会话启动、双向认证、临时 bundle 交付、8 次 ELTP request/response/receipt、独立 child fixture oracle 与认证关闭，最终重跑指纹为 `fb4d9f5c115070d1149c55dc37324eb2e46742ed0223ead5477c28605850c5cf`。状态为 `implementation_partially_verified/targeted_local_contract_migration_http_wsl2_oracle_and_session_wire_verified`。独立 Linux child process 启动及 cgroup/pidfd/seccomp/scratch 清理、process HMAC、成功 HTTP 写链、并发与故障矩阵仍未运行。它不复用 V270 gate/epoch，也不授予 v213 或生产 authority，全部 readiness=false、effect=`none`。V273 worker+authenticated ingress、V274 active refresh、V277 stable executor+atomic activation 继续 NO-GO。见 V272 authority 与 acceptance。
- V273 authority/acceptance 冻结 default-off dormant production transport/ingress kernel，而不铸造 v213 authority：唯一 env 为 `ELON_EXTERNAL_POOL_ADAPTER_ATTEMPT_DELIVERY_ENABLED`，`true` startup 只要求 Linux x86_64 与 V270/V272 runtime/custody available；Store-private current authority按每个 candidate/attempt 同 connection/checked-at 重取。production ELSP 固定八项 roots/argv 并复用 ELTP v1；durable shape 为四张完全 immutable attempt/receipt/batch/event 表加两张 immutable-intent/narrow-CAS poll 表，无 public API、v213 constructor、stable executor、route、Provider 或 market effect。完整 WSL2 GNU `elon-server` 测试目标已编译；统一 21/21 包含 18 项源码合同和 3 项动态迁移，覆盖 fresh/repeat/reopen、exact 六张空表、零 view、schema/UDF 失败关闭、恢复入口持续 `eligible_rows=0` 与 18 deny SQL 不变，规范化指纹为 `8f4bd0e305416b13ffba92bbd3e576bb20c56c7ff4465215b32a6318db7d58cc`。当前状态为 `implementation_partially_verified/targeted_local_source_contracts_and_migration_verified/production_runtime_unrun`，production runtime `passed=0/failed=0`；startup/Linux child/ELTP/network/crash/concurrency、正向 ledger/trigger/CAS 与 ingress 未运行。Provider=`registering`、打开 fence=0；V278 private source已写但未编译/运行，且#13-#18仍deny，因此仍`eligible_rows=0`，正向ACK/Lease/Runner后移。见 V273 authority 与 acceptance。
- V274 在本次 V277 耦合冻结前的 dormant overlay 统一定向验收为 `7 passed / 0 failed`，指纹为 `9c363ccc6271005b6154d6ae230a34ed2da97b8335c130e9d67998d7632c9ffe`；该证据继续作为 historical provenance，但对双时间、V274→V277 immediate witness 与重建后的当前 ABI 属于 superseded evidence，不能外推为本批已编译、已迁移或已运行。
- V274 authority/acceptance冻结activation-rooted active-successor overlay：stable root绑定V249/V254/V255/V258/V259 structural pairs、exact typed Provider logical→projection adjacent-active target与V273 lane/carrier policy，并排除renewable evidence、Secret/session、executor/route/fence。durable shape仍为两张immutable receipt/revocation表+一个非权威诊断view；V277前零row。双时间正式分为preflight `activation_target_updated_at`与final `evidence_checked_at`，zero-row rebuild可重命名旧`checked_at`并固定`created_at=evidence_checked_at`。V274 receipt以immediate FK单向引用V277 receipt UNIQUE witness/root三元组；V277 canonical/DDL不含V274 receipt identity。planned genesis real-I/O与private append/current源码已写入，但仓库没有public Store orchestrator；durable restart/refresh因缺purpose-specific active preparation继续失败关闭。当前严格`design_frozen/source_written/source_review_only/implementation_uncompiled/implementation_unrun`、`passed=0/failed=0`，Provider=`registering`、`eligible_rows=0`。见V274 authority/acceptance。
- V277 authority/acceptance已有source-written private kernel：durable namespace exact只有`compute_external_pool_adapter_atomic_activation_receipts`一张immutable历史表，0 view、0 revocation，receipt永不单独current。stable executor domains为`ELON-EXTERNAL-POOL-STABLE-EXECUTOR-ID-V1`/`...-BINDING-V1`，projected adapter digest复用v211 `ELON-COMPUTE-ATTEMPT-ADAPTER-BINDING-V1`既有8字段shape；V271 logical digest仅lineage。private transaction leaf按exact `16 INSERT + 1 CAS UPDATE=17`执行，并用non-deterministic pending-plan UDF只放行V254 #1/#5-#12九项，#2-#4/#13-#18九项继续absolute deny；同时闭合active Provider/route、V277 receipt与V274 genesis，但没有root re-export或public Store orchestrator，也仍不连接V273，`eligible_rows=0`。active V253与restart fresh V274 successor相关源码已写，durable refresh运行入口仍失败关闭；route renewal/reachability属于V278。当前严格`design_frozen/source_written/source_review_only/implementation_uncompiled/implementation_unrun`、`passed=0/failed=0`，本批未编译，且无migration/SQLite/runtime执行证据，Provider仍`registering`。见[`V277 authority`](external-pool-adapter-stable-executor-atomic-activation-authority.md)与[`acceptance`](external-pool-adapter-stable-executor-atomic-activation-acceptance.md)。
- V278 route renewal/reachability authority/acceptance已有source-written private kernel：exact一张77列`compute_external_pool_adapter_route_renewal_receipts` immutable历史表、0 view、0 revocation；每行引用同root exact V274 sequence-1 genesis历史pair且不加UNIQUE、不要求current V274。四UDF分隔route renewal、V273 reachability/ingress与V274 refresh custody；renewal为11 INSERT+1 credential-root CAS，outbound为2 INSERT+1 outbox CAS。V254仅#8-#12接受V277或V278，#1/#5-#7仍V277-only，#2-#4/#13-#18 absolute deny。当前严格`design_frozen/source_written/source_review_only/implementation_uncompiled/implementation_unrun`、`passed=0/failed=0`；未编译、测试、执行migration/SQLite/runtime/network，sealed semantic ingress也无production validator/Runner caller，Provider=`registering`、`eligible_rows=0`。normal Offer→Job→v213 source及真实ACK/Lease/Runner后移独立service-managed admission+Runner bridge。见[`V278 authority`](external-pool-adapter-route-renewal-reachability-authority.md)与[`acceptance`](external-pool-adapter-route-renewal-reachability-acceptance.md)。
- V280 已冻结 external-pool service-managed admission→Runner纵切、market-profile及其approval evidence、planned 77列admission receipt、market projection identity、Plan-derived fence/Gateway/session/validator内部ABI，以及external Adapter semantic wire registry/selection/approval-evidence元ABI；initial market/wire evidence与inventory仍`unselected`，两类current authority不可构造。整体继续`implementation_unwired/uncompiled/unrun`、`passed=0/failed=0`；全局migration最高V280且物理280已由ERP占用，service-managed admission物理migration仍absent，未来next-free当前至少V281；其table/Domain/DDL/Store/UDF/trigger/Gateway/validator/Runner均absent，#13-#18 opened=`0`，Provider=`registering`、`eligible_rows=0`。Fixture与ABI-only不能升格。见[`V280 vertical`](external-pool-service-managed-admission-runner-authority.md)、[`profile`](external-pool-service-managed-market-profile-authority.md)、[`approval evidence`](external-pool-service-managed-market-profile-approval-evidence-abi-authority.md)、[`admission`](external-pool-service-managed-admission-receipt-abi-authority.md)、[`projection`](external-pool-service-managed-market-projection-identity-abi-authority.md)、[`Gateway/session`](external-pool-service-managed-gateway-session-validator-abi-authority.md)、[`semantic wire registry`](external-pool-adapter-production-semantic-wire-profile-registry-abi-authority.md)与对应acceptance。

## 节点插件与端点会话

- 节点插件 A2 的既有 family 已有部分真实验证，但本批 A2b1 Map/Lock lifecycle 仍严格停留在 source-only：前序 JointClose baseline `e3663e109039f38477de4d6ab5cd57483dbd0541` 曾在 evidence commit `bfa1a1180d220e9a4c8e39251414fc9a1b0a9ace` 通过 targeted guard `4/4`（fingerprint `f65d234bfb21a0351862d396cc4f1a9c4030d872d68fc571ad57a14529f1e3d7`），但该历史回执只证明 legacy subset、incomplete scaffold、owner graph 与 Map template ledger 自洽。完整 terminal universe、`CaseKey`、`Expected`、denominator 与 `StaticContract` 后来已由 static denominator authority 闭合为 `43476/43476` 与 `8668/8668`。本批 current source-only baseline 为 `82d52bad8fd6e34c3fc7e82c5346430dcb26e9cf`；sealed admission、Map 三预算 `Completed` exact 6-member family、Map single-region lifecycle exact 6-member family、Lock exact 10-member managed request-validation family，以及 Lock 104-member positive lifecycle family，连同两遍非授权 pre-manifest inventory 与 root-specific reviewed source-admission bridge，均为 `source_written/source_review_only/implementation_uncompiled/implementation_unrun`、`passed=0 failed=0 actual=not_run`。Map 六项把 installed `xShmMap`、same-target pre/post、一次性 ledger、NotPresent sentinel clear 与 mapped pointer equality 接入私有 receipt；Lock 104 项继续绑定 exact identity、selected/sibling snapshot 与一次性 native/local ledger；两者都没有 actual execution。inventory source 预期 Map `12` source-present + `43,464` planned missing、Lock `114` source-present + `8,554` planned missing；reviewed digest 未冻结，默认 Map/Lock producers 仍全 Missing，两个 full candidate 均未重跑。当前仍缺剩余 program source、current-source 编译与实际执行、frozen inventory/quotient manifests、`Qmap/Qlock`、member coverage 与 Windows class evidence。Barrier、RegistrationShutdown、RegistryLifecycle、Unmap 与 JointClose 的历史 exact-commit records 分别为 `8/8`、`8/8`、`16/16`、`49/49`、`36/36`，A2b2=`117/117`，同提交 clean wide regression=`266/266`；但 A2 仍是 `implementation_not_dynamically_accepted`，生产 VFS/open/Connection、A1 producer、v15、Runtime/Ready/派发继续不可达。精确证据见 [动态验收](node-plugin-vfs-fault-acceptance.md)。

以下四条仅保留静态闭合前的 A2b1 fragment provenance；其中 Pending/frontier 不得覆盖当前 frozen StaticContract：

- A2 Map 本批在既有ABI/raw局部fragment之后新增typed-operation outer-result fragment：exact-set冻结`NotPresent/Mapped/Failure/CaughtUnwind`四类外壳结果，caught unwind按installed-envelope Drop完成/Drop unwind展开为2条，合计5个local cell；只有Mapped写非null pointer，normal return保留installed raw slots，unwind先清slots再Drop，ABI返回只分`SQLITE_OK`与`SQLITE_IOERR_SHMMAP`。新增normal-code forward与abandon catch-fence两个ledger witness，并把三个normal chain及一个unwind chain按执行顺序exact-link；这些关联位于typed frontier之后，不加入reviewed-successor prefix。route、callback、managed outcome provenance、prestate、prefix mutation与payload Drop custody全部仍Pending；owner-graph仍为Pending=5/resolved cross-links=9，typed-operation与raw-fallback两个open frontier都未关闭。该fragment是`source_written/source_review_only/validator_compiled/shared_a2b1_guard_passed`，但共享 `4/4` 仅证明局部review自洽，不产生完整`SourceBranch`、`Expected`、`CaseKey`、denominator、`StaticContract`或`WindowsDynamic`，生产open/route/runtime保持未修改。
- A2 Map 继续新增彼此独立的 route/operation-callback normal-return 6-cell fragment：入口限定outer callback-fault pass且inner live；outer controller reject/selected/inner-missing均在slice外。exact-set为1个route preparation rejection、1个callback admission rejection和admitted operation result×callback completion `2×2`。operation Error在completion Ok/Error两支都优先，operation Ok+completion Error也为typed Failure；只有双Ok进入adapter projection Pending。全部cell保持output null、installed raw slots、cleanup none、pointer write=0，caught unwind不在此slice。ledger witness逐字锚定route gate、route→test bridge→ABI adapter→registry dispatch、callback admission/completion与三个result arm，并守卫conditional quarantine先于completion。只有route-preparation直接拒绝格的adapter-projection/payload-custody轴为NotReached；其余五个已dispatch格都会经adapter error projection或携带/丢弃返回payload，所以该合并轴保持Pending。promotion/fault内部、managed cause/prestate/retention、callback owner/route custody、adapter payload与payload Drop custody继续未裁决。两个open frontier与owner-graph Pending=5/resolved cross-links=9不变。状态严格为`source_written/source_review_only/validator_compiled/shared_a2b1_guard_passed`；共享 `4/4` 不是该fragment的denominator、`StaticContract`或动态通过，且无migration或runtime证据，production ABI/managed-fs/route/open、V279、A1、v15、Runtime与Ready均未改变。
- 该6-cell fragment经红队收紧后，parent provenance改为route、callback、managed、adapter control-flow与adapter payload custody五个累计`NotReached|Pending`轴；前五条Failure chain补齐`OuterFaultPass`→ABI Failure arm→raw accepted→normal-code forward关联，四条admitted chain补齐callback `Result<(), ManagedSqliteRegistryProcessRouteRejection>`值域与`finish_callback` delegate。跨owner route→bridge→adapter→registry→`with_shm`/`complete`使用exact caller context；同owner gate/quarantine/completion/arm与unsafe-retain early-return predicate只在各自symbol span内使用真实source needle offset顺序。六条branch chain、UnsupportedRole/ShmDetached exclusion与unsafe-retain conditional custody作为三组互斥的source-step ID inventory exact-partition selector，并不声称三类运行时分支互斥；每条chain的completion attempt数分别等于对应cell的0或1声明。该收紧只提高静态review可追溯性，不新增prefix edge、terminal、denominator key或任何执行证据。
- A2 Map 本批继续把该parent的adapter projection与payload custody拆轴，并形成exact 7-cell reviewed control/result inventory：前5个Failure逐格保持`SQLITE_IOERR_SHMMAP/null/installed/no-cleanup/0-write`，并区分callback-admission、operation与callback-completion rejection；其中operation Ok+completion Error产生后丢弃的success payload custody继续Pending。另两格为Observe-only NotPresent与防御守卫通过后的Observe/Extend Mapped，分别固定`SQLITE_OK/null/0-write`和`SQLITE_OK/non-null/1-write`。commit-bound reviewed source guard把Managed typed wrapper create→adapter carry→ABI write逐层锚定；RegionMismatch/LengthMismatch/NullPointer另形成3条child-local guard review，disposition固定`Pending/Pending/NonNull-type-excluded`，不进入7格、不改变shared parent disposition，也不代表完整exclusion或动态不可达，因此7格不穷尽双Ok continuation或guard rejection路径。dropped/mapped payload底层custody、managed prestate、route/callback custody、完整successor/terminal universe仍Pending；两个open frontier、owner-graph Pending=5/resolved cross-links=9、production ABI/managed-fs/route/open均不变。状态严格`source_written/source_review_only/validator_compiled/shared_a2b1_guard_passed`；共享 `4/4` 不是该child的denominator、`StaticContract`或动态通过，且无migration或runtime证据，V279、A1、v15、Runtime、Ready与派发均未改变。
- 服务端 v216-v218 已铺 endpoint credential/session、目标绑定 owner 重认证与单次消费账本；默认关闭的 direct-TLS owner API、Windows DPAPI bootstrap、legacy no-downgrade 与独立 v13 auth-only WSS 也已写入。v14 固定为 `planning_snapshot_bootstrap_only`，终点仍为 `snapshot_ready=false`；连接不进入 `AgentEntry`/`NodeRegistry`，不产生 signed Plan、work-admission、Runtime、Ready、route、outbox、Lease 或派发。新增仍未编译、测试、运行或执行迁移。见 `node-endpoint-session-authority.md`。
