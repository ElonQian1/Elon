# 开放商业开发者 App 限权生产凭据 V1 验收

## 当前状态

`implementation_local_tcp_runtime_subset_verified`

Rust `elon-server` 测试目标已编译；全新 SQLite 专项测试已验证一次性 `oc_live_` 明文不进入列表或数据库、数据库只保存 64 字符十六进制 SHA-256 摘要、再次签发撤销旧凭据，以及资料修订和准入暂停的撤销联动。进程内 Axum 专项已验证登录、项目与 App 管理隔离、平台管理员签发边界、默认关闭、列表脱敏、撤销原因校验、幂等撤销，以及 App 所有者 editor 和项目 admin 的管理权限。隔离子进程纵向专项显式开启生产功能开关，由服务层为已准入 App 签发同时限权到 `menu.preview` 和 `order.commit` 的 `oc_live_`：查询能力无需动作确认即可调用签名商户运行时，动作能力完成服务端确认后调用同一运行时并由独立 ERP 机器凭据接管同一个 Invocation；同一生产凭据可读取两类调用的终态摘要和结果详情。查询和动作的相同请求重放均返回原 Invocation 和结果，不再次调用商户运行时或生成第二条事件；同一 App 的沙箱凭据无法读取生产事件或使用生产游标，同一 owner、同一项目内另一个独立审核并签发生产凭据的 App 也无法读取前一 App 的列表、详情或游标。该专项还验证运行中关闭生产开关后，已签发且已使用的凭据立即无法调用能力或读取事件，而项目 owner 仍可通过管理接口紧急撤销并查看撤销状态。独立重启专项进一步验证活动生产凭据已持久化后，新进程未设置生产开关仍拒绝令牌读取事件，但项目 owner 可查看凭据元数据并紧急撤销。签发策略专项已覆盖三种风险层级的有效期边界，以及空白、重复、非法、越权、32 项和 33 项 scope 输入；鉴权专项已覆盖格式、未知摘要、首尾空白、带时区未来时间、过期、损坏时间及 `last_used_at` 副作用。历史迁移专项从真实 V153 磁盘库升级到当前版本，验证旧 App 与准入记录保留、V154 空表、重复执行、二次打开、部分唯一索引、到期索引和两条删除外键；四连接并发轮换专项验证所有签发均成功且最终恰好保留一个活动凭据。生产纵向链路验证指纹为 `56cedf27e395c89b5f51c782a4d43ece9ee0cba3f8f5ee8694002347b31b7c27`，角色权限专项指纹为 `562c43d21b1e80662b717731d049a764e9fb4f60a21fe29d5bdaa0fbc1b1d434`，重启专项指纹为 `057158212d53dd5fecbbf258998166902aa65e8533709856794d40e5be91d628`，策略专项指纹为 `3424c83800e364fc481ba3c05da964df0d15dab2d977db8a4371442adc70d7dd`，鉴权时间专项指纹为 `ff14e7aaebd976e7bc8bd312572ed7c4751f5e4d6487285c32af567ec4a6e9b0`，V153 历史迁移专项指纹为 `a40596d80434cf841d6a6fdd228d1dd8c70719c5f7446e5a5a8c228028725362`；最新并发轮换专项指纹为 `6dc953e52e864fbd19feabec778600c8d79dd1726561607d66578920f42e3c0e`，验证收据为 `85b8c96ee6ca13a4fc32891e493882bff148541518816f058cf36dc72405faef`。

## 已形成代码

- V154 新增独立生产凭据表、活动凭据唯一约束、到期索引和准入外键。
- 平台管理员可为当前有效 App 签发或轮换范围受限、限时、只显示一次的 `oc_live_` 密钥；数据库只保存 SHA-256。
- 功能默认关闭；关闭时签发和生产鉴权失败，紧急撤销不受开关阻断。
- 测试和生产凭据共用开发者调用入口，但生产凭据会复核能力范围和全部当前准入条件；调用账本、幂等重放和终态事件已经按凭据环境隔离，沙箱不得触达真实商户运行时。
- 资料修改、域名重新验证、App 停用和准入暂停会原子撤销活动生产凭据。
- PC 管理员审查区可签发并一次性显示密钥，项目管理区可查看元数据和紧急撤销；完整密钥不写入本地持久状态。
- 准入暂停已验证会撤销当前凭据，并以 `production_credential_revoked` 停用活动生产 Webhook。
- 项目成员身份不自动获得 App 凭据访问权；项目 owner 可查看和撤销，普通成员、非 App 所有者编辑者、项目外用户及未加入项目的平台管理员统一在 HTTP 入口失败关闭。
- 撤销接口按当前存储契约幂等成功；重复请求返回同一已撤销状态并保留首次撤销原因，列表和撤销响应都不会返回完整 `oc_live_` 明文。
- 显式开启生产开关的本机 TCP 专项确认生产凭据身份、App ID、凭据环境、凭据 ID 和动作确认 ID 进入商户运行信封；沙箱凭据不能借此获得生产权限，测试结束后不保留生产开关。

## 统一回归必须验证

- V153 到 V154 升级、空表、重复迁移、活动凭据部分唯一索引、到期索引、外键和历史数据库兼容已通过磁盘库专项验证。
- 默认关闭、显式开启、运行中关闭开关后的既有凭据调用与事件读取失败关闭，以及关闭期间的紧急撤销入口可用均已验证；持久化活动凭据在新进程未设置开关时仍失败关闭，且管理方可查看元数据并紧急撤销。
- Token 前缀、长度、未知摘要、首尾空白归一化和明文不落库已通过专项；串行轮换和多连接并发签发原子性已通过专项，摘要碰撞处理仍待验证。
- 资料、域名、准入和 App 状态的全部组合；资料修订与准入暂停的撤销联动已通过专项测试。
- `low`、`standard`、`enhanced` 的 1 天、上限天数、0 天和上限加 1 已通过专项；未来带时区偏移、已过期和损坏 RFC3339 到期时间，以及成功与失败时的 `last_used_at` 写入边界均已验证；可控系统时钟漂移仍待验证。
- 请求范围为空、纯空白、重复归一化、非法格式、超过 32 项、恰好 32 项、超出审核范围及合法子集均已通过专项。
- 查询调用、动作准备、动作确认、生产商户运行调用、两类调用的幂等重放、生产终态事件读取、同 App 沙箱/生产事件交叉隔离及同项目不同生产 App 的列表/详情/游标隔离已通过本机 TCP 专项。
- 项目 owner、独立 App 所有者 editor、项目 admin、普通成员、其他编辑者、非项目成员和平台管理员的签发、查看与撤销权限均已通过进程内 HTTP 专项。
- PC 一次性密钥复制/清除、页面刷新不恢复、列表状态、撤销原因、小屏布局和错误提示。

## 仍未完成

- 可控系统时钟漂移、TypeScript 和 UI 验证。
- 本机回环 TCP 已覆盖生产开关显式启用后的服务层签发、鉴权和商户运行时调用；公网 DNS、TLS、跨机器网络、真实组织审核和生产部署仍未验证。
- 外部组织身份核验、持续风险监控、mTLS、硬件密钥、IP 约束和密钥托管。
- 真实第三方平台生产授权、真实资金、支付清算、链上资产和生产部署证明。
