<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application android:usesCleartextTraffic="true">
        <provider
            android:name=".UiRuntimeInitProvider"
            android:authorities="${applicationId}.yilong-ui-runtime-init"
            android:exported="false"
            android:initOrder="100" />

        <receiver
            android:name=".UiRuntimeControlReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="com.elon.uiruntime.START" />
                <action android:name="com.elon.uiruntime.STOP" />
                <action android:name="com.elon.uiruntime.ACTIVATE_NODE" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".UiRuntimePreviewHostActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:permission="android.permission.DUMP"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />
    </application>

</manifest>
