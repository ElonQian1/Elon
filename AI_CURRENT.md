---
version_status: current
reviewed_at: 2026-08-11
---

# 一龙项目当前事实

本文件是 Codex、Copilot、Claude、Gemini 和平台内 AI 的短事实快照。开始实现或判断“项目现在有什么”时先读本文件；详细架构、接口和历史原因再按链接读取。讨论、草稿和 Git 历史不能覆盖这里及正式决策。

## 当前产品主链

- 已实现：用户通过 Android APK 或 PC 工作台描述需求，AI CLI/API 代理在真实 Git 工作区开发、验证、构建和发布应用。
- 已实现：项目、频道、多人/多 AI 协作、Windows 节点执行、上下文编译、项目文档治理与版本恢复等基础能力。
- 已实现并完成编译/假数据验收：一龙统一账号已覆盖 Google OIDC 的 Win、Android 与移动 Web 登录/主动绑定，以及 PBKDF2 密码、一次性离线恢复码、可撤销设备会话、脱敏安全事件、导出清单、注销预检和联合登录请求幂等/限流审计；认证存储能力会显式拒绝宣称默认进程内后端具备多副本安全。Win 节点 AI Provider 控制面具备显式登录状态机、原子 journal/备份恢复、Codex/Gemini 假故障矩阵、脱敏诊断和 Codex 保险箱逐操作同意/精确重放合同；ChatGPT Web/Gemini Web 仅保留不可启用的类型化授权生命周期，CLI 登录和浏览器 Cookie 均不可复用。真实 Google/OAuth、真实 Codex/Gemini/Claude/Copilot 账号、邮件/短信恢复、账号最终注销、内部 Web Chat、真机/Renderer、线上部署与安装包发布仍未验收。权威边界见 `docs/unified-account-and-ai-provider-architecture.md`。
- 已实现并完成编译/静态验收：Android APK 个人页和移动 PWA 个人页均提供可发现的“账号与安全”入口，先展示脱敏的一龙账号，再显示 Google 已绑定、未绑定或暂未配置状态；绑定继续使用 Android Credential Manager / Google Identity Services 官方流程，不保存 Google token，也不按邮箱自动合并。当前生产公开能力返回 Google `configured: false`，真实账号绑定仍须先配置 Google Cloud OAuth client、Android 包名/SHA 指纹和 Web Authorized JavaScript origins；本批模拟器视觉验收因本机缺少 emulator 延后。
- 已实现并通过编译/静态门禁：Win Tauri `/user-browser` 支持按一龙账号与厂商隔离的本地 WebView2 Profile；ChatGPT 与 Google AI 模式可见语义适配器均已接线，统一 UI 可承接可见问题、回答、引用、草稿与受限网页动作，凭证不导出。Google 账号登录固定交给系统浏览器且不共享 Cookie；Google 地区/账号开放状态、真实页面 DOM 和登录仍未验收。详见 `docs/user-browser-module-integration.md`。
- 已实现并通过编译/静态验收：Codex Win 语义控制、Tauri 白名单动作与统一脱敏日志控制台；现场验收边界见 `docs/win-codex-control.md`。
- 已完成项目记忆二轮真实验收，覆盖下方第四至十一阶段的历史“未测试”说明：Rust、PC、浏览器和真实 Codex 三个单工具 MCP 均通过，且无需打开页面。仍待 `/hooks` 人工信任、严格模型 A/B、跨 PC 与生产负载；详见 `docs/project-memory-agent-integration.md`。
- 已实现并完成定向编译/测试：Windows 节点为普通 Codex 编码任务按需注入仅含 `project_context_plan` 的项目文档 `context` profile；明确“只改一个源码文件/行号”且不涉及架构、跨模块或当前状态的任务在启动前跳过该 MCP。它最多规划 8 份文档和 2400 token 后续阅读，并把结构化响应限制在约 800–2000 token；相同请求使用 5 分钟、64 项 LRU，clean worktree 绑定 HEAD，dirty worktree 只在最多 256 个普通变更文件、合计 64 MiB 内完成安全流式 SHA-256 指纹后缓存。短期 MCP 会话目录只保存不含正文/任务文本的 plan ID、路径和内容哈希账本：即使代理忘记传 `previous_plan_id`，同会话相同计划也自动返回 `not_modified`，revision 变化时省略未变信源并报告新增、移除和内容哈希漂移；语义正文仍明确未比较。`performance_receipt` 报告规划/缓存/估算节省，并包含最终结构化内容与 MCP tool result 的实际 UTF-8 字节回执。`node_agent_project_context` 定向 Rust 回归 13 项全部通过，覆盖首次完整投递、自动复用、强制刷新、profile 隔离、响应预算和账本不保存查询/信源正文；验证指纹为 `f15400a9ce832d8a2da2b6dc5d9573ab9e2c513a9e72bce17a8b7c6274cfd6c4`。完整治理工具仍只用于显式文档任务，其他 CLI 普通任务暂不自动注入。
- 已实现并完成四端后台验收：`yilong-ui-live` v1.12 可在用户不打开微调画布或可见浏览器时发现 Web/PWA/Tauri/Android，后台捕获、语义查询和受限交互，并以 source binding、双审批精确源码补丁、构建、重捕获、本机回归和验证矩阵形成闭环。PC 共用聊天输入 dock；Web/PWA/Tauri 写回 receipt `COMPLETE`、三端 `BUILD_VERIFIED`、矩阵 `PASSED`，Tauri 另取得真实 1296×809 原生窗口和受限行为工件。Android 已在隔离 API 34 emulator 上以 Runtime 语义动作进入与生产 PWA 相同的未登录页面，源码奇偶损失为 0，裁剪系统栏后的归一化像素 MAE 为 `0.099874198 <= 0.12`；正式 UI 任务门禁为 `VERIFIED`、`completionReady=true`、跨端视觉 `PASSED`，无干净目标图时 FitRun 合同为 `NOT_REQUIRED_WITHOUT_CLEAN_TARGET`，未探测或使用在线真机。后台复用同代已安装 APK 时会恢复 `DEPLOYED`，若缺少同代安装证据则以 `DEBUG_REUSE_NOT_DEPLOYED` 失败关闭，避免重连把状态误留在 `BUILDING`。最终安装节点发布身份仍须以本任务发布回执和 7799 回读为准；权威边界见 `docs/headless-ui-design-mcp.md`。
- 已实现并通过定向 Rust/SQLite 及既有真实 Codex MCP 验收：完整治理 profile 可提交、列出原生工具理解候选；候选只在工作区外 SQLite 保存短摘要、主题、相对证据定位和当前 SHA-256，不保存正文、命令、聊天、prompt 或私有 memories。审核后仍须沿 suggestions/revision/authorization/apply/Git 链路进入低权重 `context_memories`。`native_context` 回归 12 项通过，指纹为 `00726a809c88c52fe68c01f6eb058bbf92525292c443c4cc629dd3edb9c4c38b`；完整范围见 `docs/project-memory-agent-integration.md`。PC 收件箱浏览器/UI 验收仍属第六阶段边界。
- 已实现并通过 PC 合同回归、TypeScript 检查和生产构建：项目文档工作台的本机候选收件箱支持 pending/reviewed/rejected/applied 分页、证据状态、接受、固定原因拒绝、恢复、CAS 修订和单一重定位修复；漂移项不能接受。模型测试实际执行不可信响应清洗及分页、审核、修订、修复请求形状，并确认 UI 不直连 API。候选仍只经 suggestions/revision/authorization/apply/Git 共享，拒绝/恢复不写仓库；真实浏览器、loopback API 和视觉交互尚未验收。
- 第七阶段已通过定向 Rust/SQLite 回归：receipt profile 可原子保存 1–8 条短候选，服务端绑定当前证据 SHA-256/Git identity，并只保存本机会话的 24 位不可逆指纹；pending/rejected 候选支持 `updated_at_ms` CAS 人工修订，旧页面不能覆盖新版本，后续同证据代理回执也不能覆盖人工摘要。漂移候选只能拒绝，普通 context 仍为单工具只读；回执不保存源码正文、任务、聊天、prompt、命令输出或 Codex 私有 memories，也不绕过 suggestions/revision/authorization/apply/Git。`native_context` 13 项通过，指纹为 `cf1b8197aded0ff776d48ef226a93acd71f6a197799aa14381bf41d82852fd86`；真实 Hook trust、跨 PC 并发和浏览器交互仍未验收。
- 第八阶段的节点 Hook 已通过定向 Rust 回归：`PostToolUse` 账本只保存相对路径、读写类型和不可逆指纹；`Stop` 按当前 turn 的读证据、新路径、最多六路径/360 字符和单会话三次预算门控，同 turn 不重复提示；`SessionEnd` 删除会话账本，敏感路径、响应、命令、prompt 和原始 session 不落盘。Hook 配置未携带 trust bypass，未来生命周期事件保持未配置 no-op。4 项通过，指纹为 `1af6882094023c83bfb12c7804dc2f2cb9d486da2205bb07f35d8ffa3749c6d7`。仓库插件同时兼容 PowerShell 日志管道的 UTF-8 BOM，代理集成合同通过；源码 cachebuster 已更新，但真实 Codex cache 重装、`/hooks` 人工信任、真实任务触发、跨 PC 和浏览器仍未验收。
- 第九阶段已通过真实临时 Git 工作区回归：共享导航记忆的 owner、repository/path scope、review/reviewer/复核周期和可选 expiry 保持兼容默认值；旧 manifest 可读但在 Memory CI 标记治理缺口。分页 Memory CI v2 已验证 advisory、fail_on_drift、strict，对证据漂移及 Git blob 重定位、到期、复核逾期、生命周期缺口和共享事实冲突给出不可自动执行的 repair plan 与建议退出码，检查前后 manifest 字节和 Git 状态不变；到期、漂移和冲突记忆不进入代理投影。`native_context` 14 项通过，指纹为 `00146108cb99affaac0fefd713bd60df9f938e23f5c6c1e9844b8659ebaf8fa6`。PC 浏览器修复交互、真实跨 PC 并发和长期容量仍未验收。
- 第十至十一阶段已实现并完成定向 Rust、真实 Codex 和 PC 浏览器验收：`.elon/project-features.json` 以 hash/revision、依赖、租约、固定状态机和实现/test 证据登记正式需求，不复制正文，范围或需求变化会回退重审。普通 Codex 只接入各含一个工具的 context、feature、receipt profile；完整治理目录不能通过 URL 换权进入普通任务。最新 `project_feature` 回归 5 项通过，证明详细动作 schema 只在显式 `describe` 后返回、直接内部工具调用被拒绝、高优先级漂移需求不进入上下文且会继续补足最多 3 个有效功能，以及既有完整状态机、CAS、依赖和证据门禁；指纹为 `9508cc9f8541a47858d9a0b35e7023e3380592649e3daf37e87e1ce049fffc61`。`profiles` 回归 7 项通过，指纹为 `63432e09754583ef1710d6c3fbf5fcd8798839ee9db3815a7d79690fa69cdd25`。最新插件源码 cache 重装、`/hooks` 人工信任、真实任务 A/B、跨 PC 和长期生产负载仍未验收；详细边界见 `docs/project-memory-agent-integration.md` 与 `docs/project-feature-registry.md`。
- 已完成项目记忆首轮隔离运行验收：`elon-pc-node` 真实构建、项目文档 Rust 回归和 context/receipt profile 固定隔离测试通过；唯一跨产品功能图/技术架构图的重复关系已删除，版本化检索 9/9 通过。独立 `127.0.0.1:7817` 节点无需打开页面即可 bootstrap 两个单工具 profile，stdio 代理 initialize/tools/list 分别约 171ms/166ms，越权切换返回 401；首次 context 计划约 3.6s/4652B，同会话重复调用约 1.2s/2839B，返回 `not_modified`、相同 plan ID 和零源码正文。Memory CI 空库返回 pass/0；Hook 执行程序的路径账本及 SessionEnd 清理已独立运行通过。app-server 观察器仅用合成白名单事件验证配对与不保存原始 payload，真实 Codex 插件安装、`/hooks` 信任、官方事件流任务 A/B、跨 PC 和浏览器仍未验收，不能据合成窗口宣称真实 token 或时间节省。详见 `docs/project-memory-agent-integration.md`。
- 已实现：开放商业网络 V1 的商户节点、能力、授权、调用、计量和审计主干；HTTP 与 MCP 共用领域服务。能力输入和处理器输出已按有限 Schema 配置失败关闭，无效输入不创建调用，无效输出以零金额失败并释放 Grant 预算，历史成功结果重放前按当前输出契约复验。
- 已实现、生产配置依赖环境：受控 `merchant_runtime` 可把平台调用安全转交给商户自有 ERP 后端；首个 `cofficethinking` 参考节点已覆盖商品发现、服务端报价、用户确认后的幂等下单和订单查询。连接器 SDK 的通用 Node 运行时内核统一签名、重放保护、来源身份、Grant、平台动作确认、幂等生命周期、不可变 Manifest 和响应信封；商户仍需实现业务处理器与生产持久化存储。平台现在把已经服务端确认并一次性消费的 `action_confirmation_id` 纳入 HMAC 信封，`order.commit` 不再信任业务输入自行声明的确认布尔值。Node 运行时专项 18 项、SDK 全量 55 项及 Rust 本地回环端到端专项均已实际通过；Rust 专项覆盖绑定验证、签名查询、幂等重放、动作确认、提交订单、计量与审计。`cofficethinking` 的自定义 Rust 运行时和契约副本尚未同步这一字段，当前仍要求业务输入重复确认，跨仓库 SHA-256 一致性待下一批恢复。运行时每次调用重新核对标准 443 HTTPS 白名单，并形成 DNS 私网拒绝、禁代理、禁重定向和单次公网地址固定代码，但该出站安全批次仍未完成 DNS、TLS 或网络回归。真实公网启用仍需 HTTPS 域名、白名单和双方密钥。
- 已实现、范围受限：项目成员可按商户读取终态能力调用证据、结果摘要、可选标准业务回执和当前 ERP 实例关联；新商户运行响应中的非法标准回执失败关闭。项目编辑者可保存 `project_editor_asserted` 人工衔接回执；每个数据接入也可签发权限固定为 `business_handoff.write`、1–366 天限时的一次性机器 Token，服务端仅保存 SHA-256，轮换、撤销、到期和接入停用立即失败关闭，机器回执以 `adapter_token_authenticated` 固化凭据 ID 和版本。升级前已有凭据补 90 天期限。`applied` 必须绑定有效标准业务回执和外部目标记录摘要。系统从业务证据和最新回执实时派生待处理与需重试队列，成功或忽略自动移出，失败保留重试。机器鉴权不是外部平台签名或回读；队列不自动写入 ERP/CRM，不创建第二套订单账本，也不证明真实支付、履约或退款。
- 已实现：通用 ERP 蓝图 V1.3 可按修订号追加蓝图定义、发布不可变版本、新建独立商户项目或把用户有编辑权的现有项目纳入实例治理、按固定版本检索能力、聚合经授权的脱敏通用提案、原子创建提案或实例初始化 Matter，以及保存升级前后配置快照、商户执行证据和完整回滚；现有项目纳入不会复制、覆盖或清理原项目，初始化 Matter 会先要求盘点已有能力。工作台和 AI 可从 Matter、Assignment 与 Artifact 真源读取实时状态、失败任务和证据缺口。AI 不得自动接受提案、演进蓝图、启动或合并任务、发布公共内核或采用升级。权威入口为 `docs/erp/README.md`。
- 已实现：商户数据来源接入控制面、幂等同步回执及不含密钥和原始经营数据的 AI 开发上下文；具体大型平台生产适配器仍未完成。
- 已实现基础闭环、仍有后续未验证项：开放商业具备连接器 SDK、商户目录、消费者发现、限时授权、匿名关系、低敏偏好披露、客户端加密保险箱、删除请求与商户未核验外部证明、动作确认、调用配额、终态凭证和手动封禁。保险箱使用 PBKDF2-SHA256 与 AES-256-GCM，服务端只存不透明密文和最小元数据；Node Web Crypto、服务端信封、V162→V220 合成磁盘升级、HTTP 鉴权/隔离、同 ID 多租户、并发修订、删除、100 项/1 MiB 上限、审计脱敏、TypeScript 和 PC 构建已验证，密码找回、多设备密钥同步、商户/AI 解密、真实 Chromium 下载和生产库迁移未验证。消费者新导出为 V5，V1-V4 兼容输入；导入仅存当前用户和项目独享的隔离快照，可采用单包字段或从 2 至 10 个包人工合并低敏偏好并回滚，不自动恢复关系、生成 Grant、写 ERP、创建订单或结算。删除证明的迁移、权限、幂等、匿名读取、可携带包 Rust 专项和 PC 构建已验证，但固定为商户提供且平台未核验。仍缺生产审核、真实扣款、完整订单迁移、全网身份互认、外部通知、真实删除适配器/核验、联邦治理、多来源合并统一验证和跨运营方传输。权威入口为 `docs/open-commerce/README.md`。
- 已实现并通过 Rust/SQLite、本地 Axum 路由和 PC 构建验证：消费者删除请求支持 7 天内部目标、24 小时催办冷却、最多三次催办、一次超时升级、失败关闭时间解析、幂等与并发去重，以及商户限量前优先级；HTTP 路由已覆盖未登录、跨项目、冷却拒绝、幂等重放和商户匿名列表。该目标不是法定时限，升级不启动仲裁、处罚、赔付、外部通知或真实删除；跟进不进入 V5。绑定真实 TCP 端口、生产部署、PC 浏览器、窄屏、真实 V162 升级和完整历史包仍未验收。开发者终态事件已通过服务层及 2 项进程内 Axum 专项，使用真实沙箱凭据验证分页、游标/App/环境隔离、同 App 平台事件隐藏、详情越权统一未找到和响应脱敏；真实 TCP、生产开关启用后的 `oc_live_` 读取及 PC 浏览器仍未验收。权威边界见 `docs/decisions/open-commerce-consumer-data-request-followups-v1.md` 与 `docs/decisions/open-commerce-developer-terminal-events-v1.md`。
- 已实现、范围受限：PC 消费者沙盒可按商户发布的有限输入契约生成非技术调用表单，未声明默认值的可选字段默认省略，无法安全呈现的契约失败关闭。`action` 还必须经过服务端 5 分钟两阶段确认；确认绑定当前用户、App、商户、能力、Grant、幂等键和输入摘要，并与 Invocation 创建原子消费，同一幂等请求可重放但不能产生第二次动作。开发者测试 App 可按绑定游标轮询自己的终态调用摘要并读取单条结果，数据库在终态转换时原子追加一次序号；已验证部分仍是可恢复轮询，不是跨运营方事件总线。该确认不包含 WebAuthn 或跨设备可信展示，不替代商户运行时内部订单、支付、风控或履约确认。
- 已实现并通过定向 Rust/SQLite 与 MCP 回归：消费者可在五种透明非付费排序器之间显式切换，未知策略失败关闭；普通响应公开当前运营方固定 100 候选窗口、目录/合格/返回计数、截断和非穷尽边界。目录时间相同时以商户 ID 稳定候选窗口，同商户多能力在主排序值相同时先按偏好分和价格再按能力键/版本并列。消费者还可显式只保留 `current` 声明，`stale/unknown` 在能力候选层排除，开关进入原因、输入指纹和凭证。测试覆盖默认兼容、五种策略、同时间戳 105 商户、空/单/截断结果、指定能力、来源联合筛选及只读性。HTTP 实例、PC 浏览器、第三方排序、反作弊、公平性和外部真实性仍未验收。权威边界见 `docs/decisions/open-commerce-pluggable-ranking-v1.md`、`docs/decisions/open-commerce-consumer-candidate-scope-v1.md`、`docs/decisions/open-commerce-consumer-freshness-filter-v1.md`。
- 已实现并通过定向 Rust/SQLite 与 MCP 回归：消费者显式要求时生成临时排序凭证，服务端对精确规范 JSON 计算 SHA-256；凭证只携带输入指纹、候选范围、策略和有序公开结果，不含搜索词、App 或偏好明文，不持久化且固定非付费、非全网穷尽和 `signed_by_operator=false`。Chromium Web Crypto、下载篡改阻断、运营方签名和跨运营方验证尚未验收；摘要只证明字节完整性，不证明身份、数据真实性、目录完整或算法公平。权威边界见 `docs/decisions/open-commerce-consumer-ranking-receipts-v1.md`。
- 已实现并通过定向 Rust/SQLite 回归：每项公开能力可显示商户项目声明的来源和 `current/stale/unknown` 新鲜度。项目编辑者可把当前能力版本显式绑定到同项目、同商户、同接入的成功或部分成功业务同步回执及已登记数据域；绑定、重绑、解绑和审计原子提交。目录只公开厂商、接入方式、数据域、回执状态、完成时间和 SHA-256，并以回执完成时间计算声明期。能力更新或接入停用后旧绑定不再公开，无有效绑定时退回能力声明时间；重新启用只在原版本和回执仍有效时恢复。所有来源固定 `externally_verified=false`，内部回执不证明美团等外部平台签发、数据真实、库存实时或订单完成。HTTP 实例请求、旧客户端和 PC 浏览器仍未实测。权威边界见 `docs/decisions/open-commerce-public-data-provenance-v1.md`、`docs/decisions/open-commerce-capability-source-link-v1.md`。
- 已实现并通过定向 Rust/SQLite 回归：消费者可显式要求内部同步回执来源，并按一个厂商标识、一个数据域和 1 秒至 365 天的最大回执年龄精确筛选；默认关闭，空白忽略，非法标识、越界年龄、缺失/非法/未来时间失败关闭。测试覆盖同商户普通声明、有效与版本失效来源并存，接入停用、声明期四种组合、五种排序器、匹配原因、输入指纹、排序凭证及发现只读性。响应还从当前最多 100 个运营方目录候选中按公开能力汇总稳定、非穷尽的厂商和数据域建议，失效绑定和私有字段不进入建议。所有来源仍由商户项目声明且固定未外部核验；携带来源条件的 HTTP/MCP 实例请求和 PC 浏览器尚未实测。权威边界见 `docs/decisions/open-commerce-consumer-source-requirement-v1.md`、`docs/decisions/open-commerce-consumer-source-filters-v1.md`、`docs/decisions/open-commerce-consumer-source-age-v1.md`、`docs/decisions/open-commerce-consumer-source-filter-options-v1.md`。
- 已实现并通过定向 Rust/SQLite 与 MCP 回归：消费者发现可先精确匹配三位币种再比较价格微单位，可限定 `query/action` 与 `public/authorized`，也可把城市、类别和全部标签从软偏好提升为硬约束；非法值、跨币种比较、公开 `owner_only`、硬约束缺值或商户资料不匹配均失败关闭。测试覆盖默认兼容 CNY、价格边界、四种能力组合、五种排序器、来源条件联合筛选、输入指纹、排序凭证及发现只读无写入。筛选仍不提供汇率、支付、地址真实性或外部平台数据证明，也不绕过授权、Grant、动作确认和配额；HTTP 实例请求和 PC 浏览器尚未实测。权威边界见 `docs/decisions/open-commerce-consumer-price-currency-v1.md`、`docs/decisions/open-commerce-consumer-capability-filters-v1.md`、`docs/decisions/open-commerce-consumer-preference-constraints-v1.md`。
- 已实现并通过本地 Rust、SQLite 与 MCP 假数据验收：消费者发现基础输入由共享领域服务统一规范化；搜索词最多 200 个 Unicode 字符且拒绝控制字符，能力键和 App ID 复用现有规则，返回数量只接受 1 至 50，未知字段与非法币种失败关闭。真实 SQLite 查询已证明 `%`、`_` 和反斜杠按普通文字匹配；105 商户夹具证明候选窗口固定 100，结果上限和排序凭证请求指纹使用校验后的输入。HTTP 服务和 PC 页面未实际启动；规则不改变授权或排序语义。权威边界见 `docs/decisions/open-commerce-consumer-discovery-inputs-v1.md`。
- 已实现并完成编译、SQLite 假数据、MCP 路由与领域服务同源验收：消费者 AI 可通过只读 `open_commerce_discover_for_consumer` 表达透明排序、来源、币种、能力和偏好硬约束并读取候选范围、授权状态和未签名排序凭证。默认身份映射为 `pc-web`；显式 App 必须启用且属于当前用户和项目，入口参数不能覆盖身份。响应不泄漏内部用户/项目 ID，读取无业务写入；工具不申请授权、调用、下单或结算。HTTP/PC 页面、真实来源、真实交易和生产部署未验收。权威边界见 `docs/decisions/open-commerce-consumer-discovery-mcp-v1.md`。
- 已实现并完成编译、SQLite 假数据与 MCP 路由验收：消费者能力执行计划只读复核输入、App、封禁、授权和下一次调用预算；MCP 显式 App 必须属于当前用户和项目，历史批准申请不能替代当前有效 Grant，多 Grant 优先选择仍可覆盖调用的一张，被封 App 失败关闭。动作计划按准备、用户同意、确认、调用推进且不占预算或创建业务记录。真实外部平台、支付和生产部署仍未验收；详见 `docs/decisions/open-commerce-consumer-capability-execution-plan-v1.md`。
- 已实现并完成编译、SQLite 假数据与 MCP 路由验收：消费者 AI 经明确确认后，可由当前项目内本人启用的注册 App 申请单个 `authorized` 能力；任一 Grant 仍可用时拒绝重复申请，全部预算不可用时允许续额申请且不改写旧 Grant。顺序重放复用一条 pending 并审计一次，安全响应不返回内部身份；商户仍独立决定批准、期限和预算。并发首次申请、真实外部平台、支付和生产部署仍未验收；详见 `docs/decisions/open-commerce-consumer-authorization-mcp-v1.md`。
- 已实现并完成编译、SQLite 假数据与 MCP 路由验收：消费者 AI 可只读列出当前项目内本人拥有的开发者 App，并查看当前 MCP 身份和沙箱可用状态，以选择 `x-elon-app-id`。显式身份必须属于当前用户和项目；同项目其他用户、本人其他项目和未知 App 均失败关闭。响应不含测试 Token、Token 提示、生产凭据或其他用户标识，重复读取不改 App 或审计状态。该工具不创建、轮换或启停 App；真实生产凭据和部署未验收。权威边界见 `docs/decisions/open-commerce-consumer-app-directory-mcp-v1.md`。
- 已实现并通过定向 Rust 回归：消费者 AI 可按 `pending/approved/rejected/canceled` 只读列出当前用户通过当前项目本人 App 发出的授权申请，并单独列出仍有效 Grant 的期限及调用/计量预算。测试覆盖同项目其他用户、同用户其他项目、四状态筛选、有限/耗尽/无限预算、过期与撤销过滤、按 App 过滤和只读无写入；撤回本人 pending 请求要求编辑权限与固定短语，重复撤回只形成一次消费者/商户双侧审计，非 pending 不变且不撤销 Grant。撤回和 Grant 响应已统一为最小安全投影，不返回商户/消费者项目、申请人、授权人或审核人内部 ID。历史 `approved` 或列表中的有效 Grant 仍须由执行计划按拟调用输入再次复核。权威边界见 `docs/decisions/open-commerce-consumer-authorization-status-mcp-v1.md`。
- 已实现并通过定向 Rust 回归：消费者 AI 可通过 `open_commerce_get_my_action_confirmation` 重新读取当前用户与当前 MCP App 持有的单份动作确认。测试已覆盖 `pending`、`confirmed`、`consumed` 和即时派生的 `expired` 状态、稳定下一步、其他用户/App/未知 ID 不可枚举、原始输入与内部身份不泄漏，以及过期读取不改持久状态、不新增审计或 Invocation；带次数和金额预算的 Grant 连续读取后使用量和更新时间均保持不变。动作准备、确认、调用和幂等重放仍复用原服务。
- 已实现并通过领域、MCP 及 2 项进程内 Axum 定向回归，PC 边界已完成静态契约和生产构建：当前用户明确停止时，可通过 MCP、消费者 HTTP 或开发者凭据 HTTP 幂等取消尚未创建 Invocation 的 `pending` 或 `confirmed` 动作。各入口共用最小取消投影，对外明确返回 `status=canceled`、`invocation_created=false` 和 `next_step=stop`，不暴露数据库兼容状态、输入形状、请求摘要或内部身份。测试覆盖重复取消、错误短语、其他用户/App、自然过期、已消费确认、单次审计、重新准备、MCP 取消/读取、并发二选一、v166 重复迁移、独立 WAL 连接竞争，以及会话/Token 鉴权、App 隔离和零 Invocation。取消仍不撤销已创建调用，不退款、不回滚商户订单或外部动作；真实商户补偿仍待后续实现。权威边界见 `docs/decisions/open-commerce-server-action-confirmation-v1.md`。
- 已实现并通过 2 项定向 Rust/全新 SQLite 本地存储回归及 6 项进程内 Axum 专项、范围受限：开发者 Webhook 新订阅验证前停用且不能手动启用，验证后只接收匹配 App、沙箱环境和成功/失败范围的终态；耐久队列已验证单租约领取、死信确认不可覆盖、人工重试清除确认并复用原投递、成功完成后旧租约失败关闭。HTTP 已验证项目编辑权与 App 所有权双重边界、列表/健康/投递读取、停用与幂等停用、死信确认绑定和理由不可覆盖、人工重试只排队一次，以及最多 100 条历史补发的权限、跨 App 隔离、分页续传和重复入队拒绝；创建、启用、验证和轮换在权限或前置状态不足时失败关闭且不泄露密钥。创建、启停、验证和密钥轮换的 SQLite 自锁已修复；其余环境绑定、退避、自动停用、密钥派生、生产双开关与资格联动代码均已编译，生产默认关闭。正向密钥创建/轮换、真实 challenge、历史库升级、并发压力、DNS、TLS、HTTPS 外呼、跨语言签名、真实 TCP、工作器网络和 PC 页面仍未验证。它不是工商身份、跨运营方总线、支付通知或 ERP 写入证明。权威边界见 `docs/open-commerce-developer-webhooks-v1-acceptance.md`。
- 已实现并通过 Rust/SQLite 本地状态与 2 项进程内 Axum 资料接口专项：开发者 App 资料修订会退回草稿、清除当前域名证明、把既有准入退回修改并撤销活动生产凭据；资料 HTTP 已验证登录、项目与 App 管理隔离、旧修订拒绝、提交、平台管理员队列和批准，并修复只读成员或非 App 所有者编辑者被误报为 `400` 的权限映射。过期 challenge 会在白名单、DNS、TLS 或 HTTPS 处理前失败关闭且不记录网络尝试。challenge 的本地签发与状态验证只用于夹具，真实 DNS、TLS、HTTPS、公网地址固定、历史迁移、项目 admin 写入和 PC UI 仍未验收；资料审核与域名状态不等于组织身份、域名法律所有权或外部平台授权。
- 已实现并通过 Rust/SQLite 本地状态与 2 项进程内 Axum 专项验证：当前资料与域名状态可提交并批准主体准入，资料变化会失效既有准入，准入暂停会撤销活动生产凭据并停用生产 Webhook；HTTP 已验证登录、项目与 App 管理隔离、申请人确认、重复提交、平台管理员队列、批准与重复审批，并修复普通成员或非 App 所有者编辑者被误报为 `400` 的权限映射。主体信息仍是申请人声明，未连接工商或第三方身份核验；项目 admin、真正并发、历史迁移、真实 TCP 和 PC UI 未验收。
- 已实现、默认关闭，并通过 9 项 Rust/SQLite 就绪投影及进程内 Axum 专项验证：`oc_live_` 完整密钥只显示一次，数据库仅保存 SHA-256，串行轮换只保留一个活动凭据；资料修订和准入暂停会失败关闭凭据与生产订阅。生产就绪八步投影已验证完整阻断顺序、稳定下一步、调用/通知分离、显式双开关组合、暂停回退、资料/域名/准入全部非批准状态、凭据到期/撤销，以及生产 Webhook 待验证/活动/停用矩阵；就绪 HTTP 已验证登录、项目与 App 管理隔离、跨项目和不存在 App 的同响应、停用与重新启用后的当前状态读取及响应脱敏，另有 2 项凭据 HTTP 专项验证平台管理员签发边界、默认关闭、列表脱敏、撤销原因校验和幂等撤销，并修复权限错误误报 `400`。测试不修改进程环境，也没有开启 `OPEN_COMMERCE_PRODUCTION_CREDENTIALS_ENABLED`，未证明显式开启后的服务层生产签发、生产鉴权、真正并发状态竞争、真实 TCP/浏览器、真实商户运行时、资金、链上资产或部署。
- 已实现并通过 6 项 Rust/SQLite 与进程内 Axum 专项、PC 严格类型检查、开放商业契约回归和生产构建：消费者可为隔离导入生成低敏偏好差异和关系重新授权候选，只能在明确确认及修订号未变化时逐项采用类别、标签、城市、价格上限和公开商户偏好；服务端只合并真实变化的所选字段，未选字段保持当前值。采用保存完整前后快照，旧 V141 记录继续从快照派生实际字段；无当前档案时可安全新建并在回滚时删除，采用后档案变化则拒绝覆盖。HTTP 已验证登录、项目和同项目用户隔离，审计不含偏好值。它不恢复关系、Grant、披露，不写入订单、ERP 或结算；真实 TCP、浏览器交互、生产历史库和跨运营方传输尚未验证。
- 已实现并通过 SDK Node 回归、PC 类型检查/生产构建和双向互操作：PC 与连接器 SDK 可用本地口令把普通或已签名消费者数据包包装为 PBKDF2-SHA256 + AES-256-GCM 离线归档，并在上传前于消费者设备解密。新写入 V2 会把算法参数、盐、Nonce、明文摘要和创建时间纳入固定顺序 AAD，严格限制 Base64、迭代参数和 6 MiB 明文边界；两端继续只读兼容 V1。口令不进入服务端；当前没有找回、同步或云托管，也未实测浏览器文件操作、跨设备传输和上传后服务端导入全链路。权威边界见 `docs/decisions/open-commerce-consumer-portability-archive-v2.md`。
- 已实现并通过 4 项 Rust/SQLite 与进程内 Axum 专项、PC 严格类型检查、开放商业契约回归和生产构建：消费者可把本人隔离包内来源关系人工映射到当前有效发布商户，再用本人独立开发者 App 发起范围不超过旧关系的新授权申请。映射按项目和用户隔离，同一来源只保留一个有效映射；未确认、其他用户 App、目标下线、越权范围和已撤销映射均失败关闭。旧关系和旧 Grant 不复制，目标商户仍需独立决定。目录搜索和可选指纹匹配都不构成主体认证；真实 TCP、浏览器交互、跨运营方身份互认和生产公共网络尚未验证。
- 已实现并通过 6 项 Rust/SQLite V5 服务端定向回归：V4/V5 可携带经私钥持有证明的商户 RSA 公钥指纹，V5 另含商户未核验删除证明。零条/一条证明导出、幂等摘要、安全字段、语义篡改、超 5000 条和 V4 字段夹带失败关闭已验证；普通及来源运营方 RSA 签名 V5 包可完整进入用户/项目隔离快照并保留删除证明，不复制关系、Grant、删除请求或证明主表，同一信封不能更换来源运营方身份，撤销密钥不能用于新导入。只有来源运营方签名可信时才生成已发布商户的精确指纹候选，映射仍需消费者确认和商户重新授权。SDK 签名及 PC/SDK 归档互操作有独立证据，但客户端加密 V5 归档上传全链路、V1-V4 固定字节样本和 PC 浏览器下载仍未验证。
- 已实现、只预演：项目 AI 资源控制面可盘点自己的 Codex、本人节点、已授权共享 Codex 和平台模型并保存策略；它不启动任务、不验证外部额度，也未接管真实调度。
- 已实现、默认关闭，Node 离线适配与服务端本地任务路径已验证：AI 任务与开放商业调用的链外影子经济层，可生成幂等用量凭证、验收后双分录，以及未提交网络的 Sui 链下投影包；追加式争议可阻断投影，已接受争议可创建独立纠正 Matter，人工验收后原子追加冲销与替换双分录而不改写原凭证。两条腿可共同保存为原子纠正包；系统还能从任意标准、冲销或替换凭证只读解析根、纠正步骤和当前有效凭证，异常循环或分叉失败关闭。标准包和纠正包可导出统一离线交接文件；项目还能签发限时、限网络和限包类型的离线预检机器凭据，外部工具只有在服务端只读重建交接包且摘要一致时才能追加 `passed/rejected` 报告。项目编辑者可将当前可导出的投影显式加入预检队列，机器按权限领取 60 至 900 秒短时租约，交接摘要漂移会阻断任务，报告与任务完成原子写入。连接器 SDK、CLI 和自动续租工作器已通过本机假服务及 Node 测试；客户端会拒绝摘要漂移、空领取夹带任务、跨任务响应和不匹配报告，机器凭据不接受命令行参数。服务端已有 21 项 Rust 测试，覆盖 V159 新库/V158 升级与约束、Axum 鉴权、租约并发/恢复/隔离/失效、摘要与争议阻断、纠正包领取、幂等冲突、两阶段写入故障回滚和本地 SQLite 一致性备份恢复；真实一小时工作器、操作系统级磁盘故障、生产库备份恢复和 PC 交互仍未验证。报告和任务不改变投影状态，也不授权签名、广播、最终性或资金移动。当前没有 Sui 网络适配器，不移动真实资金。
- 已实现但边界有限：API Token 保管、远程节点、计算使用台账，以及默认关闭的节点模型算力共享策略。节点所有者可选择共享模型、最大并发和每日 Token 预算；指定与自动调度会在同一事务中检查今日实耗、活动租约预留和本次保守预留，所有者自用不受共享限制。可信终态先冻结实际 Token，再进入异步结算；提前断流、真正租约过期和服务重启会失败关闭并幂等释放预授权，最终状态不接受迟到覆盖。所有者控制面会从持久化记录派生近 24 小时失败、预留超出和过期租约健康快照。该快照不是 SLA 裁决或自动赔付。它目前仍不是包含异构任务、竞价、故障赔付、真实提现或链上结算的开放算力市场。
- 已形成基础代码、未编译且未完整接线：任务级分布式算力联邦已定义 Provider、Workload、版本化 Offer、标准 Compute SKU、期货/远期 Price Snapshot、Job、Reservation、带 fencing generation 的 Attempt Lease、分层执行用量与结算回执，以及旧 LLM V1 的部分事实投影。共享 CapacityPool 已形成 v165-v168 SQLite schema 与本地 Store，并以追加式多 meter 账本和稳定 Claim 管理供给、持有、释放与到期；Reservation Claim 强制绑定 Offer、Job 和 Reservation，公开 standalone 方法拒绝绕开 Broker 单独改变该类 Claim。
- 已实现并通过 2 项 Rust/SQLite 服务回归、3 项进程内 HTTP/MCP 接口回归，以及 PC 全前端严格类型检查、lint、生产构建和 bundle budget：本人 Provider、Pool、Bucket 和 Supply 控制面可在全新磁盘库执行迁移，完成双 meter 供给追加、撤回、幂等重放、显式确认、Bearer 鉴权、MCP 工具发现与本人隔离、失败原子性、账本审计及数据库重开；`/my-compute-settlement` 与 `/compute-supply` 已产出独立页面 chunk。真实 TCP、PC 浏览器交互和发布仍未验证；self-declared/available 不等于硬件 verified、市场报价、可预留容量或收入。边界见 `docs/distributed-compute/supply-control-plane-acceptance.md` 与 `docs/distributed-compute/pc-compute-build-acceptance.md`。
- `implementation_partially_verified`：v177-v181、v203-v205 激活、隔离和恢复已通过 Store/Service、本人/管理员 HTTP/MCP、角色隔离、幂等及文件重开专项；PC `/compute-supply` 与 `/compute-activation` 已静态生产构建。并发压力、真实 TCP、浏览器和生产库副本仍未验证；流程不连接节点、不派发、不发布 Offer 或移动资金。见 `docs/distributed-compute/activation-control-plane-acceptance.md`。
- `implementation_partially_verified`：v170、v182-v184 Offer 已通过 Store/Service、本人和管理员 HTTP/MCP、角色隔离、幂等重放及文件重开专项；PC `/compute-supply` 与 `/compute-offers` 已静态生产构建。并发压力、真实 TCP、浏览器、生产库副本和发布仍未验证；流程不生成 Snapshot、不取消已有预留、不派发或移动资金。见 `docs/distributed-compute/offer-control-plane-acceptance.md`。
- `implementation_partially_verified`：v171 Snapshot 与 v172-v176 Broker 已通过 HTTP/MCP、并发和文件重开专项；v223/v224 reference fallback 又通过四眼 HTTP/MCP、原子 Snapshot、拒绝零副作用、旧 TTL 触发器升级和重开专项，固定 `fallback_curve/sample_count=0`。PC `/compute-reference-curves` 已通过跨层契约、严格类型、lint、生产构建和 bundle budget；真实 TCP、浏览器、生产库副本、真实价格、撮合、派发和结算仍缺。见 `docs/distributed-compute/compute-management-mcp-acceptance.md`、`docs/distributed-compute/platform-reference-price-curve-api-acceptance.md` 与 `docs/distributed-compute/broker-control-plane-acceptance.md`。
- `design_frozen/source_not_written`：v225 Provider Capacity Commitment 已冻结为不可变 revision 1 `committed` 主表与唯一 revision 2 `canceled|expired` 终态回执表，current status 只由 LEFT JOIN 派生；数量继续以同一 Capacity Claim lines 为唯一权威，余额继续以同一追加式 ledger 为唯一权威。Create 必须同事务复核 current Provider/Offer/Pool、未过期 `capacity_future` v171 Snapshot、已批准应用的 exact v223 binding 和完整 meter/window，Cancel/Expire 必须与 Claim/ledger 终态原子提交并封死 generic Hold/Finish/recovery 绕路。`external_pool`、DeliveryAllocation、资金与结算均在禁线外；当前没有 Rust、迁移、API 或测试源码，未编译、未测试、未执行迁移、未运行。权威见 `docs/distributed-compute/capacity-commitment-authority.md`。
- `implementation_unwired/partially_verified`：Attempt v175-v215 已形成 sealed Plan、route/credential/actor authority、Start outbox 与 no-start/accepted 原子闭包，并通过源码编译、全量迁移、两次重开及冲突 backfill 门卫；accepted 成功闭包和生产链路未运行。v221/v222 的 16 个 HTTP 操作、Store 重开，以及 5 个本人和 17 个平台 MCP 工具已通过专项；PC `/compute-external-pools` 已通过跨层静态合同、严格类型、lint、生产构建和 bundle budget。生产部署、真实 TCP/浏览器、v213 route、可信 artifact/verifier/route producer、worker/ACK、Runner 与真实付款仍缺。见 `docs/distributed-compute/compute-management-mcp-acceptance.md`、`docs/distributed-compute/attempt-gateway-acceptance.md` 及同目录 v221/v222 权威与验收文档。
- `implementation_uncompiled`：算力 Attempt PC 流程已覆盖 Provider `/compute-execution`、消费者 `/compute-reviews` 与 `/compute-challenges`，以及管理员 `/compute-observations`、`/compute-verification`、`/compute-receipts`、`/compute-finalization`、`/compute-settlement-issuance`、`/compute-challenge-resolution` 和 `/compute-corrections`。v194 改状态/容量，v195 扣结预授权，v196/v197 登记申诉与终态，v199 退款平台内余额并冲减 pending；角色历史队列重审计 v195-v199 并在 PC 展示完整证据链，均不证明外部付款。全部页面尚未构建、联调、视觉验收或发布，细节以 `docs/distributed-compute/README.md` 为准。
- 节点插件已铺 V1 policy、v5/v6 catalog/rollback、sealed SQLite/VFS 与线性 custody；既有 69 项 SQLite 专项已验证，v216 install/promotion 又通过 11 项版本链测试。v217/v218 reauthorization、PlanApply、work-admission 与 A1 projector 尚未编译、迁移或运行，且没有 producer。A2 总合同已冻结；未编译、未运行的 A2b2 静态源码在 A2b1 exact route→live WAL-main 与 map/lock seam 基础上补入 barrier、完整 unmap、WAL-main 联合 close、callback/registry route/logical route/registration lifecycle 和全量 typed custody/count records。它只是静态源码，不是 Windows 动态证据；逐 case Win32/SQLite、多 Connection 与注销验证仍缺。生产 VFS/register/live `sqlite3_file`/process/root/time/rollback/node-profile/open 不可达；v14 永久 blocked-only，Runtime/Ready/派发仍缺。见 `docs/distributed-compute/node-plugin-planning-snapshot-authority.md` 与 `docs/distributed-compute/node-plugin-vfs-fault-authority.md`。
- `implementation_unwired`：服务端 v216-v218 已铺 endpoint credential/session、目标绑定 owner 重认证与单次消费账本；默认关闭的 direct-TLS owner API、Windows DPAPI bootstrap、legacy no-downgrade 与独立 v13 auth-only WSS 也已写入。当前批次冻结 v14 `planning_snapshot_bootstrap_only`：同一 exact endpoint generation 只能按 sharing→preparation→Planning Snapshot V2 六消息摘要链运行，每段 ACK 与下一 intent 必须在同一 IMMEDIATE 事务重验 v216 head/receipt/current credential并写 v219 provenance。NodeAgent 仅复用失败关闭 Bootstrap，终点仍为 `snapshot_ready=false`；连接不进入 `AgentEntry`/`NodeRegistry`，`connected=false`，不产生 signed Plan、work-admission、Runtime、Ready、route、outbox、Lease 或派发。新增仍未编译、测试、运行或执行迁移。详见 `docs/distributed-compute/node-endpoint-session-authority.md`。

## 已接受的产品方向

- 先用 AI 应用开发、ERP、营销内容、小游戏和经营分析帮助商户获得直接价值，再逐步形成开放商业网络。
- 商户数据由商户控制；消费者 AI、商户 AI 和第三方应用通过授权接口连接，不把所有参与者锁进同一个 App。
- 开放商业当前权威入口是 `docs/open-commerce/README.md`；实现状态先看 `docs/open-commerce/capability-baseline.md`。
- 商户 ERP 采用“公共内核 + 行业插件 + 商户主题 + 私有扩展”的分层模型；`cofficethinking` 与最小零售参考实例已证明同内核可保持不同主题和插件，商户私有扩展不随公共版本升级被覆盖。
- 一龙采用任务级分布式算力联邦：用户节点、受管集群和外部矿池统一为 Provider；客户端只在用户开启共享后按需下载插件与模型；公网先聚合独立任务，期货交付窗口的不可变价格快照作为首期结算基准。完整决定见 `docs/decisions/distributed-compute-federation-v1.md`。

## 尚未成为当前事实

- 接入器任务领取、短时租约、1 小时硬期限内续租、一次性租约密钥、原子完成、主动释放、拒绝退避、第 6 次拒绝暂停、编辑者恢复、Connector SDK 客户端、通用 ERP/CRM 适配器工作器和项目侧租约监控已经落位。Node 客户端和工作器已验证领取边界、任务/凭据/完成回执响应绑定、自动续租、硬截止释放、稳定回执键、幂等完成重试、永久拒绝、容量释放和进程停机；服务端 6 项 Rust/全新 SQLite 与 2 项进程内 Axum 专项已验证显式 scope、单任务租约、续租/释放/重领、原子幂等完成、凭据与项目隔离和脱敏。历史迁移、真实 TCP、并发压力、PC 浏览器、真实 ERP 和外部回读仍待验证，不能宣称生产适配器可用。既有凭据不会自动获得读取权，只有项目编辑者明确轮换并加入 `business_handoff.claim` 后才能领取单条衔接任务。
- 消费者 AI 的跨项目基础目录与授权沙盒已实现；节点模型推理的显式供给授权已实现。异构算力联邦、通用节点插件合同、共享容量账本、本地原子 Reserve、未执行任务 Release/Expire 与 staging 无用量 Attempt 中止已写入，但插件下载与隔离执行、异构任务真实派发、已开始执行的 Attempt 终态和运行中 Broker 结算、外部矿池、验证计量、期货曲线/订单簿、自动收费与跨主体结算仍未实现。
- 通用 ERP V1.3 用初始化 Matter 和可机读物化合同衔接真实项目开发，并从既有任务与产物记录派生可恢复状态；它能登记现有平台项目，但不会自动导入本机目录、复制商户源码、启动 Assignment、执行数据库迁移或部署生产系统。`cofficethinking` 仍需先在一龙创建或绑定对应平台项目，再由有编辑权的用户显式纳入。Matter 仍需商户批准和启动，真实商户项目继续走现有项目开发和发布流程。
- Sui Move 合约、测试网/主网提交、NET、CREDIT、RevenuePosition、链上治理和收入权益均是草案；链外 Sui 信封不等于已上链、已发币或公司股权。
- 任何提案只有经过正式 ADR、实现引用和验收后，才能进入本文件的“已实现”部分。

## 明确禁用

- 不设置独立“预言家 AI / Demo Oracle / DemoPreview”前置角色。见 `docs/decisions/reject-demo-oracle-role.md`。
- 不恢复 AI-to-AI Skill 路线或 Skill 市场作为当前主架构。多 AI 协作继续使用项目频道、Matter/Assignment、运行路线和供应商工具协议。见 `docs/decisions/reject-ai-to-ai-skill-route.md`。
- Codex 跨项目自动派发、PC 自动续跑和后台自进化仍暂停；本次仅授权 Win 语义控制、Tauri 调用和统一日志，边界见 `docs/win-codex-control.md`。

## 读取顺序

1. 当前状态：本文件。
2. 项目定位：`AI_PROJECT.md`。
3. 系统分层：`AI_ARCHITECTURE.md`。
4. 实现入口：`AI_INDEX.md`。
5. 开放商业专题：`docs/open-commerce/README.md`。
6. 通用 ERP 蓝图：`docs/erp/README.md`。
