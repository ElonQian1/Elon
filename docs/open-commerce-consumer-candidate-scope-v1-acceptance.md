# 开放商业消费者候选范围摘要 V1 验收

## 当前状态

`implementation_uncompiled`

## 已形成代码

- 普通发现响应中的稳定候选范围结构。
- 候选上限、目录候选、合格、返回和截断计数。
- 固定当前运营方目录范围与非穷尽标志。
- PC 无框候选范围摘要组件。

## 统一回归必须验证

- 空目录、无合格结果、少量结果、刚好达到返回上限和超过返回上限。
- 请求 limit 取 1、默认值、50 和越界值时的候选上限。
- 目录候选数、合格数和返回数的包含关系。
- `results_truncated` 只由合格数与返回数决定。
- 排序凭证和普通响应计数一致。
- PC 空结果、截断标志、非穷尽文案和窄屏布局。

## 未完成

- 本批所有编译、接口、兼容性、凭证、浏览器和 UI 验证。
- 全局游标分页、跨运营方聚合和全网基数估计。
- 目录完整性、商户数据真实性或排序公平证明。
